# Feng 对照实验扩展协议（2026-09-07）

本轮按用户明确选择使用 map2、南宁，1×、2×，全部十个共同种子。基础速度为 1.5、2.5、3.0 m/s，共 480 格；动态/静态速度偏差已取消。用户随后把断线对照缩减为 G31 与 HCA 两方法，仅用 2.5 m/s；16 个整日场景共 1,280 格，CIE-DH 与 Tarau 不进入断线批次。每个格点参与方法使用相同原始袋、canonical 分段、释放 D、终止时刻 98259 s；不挑种子、不删故障不可达袋。

四方法为 G31 S4 原生系统、独立修复副本 HCA V3、V5 语义部分重构基础上的 CIE-DH V6、Tarau 2010 路由适配。Tarau 的 M1 仲裁与 G31 原生 M3 不同，因此主表是系统比较，不能当作纯路由消融。CIE-DH 的重构假设与 Tarau 的适配边界必须留在结果表注释中，不能宣称作者原实现的精确复现。

主 THT(D) 为每个原始袋各业务段的 `finish − canonical D` 之和。全体原始袋、全部 canonical 段完成，才报告全人口 min/mean/max；此规则也适用于 2×。任何未完成时，主 THT 留空，报告固定时限吞吐量、完成率、未完成数。原生 admission 起点只作补充诊断。两段 EBS 输入保留原始释放合同；不得为改善排名改变释放或筛选共同完成子集。

源文件核对发现：原始 STD 与厂家航班输入逐袋一致；EBS 出库 D 却等于 STD−2700。论文文字的“提前45分钟送达”与留存故障工作簿的 COUNT(完成)/28506 计算不一致。因此分列固定时限完成率、STD 前送达率、严格按论文文字 STD−2700 前送达率，最后一项不能声称已对齐原故障表的操作定义。

论文故障描述为整日中断，未披露16场景具体首通知时刻。主实验选择首次路由前共同获知失效边，全天不修复，名称为**首次路由起已知的全天断线适应**。各方法同时得到同一失效有向边集合，可在可用拓扑上计算自身原有路由量；全部袋留在原始分母。不以预删拓扑的成绩冒充运行中突然断线后的重规划能力。

另外保存有界的中途通知微例，展示旧路径、故障通知、后续改路或停止。原 b00 G31 的严格静态势下降规则已经出现 map2 合法绕行需要先升势而无法继续的反例，该失败保留。用户现已授权增加仅故障通知后启用的势函数修复：在已知失效图上更新 service-aware 势和可达性，保留严格下降，正常态沿原势；无路袋保留在原合法队列等待通知或恢复。此新增机制采用独立二进制和明确方法身份，通过真实故障与无故障等价门后才用于大批次。它包含通知驱动的全图预处理，不能把整体故障机制仍称为只有一跳计算。HCA 原始运行中故障逻辑存在多个通知时未传全集等限制；主全天合同不调用该路径。

map2 论文线路编号依据原图拓扑及原故障工作簿的边元组交叉重建，不能继续把编号1–8直接当 Java 边行号。实际 Java 图为69条边，论文为72条；影响闭包按实际图重算，保留与论文表的差异。南宁使用既有、未参考算法结果的八条预选内部线路，属于场景迁移，不能称为论文原机场线路。故障具体映射、图闭包及源文件 SHA 在独立源合同和故障协议中冻结。

基础速度在执行器内改变实际运行时间，不对既有结果事后乘比例。CIE-DH V6 的0.2秒离散步保持不变，格距为 v×0.2米，边长及安全间距向上取整；alpha 按物理头距除以速度，beta=2alpha，V5的固定2秒交接假设不变。2.5 m/s 必须先通过 V5等价、边界机制及完整人口核对，再扩大批次。

原始 Java、只读镜像及旧冻结结果保留。新结果写入独立 `feng_paper_suite_20260907` 命名空间，冻结输入、源码/二进制、协议、逐格命令和逐段终态。进程失败、预算终止和人口未完成分别标识；不把进程退出等同于实验有效，不直接改旧统计。

正式执行计划以 `outputs/evidence/paper_suite_20260907/frozen_plan_v4/plan.json` 为准，SHA256 为 `ad400a4363958087625903833fa4730e10c3787864e74180b3f5fa4cfcde4e71`。早期 v1–v3 规划记录保留并标记替代关系。此前完成的四格 HCA/DH 输入、协议和运行器字节与 v4 一致，经重新读取验证后复用。

G31 与 Tarau 使用同一独立 38b07ddf 原生构建；Tarau 保持 M1、JIT FIFO，故障势修复开关关闭。两图、三速度及在途截断的八组 b00/38b 有界对照具有相同逐袋与决策结果。旧 b00 保留作诊断对照，不把 Git 源码差异直接当作旧二进制确有某个错误的实测证据。

原生合流审计的三个层次分别报告：执行中的守恒与活动授权一一对应、终态是否所有授权已经结束、生命周期日志是否完整。固定时域可能正好截在合法边遍历内，允许在途授权仍有效；只有全人口完成时才要求没有活动和待处理授权。有限日志达到上限时保留 `protocol_integrity=false` 和丢弃条数，不能据此宣称完整轨迹验证。结构不变量的失败始终阻止该格进入正式比较，不能归为一般未完成。
