# G4IRSF32 执行、负门与处置台账

更新时间：2026-08-30（Asia/Shanghai）

本台账记录 `G4IRSF32_cross_map_next_stage_action_plan.md` 的实际执行过程。
原始失败、NO-GO 和证据缺口不得删除或改名；修复只能追加新记录，并须说明
根因、最小处置、复杂度变化和复验证据。未实际执行的 gate 不写成通过。

## 当前目标与边界

- 实现基线：`46cc46ab6bc121628fd6357e9f3c7636745fd732`。
- 干净分支：`codex/g4irsf32-v3r2-minimal`。
- 冻结协议提交：`0058bfb790d1d3abe94bfaf353f8e44bf101586c`。
- 协议 SHA-256：
  `D24F9D3A33FDB0EDC2C60A20FB0D246CA4E10CE88D936C361B0EF3E6B69F8D5E`。
- V3R3 测量语义修订提交：`84ac8b9`；协议 SHA-256：
  `AC4C92B3E3090254799B1C12C723786D3A4116BBC9C1C1AAF6BF454AFC5E5C1E`。
- V3R3 执行登记提交：`a47d4c5`；SHA-256：
  `8264616947C6F84D97A2B7642BCBAC07DBD45A54C5DF17B8F0829ED511E5FACE`。
- 当前阶段：P0、Stage 0/1、Candidate A 与包括V3R22在内的多轮Stage 2
  core screen已完成；Stage 2效果门尚未通过，Stage 3–4保持未授权。当前冻结
  约束内的最小后继入口已再次穷尽，继续需新的图资产crosswalk或明确扩展状态边界。
- 复杂度边界：append-only、default-off、G31 exact-off；复用既有 DIRECT/J2、
  source/junction queue、pending-request marker 和 destination calendar；不增加
  model、全局 A*、地图 ID 分支、未来路线、持久策略状态或 V1/V2 兼容死代码。

## 问题与处置

| ID | 首次状态 | 问题 / 负门 | 根因 | 最小处置 | 复杂度变化 | 当前证据与结论 |
|---|---|---|---|---|---|---|
| G32-I001 | REJECTED | 早期 V1/V2 候选把 P0 做成约 54k 行的多版本实现与证据堆叠，无法满足“不要越改越复杂”和单一可归因改动。 | 在一个分支内连续保留被否决机制、兼容路径和生成产物。 | 不在原树继续打补丁；从 G31 基线另建干净 V3R2 分支，只移植一个 frozen shadow hypothesis。旧提交保留作历史，不复制到交付分支。 | 大幅净删除设计；最终以 `git diff --stat 46cc46a...` 再审计。 | 干净分支父提交为 G31；当前仅有冻结协议和正在实现的最小变更。尚未完成最终复杂度审计。 |
| G32-I002 | NO-GO-PRE-RUN | V3R1 把 synthetic storage 固定为节点 `0`，不能执行必需的 map2 sentinel（storage=`52`）。 | 实验 fixture 角色被误写成核心运行时常量。 | V3R2 改成请求显式传入非空、唯一且属于 starts 的 `storage_source_nodes`；核心不得按 map/node ID 分支。 | 用一个通用角色列表替换地图特化常量，无新增策略层。 | V3R2 协议已预冻结 map2 文件/profile/potential/8-row workload 哈希；运行时与 sentinel 尚待构建复验。 |
| G32-I003 | NO-GO-PRE-RUN | V3R1 runner 读取通用 decision trace 的 `schema_id`，而不是 G32 namespaced sidecar schema。 | 两个 append-only schema 字段命名相邻，校验路径绑定错误。 | runner 只读 `trace_context.source_aware_destination_service_schema_id`；通用 schema 保持不变并 fail closed。 | 单字段纠正，不增加兼容分支。 | focused runner 单测已在旧工作树验证；仍须移植到干净分支后复验。 |
| G32-I004 | TEST-GAP | V3R1 有 J2 staging rollback 注入，但缺少 DIRECT after-stage failure 注入，Stage 0 transaction gate 不完整。 | 两个真实 commit seam 的失败测试不对称。 | 在 test-only 配置增加一次性 DIRECT after-stage 注入，沿既有事务回滚路径验证 row/census/calendar generation/event/state 恢复。 | 只增加 test hook，不增加生产策略或事件。 | V3R2 实现中；尚未构建，结论保持未通过。 |
| G32-I005 | ENVIRONMENT | Windows 默认 pytest 临时目录出现 `WinError 5`，会把环境权限问题误报为代码失败。 | 系统临时目录 ACL 与 pytest cleanup 冲突。 | 所有 focused pytest 显式使用工作树内 `--basetemp tmp/pytest-*`；仍保留原错误文本作为环境记录。 | 仅测试命令变化，生产代码零变化。 | 旧树 runner focused tests 在显式 basetemp 下 18 passed；干净树仍须重跑。 |
| G32-I006 | EVIDENCE-GAP | Table 5.3 缺少原论文归档输入/释放流/实现/随机流，不能声称 exact reproduction。 | 论文公开页面未提供补充包；常见 Elsevier `mmc1` 端点均返回 404；本机相关 ScienceDirect archives 不含该论文。 | 保留论文报告数作 archived context；继续按题名、PII、DOI、本机归档可复验搜索。若核心输入仍不可得，按原计划永久标记 `TABLE_5_3_ARCHIVE_UNAVAILABLE_NON_EXACT_RECONSTRUCTION`。 | 不新增算法代码。 | 搜索尚未最终封存；不得产生 exact verdict。 |
| G32-I007 | EVIDENCE-GAP | Table 5.4 没有与 S4 同扰动语义位置、同随机流的 HCA arm。 | legacy dynamic/static 变体及随机流未归档，现有 bias 只是描述性重建。 | 只在发现相同 seam 的原实现后配对；否则保持 `NO_MATCHED_HCA_SEAM_TABLE_5_4_REMAINS_DESCRIPTIVE`。 | 不制造替代扰动，不增加算法复杂度。 | 当前仍 NON_EXACT；不驱动主结论。 |
| G32-I008 | EVIDENCE-GAP | map2 `pair_5_7` 的 worksheet 标签 `33->44,46->36` 与全局 line 5/7 映射 `14->46,33->44` 冲突。 | 源 workbook 只保留列 B 时间样本与 cached 13939/28506，没有 task IDs、fault event 或完整配置。 | 两种合理边解释都做 fresh probe；禁止挑选有利版本。两者均得到 8013，均不能复现 cached 13939，因此继续 NM。 | 零策略变化；只保留 provenance sensitivity。 | `ARCHIVED_ONLY_SOURCE_PROTOCOL_UNRESOLVED` / `NOT_MEASURED`，除非取得缺失来源协议。 |
| G32-I009 | NO-GO-CANDIDATE | TSA PGDS 的 SFO 图只够作历史 topology canary，不能作为完整 held-out map。 | 图像分辨率低、缺完整逐边方向与 fixture 参数，也无足够的图特定再分发依据。 | 不采用、不按结果换图；继续寻找有真实 provenance、明确许可、完整有向图/环/角色/参数的候选。 | 未写 importer 或地图特化代码。 | `NO_GO_REAL_HELDOUT_MAP_FIXTURE_INCOMPLETE`。当前 SCL 候选同样因逐边完整性和许可边界未过门。 |
| G32-I010 | SOURCE-FOUND-NON-EXACT | 本机归档找到论文全文，可直接核对 Table 5.3/5.4 的实验描述和数值，但仍不足以 exact rerun。 | PDF 给出算法语义、速度/偏差矩阵和汇总表；没有逐任务输入、释放流、随机种子/随机流、构建身份或原始输出工作簿。项目与 Downloads 的定向扩展名检索也只发现两份 Nanning 拓扑工作簿及无关个人表格。 | 冻结本机 PDF 身份并人工核对相关页；把论文表值保留为 archived context，不把现有 no-A* 诊断当成同 runtime-family 重放。 | 零算法变化；仅增加来源证据。 | `C:/PROGRAMING/czr005/.local_archives/g4irsf13/pdfs/thesis.pdf`，SHA-256 `37E61B8E4D67E56C0FA14C43B230BE965E200106704363F06B80A4E6A151E1AA`，67 页。PDF p43–45（印刷页 29–31）核出 Table 5.3：3.56/4.43/8.62 与 3.13/3.96/5.98；Table 5.4：4 个速度 × 3 个偏差的 dynamic/static 12 对数值。结论仍为 `TABLE_5_3_ARCHIVE_UNAVAILABLE_NON_EXACT_RECONSTRUCTION`。 |
| G32-I011 | SOURCE-AUDIT-NO-MATCHED-SEAM | Table 5.4 的论文语义已明确，但仓库快照仍不存在可配对的原 HCA/static seam。 | `ICS_PathFinding.java` 仅在提交 `8254a7a` 引入；本地归档只有两个内容等价版本（SHA 差异来自 CRLF/LF）。活动路径调用 static update，`bias_time=0`；dynamic 调用被注释、使用未冻结的 `3*Math.random()`，主要重规划段也被注释，不能表达 4×3 固定矩阵的同随机流双臂。 | 审计 git 历史与本地归档的所有同名源文件；不激活残缺代码、不合成“同随机流”，不让描述性重建驱动主结论。 | 零算法变化；关闭继续打补丁寻找该 seam 的路径。 | `git log -S update_route_and_constrain_dynamic` 仅命中 `8254a7a`；归档实质内容唯一。结论保持 `NO_MATCHED_HCA_SEAM_TABLE_5_4_REMAINS_DESCRIPTIVE`。 |
| G32-I012 | CANDIDATE-PREREGISTERED | 先前因自行加严为“必须逐 conveyor CAD/完整设备图”而把 SCL 候选判成 NO-GO，超出了原计划 §12.4 的真实要求。 | §12.4 要求真实来源、明显不同的拓扑、有向环/merge/service scale 和尽可能明确的角色；允许透明标注的子系统级事实抽象，不要求复制 CAD。 | 采用 Cavada/Cortés/Rey 2017 的 SCL BHS 作为 `REAL_PROVENANCE_ABSTRACTED_TOPOLOGY` 候选；只预注册 37 节点/54 边事实关系、9 个回环、角色和 30 s generic prior，不复制图片、不导入、不运行。最终算法与参数冻结后才可物化，结果不好也不得换图或调参。 | 新增一份预注册文档；算法/运行时代码零变化。 | `docs/G4IRSF32_scl_heldout_preregistration.md` SHA-256 `E381729961023C289C04594C0179863AFC0CB339372CFA6EEC9DA3EC2F3FAFE4`；状态 `PREREGISTERED_CANDIDATE_ONLY_NOT_IMPORTED_NOT_RUN`。科学选择门 GO；公开分发派生图仍 `RIGHTS_CLEARANCE_REQUIRED`。 |
| G32-I013 | FALSE-NO-GO-PRE-RUN | distant locality probe 给新增分量换了 scenario，原 prefix 又哈希 scenario、decision ID 和全局 event seq，因此即使 L 的动作/状态完全相同也会被身份噪声错杀。 | locality canary 同时改变了实验身份与远端状态，比较投影没有区分二者。 | 仅在 distant semantic prefix 中剥离 scenario、decision ID、全局 event seq 及对应 arrive-event identity；节点、动作、时刻、候选和其余 metadata 保留，并加入“身份变化应相等、动作变化应不等”的定向测试。 | 一个证据投影 helper 参数；生产代码零变化。 | focused runner test 已通过；正式 Stage 0 仍待 clean commit 后执行。 |
| G32-I014 | FALSE-GO-AUDIT | row 自报 `epsilon` 只要求大于零、同一物理 commit 的重复 row 可被降为 repeated-bag diagnostic、ordinary exact-off 投影又漏掉独立 telemetry 和确定性内存账本，均可能产生假 GO。 | 证据校验信任候选提供的容差，bag 去重与 commit 去重混为一谈，且只哈希了少数 payload 字段。 | 固定 `epsilon=1e-9` 且 `rel_tol=0`；按 DIRECT event seq 或 J2 完整 request/lineage/generation identity 硬去重；ordinary parity 覆盖全部确定性 payload，另设 exact-off accounting 硬门。 | 只收紧 runner/join；算法状态与策略层零变化。 | 新增 loose-epsilon、duplicate-commit、independent-telemetry 回归；focused Python tests 当前通过，正式跨二进制门待最终重编。 |
| G32-I015 | FALSE-NO-GO-RUNNER | J2 outcome matcher 额外要求成功 COMMITTED lifecycle 的 `observed_exact_calendar_reservation_present=true`，真实成功路径该诊断字段为 false，Stage 0 会被非协议条件错杀。 | 把准备期可选指针诊断误当成冻结协议的 commit 身份条件。 | 删除这一额外条件；继续严格要求 exact-slot COMMITTED、完整请求/世代/owner/edge/slot/service/time 身份、calendar generation、EDGE_ENTER/EXIT 和完成事件。 | 删除一个非协议谓词，未放宽冻结合同。 | synthetic J2 matcher 已用真实 false 语义通过；正式 Stage 0 待最终 clean run。 |
| G32-I016 | EXACT-OFF-REGRESSION | 隔离实跑同一 motif/request 时，G31 off `cpp_internal_accounted_bytes/internal_state_bytes=104168`，G32 off 为 `104008`，相差 -160；旧窄投影曾误报 exact-off PASS。 | G17 旧扩展扣减公式把新增 G32 summary 尾再次计入并重复扣减。 | 以 pre-G32 summary 边界修正 extension accounting；cross-binary gate 已把两项确定性 accounting 纳入硬比较，禁止 runner 排除差异。 | 修正一个常量边界，无新运行时状态。 | 首次差异保留为 NO-GO 证据；修复后的 Release 重编/复验进行中。 |
| G32-I017 | ROLLBACK-RESOURCE-LEAK | precommit stage 曾执行 `rows.reserve`，异常或 J2 最终 recheck 拒绝后会留下 sidecar capacity；进一步审计发现 PIBT batch 内 DIRECT 可先 publish、再在 point-of-no-return 前回滚，`resize` 同样不能恢复 capacity。 | row 的逻辑 size/census 已事务化，但 vector allocation 没有与真实 commit point 对齐。 | 普通 seam 的 reserve/push 移到真实 commit 后；PIBT action 复用既有 `PIBTActionDelta` 暂存 fixed row，只在完整 batch point-of-no-return 后按 action 顺序 publish；trace limit/ordinal 计入同批 staged rows，删除 resize 补偿。 | 不新增 policy/event/registry；每个既有 bounded PIBT action delta 至多附一个固定 POD observation。 | DIRECT、J2 recheck 与 post-publish PIBT rollback 的 capacity/resource exact native proof 正在重编复验；未通过前 Stage 0 不得判 GO。 |
| G32-I018 | TEST-HARNESS | J2 post-stage recheck 测试第一次从尚未 finalize 才复制的 lifecycle 结果表判断即时拒绝，误报 production failure。 | 测试读取了错误生命周期时点。 | 改读当时已经发布的 deterministic decision reason；保留 sidecar size/capacity、census 和 resource exact 断言。 | 仅测试观测点变化。 | 首次 native failure 已保留；修正版随 I017 一并重编。 |
| G32-I019 | FALSE-GO-RESOURCE | shadow resource gate 曾只看总量比，namespaced internal/sidecar 分解可以同步伪造而不触发失败。 | 证据层把候选自报分量当成独立事实。 | 强制 `incremental_local=0`、`internal+sidecar=total=ordinary cpp bytes`，并由原生 trace-limit/capacity rollback 测试约束 sidecar 实体；repeat gate也比较全部 NS resource 字段。 | 只收紧审计与原生不变量，无新策略状态。 | focused resource/污染测试通过；正式 Release 身份绑定与 Stage 0 尚待执行。 |
| G32-I020 | FALSE-GO-POPULATION | 只核 `requested_count` 可让错任务、重复 segment 或伪造 local winner 混入 120-case。 | 计数没有绑定冻结有序 manifest，也未从 ordinary queue 事件重放 winner。 | 逐项绑定 `(runtime,segment,task,release,deadline,start,goal,source)`；从 `LOCAL_QUEUE_UPDATE` 重建 source enqueue/dequeue，再核 `choose_index`、winner 与 enqueue time。 | runner-only 审计，无运行时分支。 | 固定 local task/queue 值的 native 与 Python 回归通过；escape-token 可验证边界另记 I039。 |
| G32-I021 | FALSE-GO-PROVENANCE | DIRECT/J2 同一物理 commit 可重复发布；重复 local bag 又可能掩盖无效 external provenance。 | bag outcome 去重、commit identity 和逐行 external episode 验证的顺序混杂。 | 每行先验证完整 DIRECT/J2 episode/request/slot/世代身份，再按 outcome-blind bag repeat 分类；物理 commit identity 独立硬唯一。 | join-only 收紧，未改统计阈值。 | 合法 repeat 使用独立 external episode；missing/mismatched repeat 与跨 seam duplicate 回归均 fail closed。 |
| G32-I022 | TEST-HARNESS | 一次只构建 Python/证明目标后直接跑全量 CTest，其他 exe 显示 `Not Run`，容易误写成代码回归。 | 构建集合小于测试清单。 | 先完整构建全部 target，再运行 CTest；定向构建只报告为 focused，不替代全量结论。 | 构建流程修正。 | 修正后曾取得 `16/16`；本轮后续 C++ 变更仍须最终全量重建复验。 |
| G32-I023 | FALSE-NO-GO-MAP2 | shadow 校验曾只接受 node type 7，冻结 map2 storage start `52` 实为 type 1，因此 sentinel 必然被错杀。 | 把两种真实 storage/EBS start 角色错误收窄成单一 node type。 | backend 与 C++ 都接受显式 start 中的 `{1,7}`，继续拒绝其他 type、重复、空列表和地图外节点。 | 删除错误限制，无地图 ID 分支。 | type1/type7 accept、type4 reject 以及 frozen map2 node52 实跑回归通过；正式 sentinel 待最终 binary。 |
| G32-I024 | FALSE-NO-GO-J2 | J2 summary 曾用 runner 重新构造的默认 request，而非实际执行的 J2 request，导致真实行被错误核验。 | `run_case` 没有把 executed off/shadow request 带到 summarizer。 | 结果显式携带两份实际 request；summary、episode、profile 和 identity 全部从实际请求计算。 | 数据流修正，无兼容支路。 | 真实 J2 hard-pass 定向测试通过。 |
| G32-I025 | FALSE-GO-EXACT-OFF | ordinary hash 相同仍不能证明 G32 扩展在 off 下完全不存在；污染的空字段/零字段可被漏检。 | exact-off 只看普通投影，未检查 namespaced surface absence。 | 隔离 worker 返回 `extension_absent`；G31、G32 omitted/explicit/repeated off 四臂都必须完全无 G32 顶层、summary、context 字段。 | worker-only 硬门。 | 污染 fake 回归被拒；正式跨二进制四臂待最终构建。 |
| G32-I026 | EVIDENCE-IDENTITY | request 允许 legacy/额外 key、source bundle 缺传递依赖、implementation identity 只看局部 clean，均可让未冻结代码进入 GO。 | 输入 schema、哈希集合与允许改动集合未分离。 | request 要求 exact key set；source bundle 广泛哈希运行时/adapter/data/build/test依赖；实现 diff 使用协议显式窄白名单并要求整个 worktree clean、G31 parent 为祖先。 | 证据约束增加，算法零变化。 | 旧 adapter-only diff 现为 unexpected；destination focused test 已纳入 hash/允许范围；正式 clean commit 后复验。 |
| G32-I027 | FALSE-NO-GO-LOCALITY | future/distant prefix 初版仍含全局 ordinal/priority seq，且漏 `hold_attempts` 与候选节点关系，既可错杀也可漏掉局部动作差异。 | 证据投影混入实验身份，同时投影过窄。 | 剥离仅全局身份噪声，保留 decisions、holds、events、observations、candidate next/records；identity-only 改动应相等、动作改动应不等。 | 一个 semantic prefix helper。 | real direct/distant 前缀定向复验得到相同 hash；正式 probes 待 clean Stage 0。 |
| G32-I028 | FALSE-GO-CENSUS | summary 的 considered/direct/J2/partition 可同步伪造，删除真实 row 仍可能保持表面守恒。 | census 没有绑定 ordinary commit seam。 | 从 ordinary 非 goal `EDGE_ENTER` 的 `one_step_reservation_committed` / `one_step_merge_grant_committed` 重建 DIRECT/J2 全量 census，要求 unclassified=0 且与 summary/rows 精确相等。 | runner-only 独立重建。 | deleted-row 与同步伪计数回归均被拒。 |
| G32-I029 | EVIDENCE-BUILD-IDENTITY | pyd/native proof 可能来自不同源码或在 Stage 间被同路径替换；最初把 Git 探测写入 CMake 又越过冻结 scope。 | 构建身份未由工件自报且 binary hash 冻结过晚。 | pybind 与两个 native proof 自报同一 40-hex build head；正式构建用命令行编译宏注入，不修改 CMake；campaign 开始即冻结 pyd hash，Stage0/1 后复哈希且所有 worker/proof绑定它。 | build/evidence plumbing；CMake diff 已撤销。 | A→B 同路径替换回归阻止 Stage1；最终 commit 后仍须重新注入 head、clean build并核三方相等。 |
| G32-I030 | TEST-GAP-STATISTICS | Spearman tie、线性 percentile、固定 bootstrap、Wilson 和 120-case边界若只用 happy path，统计实现可偏移仍 GO。 | 缺 golden/boundary vectors。 | 固定 ties、bootstrap draws/seed、percentile、23/24 directional、127/128 bags、缺 service/pop/flow、非零 control 回归。 | tests-only。 | focused statistical suite 通过；冻结阈值与 seed 未改。 |
| G32-I031 | TRANSACTION-NESTING | outer J2 先 stage、inner PIBT 先 publish 会产生重复 ordinal；inner trace budget又未计 outer slot，低 limit 可在真实 commit 后溢出。 | ordinal 在 stage 时分配，嵌套事务没有聚合 reservation。 | ordinal 只在唯一 publish seam按实际 append order分配；PIBT commit 前纯读预检 outer+inner exact stored 数；固定 8-slot 栈数组 stage，sidecar仍是唯一新增动态存储。 | 无新 policy/event/registry；把 heap delta 内 row 移到固定栈数组。 | 真实 map2 拓扑三动作 fixture：limit=2 得 DIRECT/J2 ordinals `1,2`；limit=1 在 row/EDGE_ENTER publish 前抛出并回滚；该 exe 已纳入 Stage0 proof，最终 build-head 绑定待重编。 |
| G32-I032 | FALSE-GO-REPEAT | repeated shadow 只比较 ordinary/rows/join，NS census/resource 分区漂移仍可通过。 | repeat 投影遗漏 namespaced summary/context，且未独立重跑 census/resource audit。 | 比较 ordinary、完整 NS extension、rows、join 四组 canonical hash；repeat 单独执行 census 与 resource decomposition。 | runner-only 硬门。 | 单改 stored counter 或 sidecar bytes 均 NO-GO；完全相同重复通过 focused tests。 |
| G32-I033 | FALSE-GO-SAFETY | Stage0 safety 漏 global scan/future schedule/teacher/two-step/limit/stale grant counters；map2 `exact_node=None` 又绕过全局 service overlap/duplicate/reservation 守恒，且忽略 raw service=0 节点的 runtime minimum service。 | runner hard gate 不是 native clone gate同构，service audit只覆盖 synthetic L。 | safety fail-closed覆盖全部 native counters；所有 `JUNCTION_SERVICE_COMPLETE` 按 `max(node service, minimum_service)` 重建区间，逐 `(bag,node)` 唯一、逐节点无重叠、reservation count守恒、goal无服务。 | 审计扩展，无算法代码。 | 污染逐字段、重复/overlap/count mismatch、map2 node52 minimum-service 回归通过。 |
| G32-I034 | FALSE-GO-BINARY-REPEAT | Stage0 后替换 pyd 或只改变重复跑 NS 字段，原 campaign 仍可能组合出混合二进制/非确定性 GO。 | binary hash 在 Stage0 后才取，repeat gate投影不完整。 | campaign 开始冻结路径/hash；Stage0后、Stage1后复哈希；repeat使用 I032 完整投影。 | 证据控制流收紧。 | mock A→B 阻断 Stage1且严格 JSON 可序列化；正式工件待最终 build。 |
| G32-I035 | FALSE-NO-GO-IDENTITY | runner 曾要求 local/external raw `task_id` 不同；冻结 map2 同一 raw task 的 storage_in/out 是不同 segment/runtime bag，会被错杀。 | 把 task identity误当 runtime bag identity。 | 只要求 `local_runtime_bag_id != external_runtime_bag_id`，继续完整绑定两者 segment/task关系。 | 删除非协议谓词。 | 同 task异 runtime 通过、同 runtime 失败的回归通过。 |
| G32-I036 | EVIDENCE-SERIALIZATION | `observation_ordinal` 只与 opportunity组成联合唯一，跨 event重复 ordinal 可漏过；零完成 case 又生成 `Infinity` 导致 NO-GO 无法落盘。 | 唯一键过宽且 resource错误值进入 strict JSON。 | ordinal case内单独唯一并按 vector严格连续；opportunity允许 nested同 event共享；`completed<=0` fail closed为可序列化 case error。 | runner/test-only。 | 修复与严格 JSON 写出回归进行中，完成前不跑正式 Stage0。 |
| G32-I037 | EVIDENCE-COMPLETENESS | GO 结果只留 rows/pairs hash，map2 no-mutation gate无比较证据；后段异常还会清空已完成 case/rows。 | runner把原始可重算证据当临时变量。 | GO路径保留规范化 rows/pairs 与 ordinary comparison hashes；异常路径保留已完成 partial evidence，Stage1在 summarize 前保存成功执行的 rows。 | 仅证据载荷；不得改变判定阈值。 | 待 focused 实现与回归；在完成前最终证据仍不可称独立可审。 |
| G32-I038 | FALSE-NO-GO-WINNER | local winner重放固定 FIFO；若 ordinary `escape_token_task` 激活，真实 `choose_bag` 可合法选择非FIFO winner。 | ordinary trace未直接暴露 token 生命周期。 | 在不能完整重放 token前，把 `deadlock_escape_activation_count==0` 作为 winner audit 可证前置；若正式 case非零则 NO-GO并再实现状态重放，不得猜。 | 一个保守证据前置。 | 待加定向回归；正式矩阵结果未知。 |
| G32-I039 | COMPLEXITY-BOUNDARY | shadow storage-role 校验用临时 `unordered_set` 查重，虽只发生在初始化且不是持久 registry，仍会产生协议未授权的额外动态分配。 | 通用容器查重超过了已冻结的“小角色列表、唯一新增动态存储仅为结果 sidecar”字面边界。 | 删除临时集合，直接在已有 `storage_source_nodes` 上用 `std::find(begin,current,node)` 做确定性重复检查。 | 删除一处分配与容器状态；策略、事件、输出和校验语义均不变。 | 两个受影响的 focused native target 已重新编译并通过，最终 clean build/CTest 仍待冻结提交后执行。 |
| G32-I040 | FALSE-NO-GO-WINNER-GLOBAL | I038 的全局 `deadlock_escape_activation_count==0` 保守前置会确定性错杀合法 Stage 1 case。 | escape 是全图 ordinary 恢复机制；它可在非观测节点激活，与 L 的 source-queue winner 无关。非正式 focused 的 `s=1,n=128` 八个 mixed cases 得到激活 `[1,1,5,0,0,1,1,1]`，所有 128 bags 完成且 rows 为 32–95。 | 撤回全局零捷径，不据此改协议或门限；改为绑定观测时同一 local controller 的既有 escape-token 标量，并按真实 `choose_bag` 语义离线重放局部 source queue。 | 预计只追加一个 fixed-row 整数字段和 runner 校验，无新状态、策略、事件或动态存储。 | 全局前置已判 REJECTED；精确局部字段与正/负回归尚待实现，完成前不得正式 Stage 0。 |
| G32-I041 | PROTOCOL-INFEASIBLE-STARVATION-PROXY | V3R2 把 legacy `total_wait>120s` 的完成件诊断硬要求为 0；冻结的单服务台、`n=128` 同时/突发 backlog 在物理上必有完成件等待超过 120s，因而正确的 action-inert 实现也无法通过。 | 把“有限但超过 120s 的排队等待”错误等同于原计划的“永久饥饿”。例如 `s=1,local_only,n=128` 第 122–128 件至少等待约 121–127s；非正式 focused 恰得到 7 个 legacy starved，但 128/128 完成、0 failed。 | 不提高 120s、不改 120-case 流量、不删除该诊断、不掩盖 V3R2 的逻辑 NO-GO；拟冻结最小 V3R3：旧 counter/bag flag 继续逐件重算、完整报告并要求 shadow 不增，原 MD 的永久饥饿硬门由全人口完成、最终队列/active/pending/grant 清零、两来源均完成、0 failed/unresolved deadlock/deadline miss 判定。 | 只纠正证据语义；生产算法、动作、事件、population、数值性能阈值均不变。 | 独立源码审计与容量下界证明已确认；V3R3 erratum 尚未冻结，故当前仍为阻塞负门。 |
| G32-I042 | WINNER-AUDIT-RESOLVED | I038/I040 需要精确绑定观测 epoch 的 local `choose_bag`，同时不能被其他节点的合法 escape 激活错杀。 | 全局计数缺乏节点/queue 语义；但 controller 已有 signed `escape_token_task` 标量，真实 `choose_bag` 只在 token 存在于被选 queue 时覆盖 FIFO。 | fixed row 追加 `local_escape_token_runtime_bag_id` 并在 stage 原样复制；offline 由完整 `LOCAL_QUEUE_UPDATE` 重建 source queue，token 在 queue 时选 token，否则按冻结 FIFO，逐项核 index/winner/enqueue time。 | 一个现有标量字段；无新状态、策略、事件、registry、扫描或动态存储。 | 两个 native exes、75 项 focused Python 通过；真实 n128 样本全局 escape=1 而观测行 token=-1，winner audit 通过；G31/G32 四臂 exact-off、104168 accounting 与 extension absence 均通过。最终 commit-head 重编待执行。 |
| G32-I043 | PROTOCOL-REVISION-FROZEN | I041 的 V3R2 legacy-wait 代理门与单服务台容量下界逻辑矛盾，同时原 P0 实现漏了动作前的真实南宁 node49 shadow 证据。 | V3R2 把 120 秒完成件诊断误作永久饥饿；synthetic+map2 不能证明 node49 状态在真实投影 workload 中出现。 | 单独冻结 V3R3：120-case/population/阈值/算法全不变，legacy >120 逐件重算并要求 off-shadow不增；永久饥饿按有限全完成与最终状态清零；另冻结 G31-control-only 选择的南宁 1x/2x、2.5m/s、各64段只读 slice，G32 前不得查看结果或缩池。 | 证据语义与一个 P0 real-slice gate；生产算法零变化。 | commit `84ac8b9`，协议 SHA-256 `AC4C92B3E3090254799B1C12C723786D3A4116BBC9C1C1AAF6BF454AFC5E5C1E`；尚无 V3R3 data，runner/selection manifest 待实现。 |
| G32-I044 | EVIDENCE-HARDENING-CLOSED | I036 的 ordinal/strict-JSON 与 I037 的 raw/partial-evidence 缺口会让重复 nested 行漏检、零完成 NO-GO 无法落盘，或后段异常抹去已完成证据。 | ordinal 与 opportunity identity 混用；资源除零返回非有限值；runner只保留末端摘要。 | ordinal 必须按 case vector 严格 `1..N`，共享 opportunity 仍允许；零完成直接抛可序列化错误；Stage0 每 fixture 保留 rows/pairs/off-shadow ordinary hashes及 partial，Stage1 在 summarize 前持久化成功 rows/pairs。 | runner/test-only，不改变算法、population或门限。 | 相关重复ordinal、nested合法序号、零完成strict JSON、后段异常partial evidence回归均通过；当前三文件 focused 总计75 tests通过，正式证据尚未运行。 |
| G32-I045 | EXECUTION-AMBIGUITY-CLOSED | 冻结 V3R3 后仍有南宁 source 标签、完整 request、legacy 向量排序与 1.7 亿对选择内存边界四处实现歧义。 | G31 canonical source不是 literal local/external；主协议只引用继承请求；诊断未写排序；笛卡尔积不可安全物化。 | 数据前单独冻结执行登记：E/L 投影 external/local并双哈希；完整 V3R2 request + `[53]`；runtime ID排序；精确lazy global-rank选择并以小集合 brute-force oracle 证明等价，绑定全pool cardinality/hash。 | 只收口证据执行，无算法或门限变化；避免巨大内存复杂度。 | commit `a47d4c5`，SHA-256 `8264616947C6F84D97A2B7642BCBAC07DBD45A54C5DF17B8F0829ED511E5FACE`；尚未执行control data。 |
| G32-I046 | SELECTOR-ORACLE-CLOSED | outcome-blind lazy selector虽能避免物化约1.7亿候选对，但若只用自身结果冻结自身哈希，排序/rank偏差可被一致地隐藏。 | 缺少与实现结构不同的正确性 oracle。 | 对小型多重集用显式全量笛卡尔积、稳定排序和去重作 brute-force oracle，覆盖 ties、复用、边界与随机性质样本；正式1x/2x仍绑定全池基数和固定global-rank。 | tests-only oracle；正式算法及选择规则零变化。 | selector focused 26项通过；本轮纯重生成得到1x pairs/rows `a0d62d24...8673b` / `0a25e647...f89d`，2x `b5498d0d...9e3a7` / `70be3cac...e1eb`，与冻结值完全一致。 |
| G32-I047 | PRE-NATIVE-IDENTITY-CLOSED | control/shadow入口曾可能在核完G31路径/SHA、重生成/选择哈希、依赖 callable 和注册request尾字段之前调用native executor；失败虽会NO-GO，但已污染“一次正式执行”边界。 | 校验顺序分散在执行后审计，且一个依赖名只在晚路径解析。 | 把G31规范路径+SHA、两尺度重生成与selection/projection哈希、完整request及必需依赖全部移到第一次executor调用之前；proof executable也只允许登记的resolved path。 | 控制流前移，无算法分支或运行时状态。 | 路径伪造、哈希漂移、缺依赖和proof别名测试均断言executor调用数为0；组合Python回归通过。 |
| G32-I048 | ARTIFACT-TOCTOU-CLOSED | 证据JSON若先hash再第二次打开解析，可在两次读取间被替换；仅核loaded binary SHA也无法证明实际加载的是登记路径。 | 文件身份与解析不是同一字节快照，动态库身份缺规范路径维度。 | `_load_bound_json`只读取一次bytes，再由同一bytes算SHA并严格解析；G31/G32同时要求loaded resolved path和SHA精确相等。 | evidence-only；无生产复杂度变化。 | 单次读取、内容替换、路径alias及SHA错配回归均fail closed。 |
| G32-I049 | FALSE-GO-SHADOW-NO-EVENT | 南宁shadow零观测分支曾可把executor异常或其他硬门失败折叠成`SHADOW_NO_EVENT`，掩盖真正负门。 | 状态归类优先看row count，而不是先保留完整硬门结果。 | 仅当执行成功且除“观测数为零”外全部注册硬门成立时才允许`SHADOW_NO_EVENT`；任何异常/身份/安全/资源/ordinary差异保持原失败。 | selector-only判定收紧。 | “零row+异常”和“零row+另一硬门失败”定向回归均不再被掩盖。 |
| G32-I050 | SELF-SIGNED-EVIDENCE-CLOSED | control/synthetic artifact的顶层content hash、status和summary可自洽伪造；跨case重新绑定observations/pairs也可能保持总哈希。 | 下游composer/selector曾信任上游结论，未独立重算所有嵌套gate、向量与case关联。 | 增加严格deep loader：exact key/gate集合、父子pass、nested audits/checks、资源、服务/完成/lifecycle向量和hash全部重算；observations/pairs逐case一一绑定，禁止跨case重绑。 | 只增加纯证据验证函数，无策略/状态变化。 | 完整mock可通过；伪PASS、嵌套向量篡改、case重绑和漂移均在进入shadow前被拒。 |
| G32-I051 | FINAL-AUTHORITY-AND-PUBLICATION | 可注入测试core曾有机会产生最终GO/P1授权；两文件发布若第二次link失败可能遗留半成品；普通JSON解析还接受重复键。 | 测试依赖注入与正式授权边界不清，report/JSON没有明确commit marker。 | injectable core永远只产`TEST_ONLY`候选且无`final_go_label`；正式wrapper绑定import-time依赖/路径并用独立纯promotion validator；先发布report、最后以JSON为commit marker，只恢复内容精确匹配的孤儿report，未知孤儿fail closed；严格JSON拒绝重复键/非有限数。 | evidence composer收紧；生产算法零变化。 | composer 12项、selector集成4项和组合回归通过；shadow NO-GO、伪synthetic、二次link失败、精确孤儿恢复及未知孤儿均覆盖。 |
| G32-I052 | FALSE-NO-GO-DORMANT-NODE | 永久饥饿审计曾要求所有声明的storage source都出现在final零状态向量；合成fixture里保留但未投放bag的dormant source会被误杀。 | 把配置角色集合误当成实际活动节点集合。 | 最终零状态仍覆盖所有bag start/goal及真实事件节点，但不强求完全未参与运行的dormant source。 | 删除一个无证据依据的审计条件，未放宽任何实际活动状态。 | dormant-source正例通过；遗漏真实active node仍失败。 |
| G32-I053 | FALSE-GO-LIFECYCLE | 只核最后状态可让`REQUESTED→CONSUMED`等缺ISSUED/PREPARED/COMMITTED的伪链通过，summary的stored/dropped及transition计数也可同步伪造。 | lifecycle向量未按request identity逐链重放，summary未从原始vector重算。 | 每个identity只接受冻结的精确前缀/完整链和合法terminal尾；核grant/time一致性，并从原始lifecycle、decision和hold vectors重算全部summary计数。 | runner-only重放，无运行时状态变化。 | incomplete、乱序、错误terminal/时间/grant、跨destination identity及伪summary定向回归全部fail closed。 |
| G32-I054 | FALSE-GO-CALENDAR-AND-SAFETY | `L0/L1`早于事件、local/external区间重叠等物理不可能row曾可通过；ordinary stored counts与full-CIE A*调用也未纳入硬门。 | 行代数只核部分公式，ordinary health/safety字段覆盖不全。 | 增加`L0,L1>=event_time`、`L1>=E1`、`H_gap>=0`及严格有限值；decision/hold stored数从vector重算；full-CIE A*计数必须为0。 | 证据校验收紧，无算法变化。 | impossible-calendar、stored-count污染和A*污染回归均fail closed。 |
| G32-I055 | SOURCE-BUNDLE-AND-RAW-EVIDENCE | GO证据的source bundle曾遗漏selector/composer/evidence文档及测试；部分结果只留摘要，无法独立重算final队列、服务、完成与lifecycle。 | 身份清单和审计载荷随实现增长未同步闭包。 | 把所有传递脚本、测试和冻结补充文档加入source bundle/窄allowlist；保留规范化rows/pairs、final queue/active/pending/grant、service/completion/lifecycle vectors及各自hash；identity包含destination。 | 证据体积增加，不增加运行时策略或动态状态。 | source-bundle与deep-replay测试通过；正式artifact尚未生成。 |
| G32-I056 | P0-LITERAL-TELEMETRY-CLOSED-PRE-DATA | 原P0.3/P0.5点名的ready count/work、incoming/pending、两来源oldest age、next-free、existing calendar wait、selected action和origin在V3R2 row中部分只能间接推断，不满足逐字段审计。 | 最初最小schema优先覆盖virtual-wait因果量，遗漏原MD的字面可核字段。 | 数据前冻结V3R4 evidence-only additive row：追加13个fixed numeric POD字段，generation沿用既有字段；全部从当前节点source queue/calendar/scheduled scalar/bounded controller在commit-preflight读取，并由ordinary queue replay与代数/identity门独立核验。 | 每条既有bounded sidecar row增加固定数值成员；无cache、policy、event、registry、全局bag/task扫描或新动态容器。 | `docs/G4IRSF32_v3r4_p0_telemetry_completeness_addendum.md` SHA-256 `6817FF59F2001A9605E8FCF29A06D28DCB1A51E7E3AEA00F819003BCE6FF9811`；正式数据未运行，最终回归待本轮其余审计修复后重跑。 |
| G32-I057 | MOTIF-WORDING-CLOSED | 原MD要求保留diverter和goal，固定合成motif的node2虽为type4 diverter但路径呈线性，容易被误解成缺少第二分支。 | “角色/path完整性”与“证明替代路线性能”两个命题混在一起。 | 明确冻结解释：node2的真实type4角色和其后goal必须保留；P0不据此声称第二路线或替代路径效果，也不为满足措辞临时扩图。 | 零代码、零拓扑变化，避免无必要复杂化。 | V3R4补充协议§3已记录；后续若测试替代路径效果，必须另行预注册，不能倒推本P0证据。 |
| G32-I058 | BUILD-HEAD-PRECOMMIT | 本轮全量Release构建成功，但三个proof工件自报build head仍是旧配置`98d762a...`，不能作为最终正式G32身份。 | 构建目录是在后续文档/实现提交前配置；编译宏按configure-time值固定。 | 仅把本轮结果用于precommit回归；实现提交后以最终40-hex HEAD重新configure/rebuild，要求pyd与两个proof同头，并冻结pyd文件SHA后才允许正式control/Stage0。 | 构建流程，无代码复杂度变化。 | 当前precommit CTest 16/16通过；明确标记`NOT_FORMAL_EVIDENCE`，最终build-head复验待提交后执行。 |
| G32-I059 | FALSE-NO-GO-RESOURCE-1P10 | deep loader若把独立`trace_sidecar_accounted_bytes`也直接套用shadow/off `<=1.10`，off固定为0而合法shadow为正时会得到无穷比并误拒；同时又不能因此放掉真实总资源超限。 | 把用于精确分解的sidecar诊断分量误当成冻结的独立ratio gate；原门实际约束events/completed、junction local、runtime internal和含sidecar的total。 | 保留off-sidecar为0、shadow `total=internal+sidecar`的精确分解与非负校验；ratio循环只排除独立sidecar，四个冻结资源量仍逐项执行固定`<=1.10`硬门。 | deep-loader一个固定集合差集和既有代数校验；不改阈值、实验设计或运行时算法。 | 实现/定向测试：`100/0/100→102/5/107`合法证据通过，`106/5/111`仍以hard gate拒绝；正式control、Stage 0/1均`NOT_RUN`。 |
| G32-I060 | DORMANT-NODE-INDEPENDENT-REAUDIT | 独立复核确认，声明为storage source但从未投放bag、无事件且不在运行状态中的dormant node若被强制要求出现在final零向量，会产生负gate误拒。 | 配置角色全集与本次执行实际活动节点集混用。 | 沿用I052的最小修复：bag start/goal和真实事件触及节点仍必须完整进入最终零状态核验，仅不要求完全未参与执行的配置节点。 | 只移除一个无证据依据的集合包含条件；不增加状态、不扫描全局对象，也不放宽任何active node。 | 实现/定向测试：dormant source正例通过，缺失真实active node仍fail closed；正式control、Stage 0/1均`NOT_RUN`。 |
| G32-I061 | MIXED-ORIGIN-IDENTITY-SWAP | request与payload若把local/external标签成对互换，二者仍可逐字段自洽，旧的“payload等于manifest”检查会接受估计对象身份被整体倒置的伪证据。 | 身份绑定只验证相对一致性，未验证冻结canonical origin角色的绝对语义。 | 在保留exact ordered bag manifest和row/event identity重放的同时，canonical mixed-origin case强制shadow row的local bag确为`local`、external bag确为`external`，并继续绑定`LOCAL_QUEUE_UPDATE`来源身份。 | runner-only增加两个固定角色断言；无新schema、策略分支或运行时状态。 | 实现/定向测试：payload与request同时交换两来源仍被拒，正常manifest及重复task-id的不同runtime bag保持通过；正式control、Stage 0/1均`NOT_RUN`。 |
| G32-I062 | HISTORICAL-CURRENT-LIFECYCLE-SEPARATION | 累计lifecycle transition历史与最终时刻的pending/active/outstanding/consumed库存若共用一组计数，可把已消费/已终止请求误算成当前未清空，或用伪summary制造GO。 | historical transition totals、每个request的last state和current live inventory属于不同语义域，先前未在证据中显式分离。 | 按含destination的request identity重放合法链；单独输出`historical_last_lifecycle_state_counts`，再从每链last state重算`final_consumed_count`、`final_committed_active_count`、`final_terminal_count`和`final_outstanding_count`并与当前summary逐域核对。 | runner-only一次有界向量重放，复用既有raw lifecycle；不新增生产状态或控制策略。 | 实现/定向测试覆盖CONSUMED、EXPIRED、post-commit rollback、非terminal尾、缺链/乱序及伪summary；仅测试通过，正式control、Stage 0/1均`NOT_RUN`。 |
| G32-I063 | PROOF-BINARY-TOCTOU | proof EXE或加载的G32 pyd若只在执行前取一次SHA，可在子进程运行窗口被替换，最终证据仍可能引用旧身份。 | 登记路径/单点哈希没有闭合“实际执行期间字节未变”的时间边界。 | 对两个登记resolved proof EXE和G32 pyd分别冻结执行前/后SHA并要求相等；同时要求两个proof与pyd的40-hex build head一致，deep loader再次核前后哈希、路径和head。 | evidence-only常数次文件哈希；不改变native算法、case数或门限。 | 实现/定向测试：执行中替换EXE及artifact中篡改EXE/pyd前后SHA、路径或build head均fail closed，campaign层pyd漂移也阻断后续stage；正式proof和Stage 0/1均`NOT_RUN`。 |
| G32-I064 | COMPOSER-RESUME-ORPHAN | synthetic冻结后中断若一律重跑会碰撞append-only工件并重复昂贵阶段；report先发布、最终JSON未落盘时又可能留下孤儿，盲删或盲复用都会破坏证据。 | composer缺少经独立验证的checkpoint续跑语义，双文件发布也缺少唯一commit marker和可证明安全的孤儿判定。 | 仅在现有synthetic artifact通过文件SHA与deep validation后续跑并跳过Stage 0/1；shadow中断从shadow恢复；发布顺序固定report先、authoritative JSON最后，只有孤儿report字节与待发布内容精确相同才恢复，未知孤儿fail closed。 | composer-only小型状态机，复用既有artifact/checkpoint；不增加生产运行时状态、算法分支或正式case。 | 实现/定向测试覆盖已有synthetic续跑、synthetic后进程中断、第二次link失败、精确孤儿恢复和未知孤儿保留拒绝；正式组合campaign、control、Stage 0/1均`NOT_RUN`。 |
| G32-I065 | V3R4-EXPLICIT-CALENDAR-WAIT-REAUDIT | 独立复核确认，若只保留`L0`和`event_time`供事后相减，仍不满足原MD对existing calendar wait的字面逐字段证据要求。 | V3R2最小row把该量视为可推导值，遗漏显式字段及对应硬门。 | 沿用I056的V3R4 additive POD字段`existing_calendar_wait_seconds`：commit-preflight直接记录，并强制有限、非负且精确满足`existing_calendar_wait_seconds=L0-event_time`。 | 每条既有bounded sidecar row仅增加一个固定数值成员及常数代数检查；无cache、动态容器、全局扫描或策略变化。 | C++导出、pybind、Python schema/代数和篡改测试已实现并通过定向回归；正式control、Stage 0/1及南宁shadow均`NOT_RUN`。 |
| G32-I066 | NATIVE-STDOUT-STRICT-JSON | 原生proof stdout曾用裸`json.loads`；重复键会被last-wins折叠，原始歧义在证据重序列化后消失，且解析层接受`NaN/Infinity`。 | proof固定字段门检查的是解析后的对象，不足以证明原始JSON只有唯一、有限的规范解释。 | native proof和off-worker request统一使用duplicate-key拒绝hook及non-finite constant拒绝hook；重复键或非有限常量在任何schema/gate判断前fail closed。 | runner内两个纯解析hook；不改proof schema、native执行、case或算法。 | 重复`schema_id`和`NaN`定向测试通过；正式native proof、Stage 0/1均`NOT_RUN`。 |
| G32-I067 | REGISTERED-CLI-BOOTSTRAP | selector/composer在pytest导入时正常，但从登记脚本路径直接执行会因仓库根未加入`sys.path`而`ModuleNotFoundError: scripts.eval`，正式CLI无法启动。 | 两个新入口只计算了`ROOT`，遗漏与synthetic runner一致的root/src自举。 | 两个脚本在任何项目import前仅插入解析后的仓库根和`src`；新增真实子进程`--help`启动测试。 | 每个入口4行环境自举；无包重构、安装步骤或算法变化。 | 两个登记CLI直接`--help`均exit 0，聚焦测试通过；正式control/campaign仍`NOT_RUN`。 |
| G32-I068 | PRELOADED-PYD-IDENTITY-CACHE | 同一Python进程若已缓存`czr005_cpp`，磁盘pyd随后变为登记字节，`import_module`仍可能执行旧内存image，而path/SHA字段重新读取磁盘后表面一致。 | Python扩展模块缓存把“磁盘身份”与“实际首次加载image”之间留下边界；普通CLI通常新鲜，但公开正式API未强制。 | 唯一有FINAL_GO权限的formal wrapper在任何build-head读取/native调用前要求`czr005_cpp`尚未加载；命中即fail closed并要求新进程，后续全campaign复用这次唯一加载。 | 一条formal preflight；不卸载扩展、不热替换pyd、不引入新执行层。 | 预加载同名模块负测通过；正式campaign将用新CLI进程，control/Stage 0/1仍`NOT_RUN`。 |
| G32-I069 | STAGE0-RAW-REPLAY-CLOSED | Stage0的native/cross/map2/fixture/probe曾有多项只检查上游`pass/gates`；删除raw fixture、置假raw proof assertion或保留协调后的摘要仍可能被deep loader接受。 | 下游复核覆盖了顶层hash和部分向量，但未冻结Stage0 exact key/角色集合，也未从已保留rows/pairs/audits重算所有可重算关系。 | selector固定4次cross run、9个fixture、4个motif role与Stage0 exact schema；重算proof schema/test/assertions/exit、cross路径/SHA/request mode/ordinary/accounting、DIRECT/J2唯一发布、repeat、future/distant、map2 frozen/no-mutation/join/census/resource摘要；proof EXE当前文件SHA绑定before/after。runner只把已计算的map2`join_status/census`加入证据。 | evidence-only纯验证及两个现成值的序列化；生产算法、case、阈值、事件和状态均不变。 | selector 28项聚焦测试、py_compile、diff-check通过；正式proof/control/Stage 0/1均`NOT_RUN`。 |
| G32-I070 | RESUME-REEXECUTE-EXACT | I064的“已有synthetic深验后跳过Stage0/1”仍受限于artifact只保存部分raw preimage；协调修改摘要可能保持内部自洽。 | 仅继续堆叠raw capsule和第二套完整重算器会放大证据代码与schema，且仍难形成独立执行见证。 | 续跑先deep-load已有append-only synthetic，再用同一固定executor、G32、identity、proof/G31参数完整重跑Stage0/1；重跑结果经validate/content-hash后，完整mapping与canonical pretty原始字节必须同时等于冻结文件，才进入shadow；永不重写旧工件。此条明确取代I064中“不重跑Stage0/1”的恢复细节，保留其append-only/orphan发布边界。 | 用确定性重执行替代更多raw sidecar/schema；增加的只是崩溃恢复时计算成本，无生产复杂度。 | composer 19项测试覆盖参数绑定、两类崩溃、bytes-only非规范文件、非确定输出、冻结文件保留及fresh/resume最终字节一致；正式campaign`NOT_RUN`。 |
| G32-I071 | STAGE1-DERIVED-CONSISTENCY-REAUDIT | Stage1 deep loader能核hash/cardinality，却曾接受`completed=false`、service complete<start、未知lifecycle state、错误global completion count、错误origin或污染V3R4 row等“派生字段自洽但物理矛盾”的证据。 | completion/service/lifecycle/row/pair各验证器未把现有向量跨域回绑，部分checks仍被当成可信摘要。 | 仅用artifact已保留字段补严：从completion/service/junction/lifecycle向量重算完成、deadline、时长、来源与final accounting；V3R4 rows重放exact schema/有限值/代数/零副作用；pairs重放identity与算术；global count与origin交叉。缺失完整raw preimage的摘要不扩schema，由I070续跑精确重执行闭合。 | selector-only纯函数和负测；不新增runner字段、sidecar、runtime状态或策略。 | selector整文件`28 passed`；矛盾样例均fail closed。正式Stage 1仍`NOT_RUN`，不得把本行写成效果通过。 |
| G32-I072 | DUPLICATE-CLASSIFICATION-AND-COMMIT-CAPACITY | PIBT此前先为预算预检分类一次，真实dispatch又重新分类；同时若publication才`reserve()`，完整事务越过point-of-no-return后仍可能因分配失败抛出。 | 预算预检丢弃了已分类row，容量检查只约束trace limit而未把实际sidecar capacity准备纳入同一提交边界。 | 每个PIBT DIRECT机会仅在不可变pre-transaction状态stage一次，结果保存在既有8-slot固定栈数组并由真实dispatch直接消费；DIRECT/J2/PIBT在任何逻辑修改前按本批exact stored count有界预留，publication只验证已预留容量并按实际append顺序发布。此条取代I017中“reserve也移到commit后”的实现细节，保留其“row只在唯一真实commit后发布”的合同。 | 未新增动态容器、policy、event、registry、地图特例或二次扫描；复用既有固定8-action上界与唯一sidecar。 | Release完整构建通过；native/nested proof全true；相关CTest 5/5及最终全量CTest 16/16通过。正式Stage 0/1仍`NOT_RUN`。 |
| G32-I073 | FULL-REGRESSION-ENVIRONMENT-GATES | 首次全仓pytest在collection阶段依次缺`pyarrow`、`zstandard`；补齐后又有旧阶段测试绑定历史分支/upstream、未纳入Git的`.local_archives`/`build_g4irsf12`/大任务文件，以及Windows checkout把冻结文本转成CRLF而触发raw-byte/size门。G19短任务进程池另一次未观察到两个worker。 | 隔离工作树只含Git内容且使用新分支/新构建目录；开发解释器未安装全部声明依赖；Windows进程启动速度使8个毫秒级任务偶发只被一个worker领取。 | 安装项目已声明的`pyarrow>=16,<22`、`zstandard>=0.23`及构建清单测试所需`pybind11`；显式绑定当前Release pyd。保留12个历史身份/本地工件检查为deselect而不伪造旧环境，G19易变断言单独连续重跑5次；不改旧阶段算法或测试门。 | 仅测试环境与运行命令变化，项目代码复杂度零变化。 | 最终适用全仓回归`2038 passed, 3 skipped, 13 deselected`；13项为12个已逐项登记的历史环境检查及1个G19调度断言，后者随后5/5通过。原始全仓失败不改写为全绿。 |
| G32-I074 | PRECOMMIT-IMPLEMENTATION-REGRESSION | 新增P0证据链跨C++、pybind、Python runner/selector/composer，局部测试不能排除旧模块兼容性或原生链接回归。 | 多层改动需要在正式identity commit前统一验证，但该构建仍自报旧configure-time head，不能充当正式证据。 | 完整Release build后跑全部CTest；合并运行5个G32测试文件；再运行I073所述适用全仓回归。正式control前仍必须提交实现，并用最终HEAD重新configure/rebuild。 | 仅验证流程，无代码复杂度变化。 | Release build成功、CTest`16/16`；G32五文件合并回归`168 passed`；适用全仓`2038 passed`。全部仅为`PRECOMMIT_NOT_FORMAL_EVIDENCE`。 |
| G32-I075 | FALSE-NO-GO-CONTROL-EVENT-SEQ | 首次正式G31 control选择在1x执行后返回`NO_GO_NANNING_P0_CONTROL_SELECTION_AUDIT_FAILED`，错误为`ordinary events are not unique and monotonic`；真实10543条event的time无回退、seq无重复，但有2916处seq下降。 | `seq`在事件调度时分配，是唯一身份而非执行ordinal；runtime按time/microphase执行，较晚创建的passive/近时事件可先于更早排定的future事件执行。审计把调度身份误当全局执行序。 | 删除无效的全局seq排序条件；继续硬拒绝重复seq和time回退。首次NO-GO原字节改名归档为`outputs/tables/g4irsf32_v3r3_nanning_p0_control_selection_attempt1_no_go.json`，不覆盖；修复提交后才在原登记路径执行attempt2。 | 两个纯证据谓词替换一个错误复合谓词；native算法、选择样本、阈值、请求和输出schema零变化。 | attempt1文件23,592,312 bytes，SHA-256 `52BB8DD17F3A4DD52F7257F286072CD3EA2EE62545A1685C4798B2C4AA97658B`，strict content hash复核通过；selector`30 passed`。attempt1状态保留NO-GO，attempt2尚`NOT_RUN`。 |
| G32-I076 | FALSE-NO-GO-SHARED-QUEUE-TELEMETRY | control attempt2在同一冻结1x payload继续返回`node49 local queue update reason is not replayable`；node49共有32对source enqueue/dequeue和64对junction enqueue/dequeue。 | `LOCAL_QUEUE_UPDATE`是source queue与ordinary junction queue共用的事件类型；winner重放只需要source行，却把合法junction行当未知source transition拒绝。 | source winner重放仅消费并严格核验`source_enqueue|source_dequeue`；显式识别并忽略`junction_enqueue|junction_dequeue`，其他未知reason仍fail closed。attempt2原字节归档，不覆盖登记路径。 | 一个固定四值reason分派；无runtime、样本、阈值、schema或策略变化。 | attempt2文件23,592,317 bytes，SHA-256 `1E96714E7F7159744661264086832EBFD5EA705DE0239709AC091360A73F4E51`，strict content hash复核通过；selector`32 passed`。状态保留NO-GO，attempt3尚`NOT_RUN`。 |
| G32-I077 | MIXED-NO-GO-CONTROL-AUDIT-AND-NO-EVENT-SIGNAL | control attempt3首次完成同一冻结1x/2x各64袋G31控制运行；两尺度均64完成、0失败、0活动、未触发event/time limit，但顶层仍为`NO_GO_NANNING_P0_CONTROL_SELECTION_AUDIT_FAILED`。false checks同时包含permanent junction exactness、global goal service和qualifying event；因此此工件不能被改写成正式`NO_EVENT`。 | 三个证据谓词混入错误语义：运行时可物化配置内的额外惰性junction row；type-2节点可能只是另一袋的transit/service节点；event `seq`是调度身份而非执行ordinal。独立queue重放同时发现真实信号：两尺度各32个`53->49 EDGE_ENTER`处source queue均为0（nonzero `0/32`、max 0），冻结pair按约60秒后的arrival对齐，而观测点在commit-time。 | attempt3原字节先改名归档且释放登记路径；下一提交只修三个审计谓词及深重放，绝不改runtime、pair、release、rank、阈值或样本，再用完全相同G31控制请求复跑。若得到`NO_EVENT`，永久保留为V3R3 terminal negative，不从G32 outcome重选样本。 | 本条仅归档证据与登记处置；生产代码、策略、动态状态、实验样本均零变化。 | `outputs/tables/g4irsf32_v3r3_nanning_p0_control_selection_attempt3_no_go.json`为47,245,779 bytes，文件SHA-256 `18EC475FDCF16D9E912E005C6E17D14584CA32561949E37C3C99F6DA1B493EA2`，strict content SHA-256 `bd27c54a47cd996c3358332d4aa98beb4c0807d323cf9b477e7dcb908581b54d`复核通过；1x state/expected/configured=`116/98/151`，2x=`105/85/151`；`g32_executed=false`。 |
| G32-I078 | FALSE-NO-GO-MATERIALIZED-DORMANT-JUNCTIONS | runner permanent gate和selector顶层control gate都把`junction_state`强制等于bag/event触及节点集；attempt3中1x多18行、2x多20行配置内已物化但最终惰性的状态，连带把final queue/incoming和vector bound误判为false。 | runtime可为配置节点物化状态，即使该次执行没有bag/event触及；“活动节点必须存在”不等于“状态向量不得含惰性配置节点”。原vector bound也错误使用expected active数，而保留向量按configured上界分配。 | 两层审计统一为`expected ⊆ materialized ⊆ configured`且node唯一；额外行必须`service_reservation_count=0`，全部物化行最终source/junction queue及incoming仍必须为0；vector上界改用冻结configured节点数。非配置extra、缺active、非零extra均fail closed。 | 仅替换集合谓词与证据上界；无runtime容器、策略、事件、扫描或阈值变化。 | attempt3原payload用修复后代码只读重放，两尺度permanent全部checks为true；新增inert extra正例及reservation/queue/unconfigured负例。runner全文件105项、selector全文件36项通过。 |
| G32-I079 | FALSE-NO-GO-GLOBAL-GOAL-ROLE | global service gate曾要求所有type-2节点的aggregate service count为0；南宁节点可同时是某袋goal和另一袋的transit/service节点，attempt3因此误报`goal_arrival_has_no_service=false`。 | `complete_on_goal_arrival`约束的是“每个bag到达自己的goal后不再服务”，不是按节点类型实施全局禁服。 | 从冻结`bag_records`按runtime ID绑定每袋own goal，只禁止episode发生在该袋自己的goal；未知bag/goal仍fail closed。现有completion vector仅追加一个`goal`整数，deep validator用完整service sequence逐袋重算。 | evidence-only每袋一个固定整数；runtime、地图、服务日历、策略和阈值零变化。 | type-2共享角色正例通过、自身goal服务负例失败；attempt3两尺度global calendar只读重放均全true；runner105项、selector36项通过。 |
| G32-I080 | FALSE-NO-GO-LIVE-WINNER-SCHEDULING-SEQ | qualifying control曾要求local service completion `seq`大于external enter `seq`；同I075，调度时先取得的future事件可有较小seq但在entry之后执行。 | 再次把调度身份当作执行顺序，导致物理上仍live的local bag被排除。 | live谓词只使用冻结执行时间：service start不早于entry且service complete严格晚于entry；全局event time单调与seq唯一门继续保留。 | 删除一个无效seq比较、增加一个时间端点比较；无样本或runtime变化。 | 新回归把external enter/exit/complete seq设为50/51/52而local completion保持4，仍正确产生1个qualifying event；attempt3同一payload重放只剩真实`NO_EVENT`一项false。 |
| G32-I081 | PYTEST-BASETEMP-PARENT-MISSING | 本轮首次并行聚焦回归把`--basetemp`写成工作树内不存在的`tmp/...`，pytest在Windows不会递归创建缺失父目录，runner出现14个setup error、selector出现10个setup error；已执行的测试主体没有断言失败。 | 测试命令的临时路径选择错误，不是代码或权限回归。 | 改用工作树根下两个独立basetemp并禁用陈旧cacheprovider，完整重跑；不修改测试门或生产代码。 | 仅验证命令变化。 | 重跑runner全文件105项、selector全文件36项均exit 0；首次setup error原输出保留在任务日志，未冒充代码失败。 |
| G32-I082 | TERMINAL-NO-GO-V3R3-NANNING-CONTROL-NO-EVENT | 修正全部已知false-negative后，以完全相同冻结G31 binary、workloads、32 pairs/scale、request和阈值执行attempt4；1x/2x均返回`NO_GO_NANNING_P0_CONTROL_SELECTION_NO_EVENT`，每尺度qualifying count=0，且唯一false check为`real_53_to_49_with_released_live_local_winner`。 | 选择器按external projected arrival配对local release（约commit后60秒），但V3R4观测seam在external commit时读取当前ready source queue；因此两尺度各32个真实`53->49 EDGE_ENTER`时local queue均为空。它是预冻结cohort与观测时刻不相交，不是算法崩溃或审计误判。 | 原字节归档为attempt4并永久保留V3R3 terminal NO-GO；不改pair、release、rank、seam或阈值，不用G32 outcome重选。鉴于该补充门不是原始MD的Stage0/1→P1硬前置，下一步须在任何G32正式outcome前冻结新修订，仅恢复原MD的晋级边界；V3R3结论不得回填为GO。 | 运行与证据归档；生产代码、G31/G32算法和样本零变化。 | attempt4文件47,253,201 bytes，文件SHA-256 `CC9C7ED4A19E2DB3BCFA4397324DE2CCECFD08FEE862A95CB250D234C67074CD`，strict content SHA-256 `73b66386a62f374268d593f079fa440ec266d28fb2c4cffabfe1294991396d6a`；两尺度64/64完成、0失败、无limit，service/permanent/global全部checks为true，`g32_executed=false`、`selection_outcome_blind=true`。 |
| G32-I083 | CONTROL-INFORMED-REVISION-FROZEN-BEFORE-G32 | 对I082后晋级边界再次逐字复核原计划§15：除了Stage 0/1合成门，原计划还要求南宁小slice和map2 sentinel证明该混合来源状态真实存在，才允许接入J2 authority；因此不能以“追加门”为由绕过南宁阴性。 | V3R3不是机制失败，而是选择器把local release对齐到约60秒后的projected arrival，和commit-time shadow seam错位。完全放弃真实slice会失去原计划要求的state-existence证据；继续扩大Cartesian pairs则会增加复杂度并引入选择自由度。 | 在任何G32南宁或P1 outcome前冻结V3R5：每尺度仅从原冻结G31 workload取external pool中multiplicity最大的精确release `t*`（tie取更早），选择全部`release=t*` external行及local node49在`[t*,t*+120s)`的行，只增加`source`标签并规范排序。G31-only可行性预检后冻结规则、计数和全部identity hash；正式门仍仅为每尺度至少1个qualifying event，预检观察值不成为阈值。 | 不改runtime、P0 helper、action、地图、workload row、service、阈值或schema字段；删除32×32配对思路，选择器是一次histogram与线性过滤，无Cartesian product或地图ID运行时分支。 | 新增`docs/G4IRSF32_v3r5_commit_aligned_nanning_p0_addendum.md`，SHA-256 `10291EB088000A70838DE9DED86EA3F1CFE7975EBF3FB6A3453FDB615AB080CE`。冻结1x为`t*=23700.0, 310E+9L=319`，2x为`t*=22200.0, 470E+24L=494`；G31-only预检观察到14/19个qualifying events且两次均complete/no-limit，但这只是冻结前可行性，不是正式control、Stage 0/1或G32 PASS。 |
| G32-I084 | FALSE-GO-DEEP-REPLAY-SELF-SIGNED-OWN-GOAL | generic synthetic deep loader曾从同一份`bag_completion_vector.goal`构造逐袋goal map，再用它复核service calendar；协调修改completion goal并重算其自哈希可让standalone loader把“在自己goal服务”改写成“在别处服务”。正式fresh runner和resume精确重执行不受此漏洞影响，但独立deep replay声明过强。 | own-goal约束的身份来源和被审completion evidence相同，冻结population manifest/request没有参与逐runtime绑定；future/distant现有request/profile/potential SHA也只校验格式。 | 不改artifact schema：`_validate_service_audit_evidence`强制接收外部`expected_goal_by_runtime`，completion逐runtime必须与之相等且global calendar只使用外部map。Stage1取当前冻结manifest的有序`bag_rows`；Stage0按固定四角色重建；future/distant重建冻结request并精确复算request/profile/potential SHA；map2由冻结fixture重建request。所有入口缺外部map即fail closed。 | 仅新增纯证据helper和参数透传；无runtime字段、动态状态、事件、样本、阈值、策略或artifact schema变化。 | 新增“只改completion goal并重算vector SHA”负测，仍因`own-goal mapping differs from frozen request`拒绝；selector专用测试`37 passed`，campaign`26 passed`，基础runner`105 passed`。这些是pre-formal验证，Stage 0/1仍`NOT_RUN`。 |
| G32-I085 | FALSE-GO-SHALLOW-SHADOW-PROMOTION | Nanning shadow fresh runner会保留完整payload和审计，但formal promotion曾只复核顶层schema/hash及两尺度`pass=true`；把scales替换为两个浅`{"pass":true}`并重算content hash，纯candidate validator仍可能通过，无法独立证明实际审计了319/494袋。 | 组合器把runner内存返回值当成可信来源，promotion没有从登记control/synthetic/G32身份重建请求并重放nested evidence；自哈希只能证明内部一致，不能证明语义。 | 抽取fresh/deep共用的纯scale replay helper；formal promotion从登记路径重新深载control与synthetic，绑定本次显式且非symlink的resolved G32路径/现场SHA，从冻结selection重建319/494 shadow request，不调用executor，仅用保留payload重算observations、join、census、service、resources、legacy wait、ordinary state和49←53 admitted，再与完整scale对象逐字段全等；顶层同时重算control content、synthetic decision/head、status/pass。raw mapping helper改为私有，公开入口只接受Path重载。 | 将原内联审计抽成单一helper，删除重复逻辑与旧`mathematical_pair_count`/`full_v3r3_service_audit`命名；无runtime状态、事件、策略、样本、阈值或正式执行次数变化，deep replay只增加离线验证计算。 | 两尺度浅pass、nested observations协调改写并重算各层hash、wrong loaded binary path均被拒；正例明确重放1x=319、2x=494且executor调用为0。selector+campaign`64 passed`；三文件合并`169 passed`。正式G31 control、G32 Stage0/1仍`NOT_RUN`。 |
| G32-I086 | V3R5-CONTROL-PREFLIGHT-READY | V3R5登记control在正式执行前必须证明冻结源、选择、请求和off-only边界可独立重建；否则一次性append-only输出可能因配置漂移形成无意义负门。 | 新revision改变cohort cardinality，旧64-row测试/request假设不能充当319/494的执行前证据。 | 不调用`run_control_selection`或任何executor，做两次独立temp regeneration、selection deep validation和两尺度request构造；精确检查G31 path/SHA、65-key omitted/off request、row顺序/身份、trace/event limits及G32 namespace完全缺席。旧pair测试改为modal release、earliest tie、half-open window与顺序不变性测试。 | tests/preflight-only；删除Cartesian selector代码，生产runtime零变化。 | 预检10/10冻结源通过；1x `310E+9L=319`、2x `470E+24L=494`及全部冻结hash两次一致；request SHA分别`59b2ed58061268e566fff651247dce79d96de8aec3b78e8cfec1a7e27e79ba0e`、`34c2b0e58d5746cfee8bd7bfd35e44d6b7680138a51c6bfee199929f012fd032`。正式输出路径仍不存在，结论仅`READY_NOT_RUN`。 |
| G32-I087 | TERMINAL-NO-GO-V3R5-LOAD-INDUCED-STALE-HARD-GATE | 冻结V3R5正式G31-only control在两尺度均观察到目标状态，却返回`NO_GO_V3R5_NANNING_P0_CONTROL_SELECTION_AUDIT_FAILED`。 | 全modal burst一次选择310/470个external，加9/24个local；319/494袋虽全部完成，仍产生冻结I033 safety hard gate中的`merge_grant_stale_arbitration_count=41/155`。另有2x lifecycle telemetry次序误判，但不是决定性门。 | 原始322,188,323-byte工件append-only归档为`...v3r5...attempt1_audit_failed.json`；不删除stale gate、不改阈值、不重判V3R5。下一revision只缩小证据cohort，任何G32仍未执行。 | 只产生G31 evidence；runtime/阈值/地图/服务/动作零变化。 | 文件SHA-256 `BCDE3E4A68609432CE74B6996FF8798928770846984E181FA7AD2D6F3497F56C`，strict content SHA `1F4D2BBCF98EAF74C962FC0CBDD11CD53A7BA6CF6120CB44119C7698293DC94A`；1x/2x qualifying=14/19、completed=319/494、failed/active/limit/global overlap均0，`g32_executed=false`，sources/dependencies首尾一致。 |
| G32-I088 | FALSE-NO-GO-SUBEPSILON-LIFECYCLE-TELEMETRY-ORDER | V3R5 2x的4348条merge lifecycle中唯一identity `(2960,2960,9,35,100)`在raw表呈`ISSUED/PREPARED/COMMITTED/REQUESTED/CONSUMED`，导致`lifecycle_chains_valid=false`。 | controller真实先append REQUESTED；最终表按exact time排序。REQUESTED `23003.746890000002`比I/P/C `23003.74689`仅晚约`3.638e-12`（1 ULP，远小于冻结`EPSILON=1e-9`），被序列化排序到其后；grant/计数/守恒/终态均正确。 | 保留全局raw exact-ordered门；逐identity仅在合法状态模板、逻辑相邻时间逆差≤EPSILON、I/P/C跨度≤EPSILON、REQUESTED grant=0且其余同一正grant时按冻结状态序重建。final/post-commit取逻辑序；缺失、重复、grant漂移或>EPSILON逆序继续fail closed。 | 纯离线审计修正；runtime event/state/grant/阈值零变化。 | 原attempt1离线重放后1x/2x lifecycle均`ordered/chains_valid/counts_exact=true`，但两尺度仍因41/155 stale hard gate保持NO-GO；新增0.5×EPSILON正测、2×EPSILON及缺/重状态、grant/time漂移负测。 |
| G32-I089 | PREEXEC-DESIGN-REVIEW-REJECTED-FIRST32 | 初稿拟将modal external规范截为前32以复用历史control规模；若直接正式执行，可能重新制造1x `NO_EVENT`。 | 独立审查V3R5 G31 control trace发现1x首个qualifying external在canonical rank60，14个事件位于rank60--73；前32会删除已证state。该发现发生在任何V3R6 executor、提交或正式输出之前。 | 不扫描多个cap、不按bag identity挑行；改用包含首个已证rank60的最小二次幂canonical前缀64，并保留全部local window。规则仍只按modal release和canonical identity执行；G31 trace仅用于预注册cap，符合原计划control-informed边界。 | 一个常量由32改64；无新增算法分支、运行时状态或阈值。 | first32从未执行、无结果工件；V3R6冻结为1x `64E+9L=73`、2x `64E+24L=88`。 |
| G32-I090 | V3R6-BOUNDED-COMMIT-ALIGNED-CONTROL-FROZEN | V3R5全modal cohort证明状态存在但触发stale安全硬门；需要用单一、可归因且无行级后验选择的更小真实slice。 | 同时burst规模远超状态存在证明所需；安全门不能放宽。 | 冻结`MODAL_EXTERNAL_RELEASE_CANONICAL_FIRST_64_PLUS_120S_LOCAL_WINDOW_V1`：完整pool求唯一modal `t*`，modal内按字符串`segment_id`等canonical key取前64，local仍全取`[t*,t*+120)`；两次独立regeneration/deep validation/request构造，无executor。 | 线性过滤后固定prefix slice；删除大量证据行，runtime、action、地图、workload row、service、阈值不变。 | addendum SHA-256 `4A2A6A66337989CC18EB7B5F3E2B99F4DFCE57DB0EFCF618B5C8C11545ACE4D8`；1x hashes histogram/original/projected/projection/IDs=`29b791.../24c9bb.../496222.../054012.../3b1b9b...`，2x=`ba35a3.../7bd878.../777c6a.../4ec880.../81f98c...`；73/88-row omitted/off request SHA=`b929b94e23fcd2e948f6e9f779bf5ecfb661671a65c329e30f8df25083b85edc`/`122640b4b4ae0fd59a7993fe28cd6cd8168f06d027b958ec3037e9e60f025319`，均65 keys且无G32 namespace；相关五文件193项回归全通过。正式V3R6 output不存在，结论仅`READY_NOT_RUN`。 |
| G32-I091 | FROZEN-EVIDENCE-EOL-PORTABILITY | 新V3R5负工件和V3R6 addendum以LF生成，但本机`core.autocrlf=true`且G4IRSF32尚无专属attribute；未来checkout可能改变物理字节，破坏已登记文件SHA。 | 现有`.gitattributes`只覆盖G4IRSF14/15/16等模式，G4IRSF32路径未命中；负工件含7,833,875个LF且0个CRLF。 | 增加G4IRSF32 docs/reports/tables/runners/src/tests的`text eol=lf`模式；把`.gitattributes`本身加入source bundle与implementation allowlist，防止规则漂移。 | 6行属性与1个source path；实验算法、runtime、数据内容和阈值零变化。 | 增加属性前后负工件仍322,188,323 bytes、SHA `BCDE...F56C`，V3R6 addendum仍SHA `4A2A...E4D8`；`git check-attr`返回`text:set/eol:lf`。 |
| G32-I092 | TERMINAL-NO-GO-V3R6-STALE-HARD-GATE | 冻结V3R6正式G31-only control在两尺度都保留目标状态，却返回`NO_GO_V3R6_NANNING_P0_CONTROL_SELECTION_AUDIT_FAILED`。 | bounded 64-row external前缀将V3R5的stale从41/155降到11/21，但未归零；1x/2x的4/2个qualifying events证明state存在，`full_service_audit=false`只是同一stale safety失败的派生结果。 | 原始63,256,026-byte工件append-only归档为`outputs/tables/g4irsf32_v3r6_nanning_p0_control_selection_attempt1_audit_failed.json`；不删除或放宽stale gate，不覆盖旧V3R6登记路径，不运行G32。下一revision改为用冻结release/headway语义直接构造最小局部重叠，不再试探不同prefix cap。 | 只产生G31 evidence并缩小下一设计的因果范围；runtime、action、地图、workload row、service、阈值和G32状态均零变化。 | 文件SHA-256 `D025204C0BE56B79E1CF728D68B4051D444277C71B05FAE68FCADC106E74DFC3`，strict content SHA `E7FEBFEE7120B60E6DAAF1B4262A4A44816691F8F477379C823A2D567EA82DB6`；1x/2x为73/88全部完成、qualifying=4/2、stale=11/21，failed/active/limits/conflicts/global scans/full A*/pending均0，permanent/lifecycle/global service全部通过，`g32_executed=false`且sources/dependencies首尾一致。 |
| G32-I093 | PREEXEC-DESIGN-CORRECTION-LOCAL-MAY-PRECEDE-EXTERNAL | V3R7首个纯release-geometry草案沿用V3R5的forward-window直觉，隐含要求`external_release<=first_local_release`，因此初算2x为2E+2L；同时用`A+1<=first_external_arrival`描述“local交互在external到达前清空”，只保证第一件local清空。 | 原冻结qualifying gate要求local在external commit时已经release且live，不要求晚于external release；服务两件相邻local完整清空至少需要`A+2`而非`A+1`，更一般的retry精确式随后由I097补齐。 | 在任何V3R7 executor、输出或提交前，经独立全pool枚举删除错误release先后约束；所有gap/commit边界用现有EPSILON严格收口，并先用更强`A+2<=first_arrival-EPSILON`消除只清空第一件的错误。按`(prefix_count,external_release,local canonical identity)`一次取最小，不扫描runtime outcome；I097再把该简式精化为精确retry日历。 | 2x又删除1个external，1x不变；只修改离线选择器/测试，无runtime分支、动态状态或地图ID特化。 | 草案2E+2L从未执行、无工件；最终候选全集1x/2x为9/82，SHA=`0a62a1c7...f1684`/`f4e8b623...7d54e`；边界测试覆盖local先release、EPS上下端、完整clearance和rank/multiplicity恰好差1。 |
| G32-I094 | V3R7-MINIMAL-PREARRIVAL-OVERLAP-CONTROL-FROZEN | V3R6依赖64-row external burst才制造local等待，既保留state又产生11/21 stale；需要直接由最小local-local服务重叠产生唯一live source queue。 | node49服务为1秒，source retry为0.25秒，53→49首entry偏移0.001秒且travel 60.1秒；两件相邻local在第一件服务中释放第二件即可形成唯一队列，无需大burst。 | 冻结`MINIMUM_EXTERNAL_PREFIX_ADJACENT_LOCAL_SERVICE_OVERLAP_V1`：完整pool仅读role/release/identity和冻结服务几何；commit严格落在`B+EPS`与`A+1-EPS`之间，两local按精确retry日历在首external arrival前全清空；取最小external prefix与恰好两local。所有V3R3/V3R5/V3R6阴性、addendum和hash继续进入source chain；stale gate仍严格为0。preformal只读审计发现产物说明字符串一度写成未clamp的`ceil(...)`，在任何executor/commit前对齐为登记公式`max(0,ceil(...))`并增加精确断言。 | 正式runtime/action/map/workload/service/阈值零变化；control cohort降至1x 6行、2x 3行，离线选择多一个固定candidate枚举但删除结果调cap自由度；说明字符串修正不改变选择或任何已冻选择hash。 | addendum SHA-256 `1733B7311D996EEF27AC70097C11984D90D9E929B5E49C9FB6645BD471537531`；1x=`4E+2L@58200, rank3, commit58203.001`，rows/projected/identity/IDs=`79d321...94478/48727b...dbd3/691d60...0874/fe2037...dda6`，request=`395cbb...e7ade`；2x=`1E+2L@45000, rank0, commit45000.001`，对应`5b2c8d...24f8f/7b86b3...9b1bd/eddb03...57b0/bdf084...219d`，request=`f58630...069c`。这些是预注册身份和静态因果预测，不是PASS；V3R7正式输出不存在。 |
| G32-I095 | PREFLIGHT-FALSE-NO-GO-TEMP-PATH-MANIFEST-BYTES | V3R7首次双临时目录预检直接比较整个regeneration对象，虽selection、manifest语义及所有稳定hash相同，仍在whole-object equality处失败。 | generated manifest物理JSON记录各自临时输出路径，因此`regenerated_manifest_sha256`按设计随temp root变化；把路径绑定的文件SHA当成跨目录语义身份会误报。 | 两次仍分别执行返回值/写入manifest全等、严格解析和全部冻结validation；跨目录比较只排除每尺度这一个物理manifest SHA，继续要求其为独立有效64位digest，并对selection、manifest semantics、raw/canonical/pool/ordered rows逐字段全等。 | 仅修正一次性预检断言；生产代码、选择、gate、hash常量和正式运行次数零变化。 | 首次失败发生在任何executor之前；差异仅1x/2x的`workload.regenerated_manifest_sha256`，两尺度`selection_equal=true`；修正后的相同双重预检exit 0。 |
| G32-I096 | V3R7-CONTROL-PREFLIGHT-SNAPSHOT-SUPERSEDED | 正式一次性G31 control前必须证明新6/3人口、请求、旧阴性链和deep replay合同在无executor条件下闭合。 | V3R7改变selection schema、人口和campaign revision；旧V3R6的73/88预检不能充当新revision证据。 | 两个独立temp root重建并深验完整pool/选择；构造两尺度65-key omitted/off请求，检查无G32 namespace、固定binary path/SHA和动态人口；运行五个G32相关Python测试文件全回归；确认V3R7登记输出不存在。 | tests/preflight-only；不调用`run_control_selection`、G31 executor或任何G32路径。 | 当时195项全通过、人口=6/3、request/candidate SHA不变，frozen-source/dependency snapshot=`dc3c612d...b099`/`17ee4334...f2c20`且output不存在；随后I097精确retry修订使该指纹失效，故本行仅保留历史且状态为`SUPERSEDED_PREEXEC`，不可授权正式运行。 |
| G32-I097 | PREEXEC-EXACT-SOURCE-RETRY-CLEARANCE | preformal审计发现`A.release+2.0`只在`B.release-A.release<=0.75`时等于第二件local完成时刻；更大gap下它会低估source retry造成的完成时间。 | frozen runtime在B首次遇到A的service calendar时把wakeup排到`max(B.release+retry_interval,A.release+service)`，故精确完成式为`max(B+0.25,A+1.0)+1.0`。 | 在任何executor/commit前把selector、独立oracle、deep validator、addendum和rule文字统一为精确式；将`source_retry_interval_seconds=0.25`纳入exact-key evidence，并让G31 request显式fail-closed绑定同一值；增加gap=0.9旧式会误接收的负测。 | 一个固定`max`和一个常量等值门；不改runtime、workload、阈值、winner、action或地图分支。 | 完整冻结池精确重建仍为1x 9/9、2x 82/82合法候选，candidate SHA、6/3 winner、row/projection/request SHA全部不变；新addendum SHA=`1733B731...7531`，39项selector/deep replay及最终195项回归通过。 |
| G32-I098 | PREFLIGHT-TEMP-ROOT-AND-REQUEST-HASH-FALSE-NEGATIVES | 最终回归首跑在公共pytest temp root出现大量setup `PermissionError`；精确retry后的首次双预检又用raw canonical JSON hash比较request，误报1x identity改变。 | 前者是Windows共享`pytest-of-38908`目录权限/并发清理冲突，所有错误均发生在`tmp_path` setup；后者忽略了冻结`request_sha256`会先规范化binary locator路径，raw hash本来就不同。 | 回归改用经不存在检查的独立`--basetemp C:\tmp\g4irsf32_v3r7_pytest_final_retry_001`；双预检改用正式auditor的`request_sha256`和`ordinary_request_sha256`并要求两者相等，仍保留65键和零G32 namespace检查。 | 只隔离测试环境并调用既有注册哈希函数；生产选择、executor和gate零变化。 | 首跑权限错误不含测试断言失败；隔离后195/195 exit 0。双重无executor预检exit 0，注册request仍为`395cbb...e7ade`/`f58630...069c`，raw hash差异不再被误判，正式output始终不存在。 |
| G32-I099 | V3R7-EXACT-RETRY-CONTROL-PREFLIGHT-READY | I097使旧preflight指纹失效，正式一次性G31前必须以最终精确retry代码重新闭合完整合同。 | 不能用I096的旧bundle或修订前测试结果授权executor。 | 两个新的独立temp root重建并深验完整pool/选择；只排除各自路径绑定的物理manifest SHA后稳定证据逐字段相等；重构两尺度65-key omitted/off请求并用注册路径规范化hash验证；运行最终五文件全回归并确认所有正式输出不存在。 | tests/preflight-only；不调用`run_control_selection`、G31 executor或任何G32路径。 | 195/195 exit 0；1x/2x仍为candidate=9/82、population=6/3、prefix=4/1、retry=0.25，candidate/request SHA=`0a62a1...f1684`/`f4e8b6...7d54e`与`395cbb...e7ade`/`f58630...069c`；最终frozen-source/dependency=`606a6a28...cde89`/`34e13c37...d6ef4`，首尾相等，`formal_output_exists=false`。结论仅`READY_NOT_RUN`。 |
| G32-I100 | FORMAL-G31-PASS-V3R7-ZERO-STALE | V3R5/V3R6虽观察到目标状态，却分别以41/155和11/21个stale违反零容忍硬门；必须证明最小局部重叠既保留真实事件又完全消除stale。 | 大external burst会制造被后续generation取代的destination-merge wakeup；V3R7改由两件相邻local直接制造唯一live source queue，并只保留达到该commit的最小external prefix。 | 在clean commit `e2d63211e4f4b64606108e1424b3061f3aa7f548`后只执行一次冻结G31 omitted/off control；随后用注册loader对落盘字节、完整选择、请求、payload、安全、service、lifecycle及首尾身份做深重放。 | 只新增4/2+1/2的G31 evidence cohort和正式工件；runtime/action/map/workload/service/阈值/G32均零变化。 | 状态`PASS_V3R7_NANNING_P0_CONTROL_SELECTION`；4,029,387-byte工件file SHA=`79DB82402CC8F8C9ABD8D1A9A01A6E9216ADED76008FFC9DA5945D4F7CBD38B0`、strict content SHA=`8C30C65CD06A6D340D1E2B6C25A2713FF92DD3A0B92E7A2ADF664830E984F281`。1x/2x请求与完成=6/6、3/3，qualifying=1/1，`merge_grant_stale_arbitration_count=0/0`且`stale_arbitration_event_count=0/0`；failed/active/limits/conflicts/global scans/full A*/future routes/final pending均0，full safety、service、lifecycle、permanent-starvation审计全通过。frozen/dependency首尾分别=`606a6a28...cde89`/`34e13c37...d6ef4`，`selection_outcome_blind=true`、`g32_executed=false`。仅授权Stage 0/1，不授权P1。 |
| G32-I101 | POSTRUN-HASH-SEMANTICS-CORRECTION | 正式工件初次摘要把`load_and_validate_control_artifact()`第二返回值误标为strict content SHA，因而暂时把物理file SHA写了两次。 | 该loader返回`(validated_artifact, verified_file_sha256)`；strict content identity位于工件的`artifact_content_sha256`字段，语义不同。 | 在任何证据提交或后续Stage前，严格重读单一落盘字节，删除自哈希字段后重新canonical hash，并与记录字段逐字节比较；修正I100，保留本行说明误读与处理。 | 只校正文档标签，不修改工件、runner、gate或任何实验结果。 | 物理file SHA=`79DB8240...38B0`；重算与记录的strict content SHA均=`8C30C65C...F281`，`content_match=true`；注册loader deep replay仍PASS。 |
| G32-I102 | FORMAL-V3R7-CAMPAIGN-ATTEMPT1-NO-GO | 唯一一次正式V3R7 campaign通过preflight与Stage0，但Stage1返回`NO_GO_V3R3_STAGE1_CONTRACT`；外层随后在synthetic严格重读处异常停止，Nanning G32未执行、P1未授权。 | 正式结果同时包含真实Stage1负门和独立证据冻结缺陷，二者不能互相替代或被覆盖重跑。 | 将三份原始落盘字节append-only改名归档为V3R7 attempt1；记录物理SHA、strict content SHA、G32 binary/source bundle身份和未执行边界；后续revision使用新的空路径。 | 只增加失败证据与台账；production/runtime/action/threshold零变化。 | synthetic 34,265,770 bytes，file SHA=`719D4E7F...6888D`、content SHA=`7F455BEF...C721`；final JSON file SHA=`DF633678...4977`、content SHA=`9120A3C7...0B95`；report file SHA=`72222CC3...CB1`。G32 binary=`442954C8...75A`、source bundle=`21ECC0C4...364D`；`nanning_shadow=null`、`p1_review_authorized=false`。 |
| G32-I103 | FREEZE-FALSE-NO-GO-NONCANONICAL-IN-MEMORY-KEYS | synthetic JSON内容自哈希有效，campaign仍报`frozen synthetic artifact changed on strict reread`。 | `_global_service_calendar_audit()`在内存返回int node keys；JSON规范化把它们变为string keys，`with_content_hash()`却保留原对象，导致内存对象与严格重读对象不等。首差在Stage0首case的`completion_counts`，同类差异覆盖`reservation_counts`及后续引用。 | 下一证据修订在runner生成边界把service-count node keys规范化为十进制string，并新增完整artifact JSON roundtrip equality测试；不改实验值或gate。 | runner/evidence-only类型规范化，无runtime状态或策略变化。 | attempt1文件的content hash校验通过，证明物理JSON未损坏；该缺陷只解释外层冻结异常，不解释Stage1的真实NO-GO。另有selector global-service全量/精确L序列错绑，须同轮以显式全量证据或正确subset合同fail-closed修复。 |
| G32-I104 | STAGE1-FALSE-NO-GO-EVENT-ID-NOT-EXECUTION-ORDER | 120例中20例`hard_gate_pass=false`，但全部bags完成、安全、service/calendar、pending、parity、census和join均通过；失败仅来自shadow人口复合审计。 | `_bag_population_identity()`按`seq<=row.event_seq`截取并排序queue事件；native `seq`是事件身份/入堆次序，不是heap实际执行次序。更早执行的source enqueue可拥有更大的seq，因而被错误遗漏。 | runner-only改为保留raw event trace执行顺序，唯一定位row trigger位置后重放此前source enqueue/dequeue；seq只作唯一身份。新增seq下降正/负回归，并保留首个字段级不一致诊断。 | 删除错误排序假设，不改C++ telemetry、release、action、map或阈值。 | 20例共1,312 bags全完成、652 admitted rows、0 invalid；20/20仅`shadow_row_local_telemetry_exact=false`，其中4例另有winner=false，off 20/20 PASS。定向只读复现首row：source enqueue已在trace索引74执行但seq82>trigger seq81。尚未以修复后正式工件改判。 |
| G32-I105 | TERMINAL-NO-GO-V3R7-PRIMARY-IDENTIFICATION-DEGENERATE | Stage1有2,664 observations/pairs、177条primary joined及354 unique primary bags，数量、join/census和资源门通过，但`directional_cases=0`，所有rho/bootstrap/Wilson均未定义。 | 冻结单槽定义在同一case内令`X_insert=L1-L0`恒为一个service quantum；96/96 mixed cases的primary `distinct X=1`，其中85例又只有1条primary，故至少24个case内方向层在现有释放与estimand下结构性不可识别。 | 保留本次真实NO-GO，不把repeat伪装成独立primary、不靠浮点噪声、不全局池化、不放宽24/128/rho/Wilson阈值，也不启动closed_loop。下一revision先预注册新鲜、解析可证明非退化的synthetic identification cohort；原120例继续作为完整安全回归，公式/样本/停止门在新数据前冻结。 | 当前production零变化；下一步优先实验设计而非新增arbiter/model/state。 | primary数量门PASS；资源max ratio：events=1、junction-local=1、runtime-internal=`1.0026078`、total=`1.0364983`，均≤1.10。结论严格为hypothesis identification NO-GO，不是吞吐、资源或安全崩溃。 |
| G32-I106 | V3R7-ATTEMPT1-APPEND-ONLY-CHAIN-CLOSED | I102最初只以简称记录三份失败工件，原登记路径、归档路径及wrapper未绑定synthetic的事实不够精确，容易让后续审计靠目录猜测。 | campaign在synthetic写盘后、strict reread异常前尚未写入`after_synthetic_freeze` checkpoint，因此wrapper中的synthetic file/content SHA均为`null`；归档动作必须另行给出字节级链。 | 在提交`5913e30`保留原字节，并登记映射：`outputs/tables/g4irsf32_v3r3_external_commit_local_virtual_shadow.json`→`outputs/tables/g4irsf32_v3r7_synthetic_stage01_attempt1_no_go.json`（34,265,770 bytes，file `719d4e7f70902a09831601ee44e26aa557b575c5895adf22350cedbf6296888d`，strict content `7f455bef83c37af632ffd8681105e8027ecea4da59a5e3e9bf30a8195088c721`）；`outputs/tables/g4irsf32_source_aware_shadow.json`→`outputs/tables/g4irsf32_v3r7_p0_campaign_attempt1_no_go.json`（22,413 bytes，file `df633678493baf2706b0139978969cabd2e477ed17641a9e2b489a4dc46f4977`，strict content `9120a3c793566cad1ff404caef399f3778f56a8c613bb51b7ec518fe3c730b95`）；`outputs/reports/g4irsf32_source_aware_shadow.md`→`outputs/reports/g4irsf32_v3r7_p0_campaign_attempt1_no_go.md`（1,307 bytes，file `72222cc3e219288d2f86207604d9ef840b5a6c74617ccd34ff7670a328c43cb1`）。下一source bundle显式包含三份archive。 | 只强化provenance；零runtime/策略/阈值变化。 | attempt1 HEAD=`8262901d389621a9bae32ed17c7ea19591e3d272`、G32 binary=`442954c836d284a476772c3e01ef57bf147f5d5b4906d1d3d7127ab5cf26475a`、source bundle=`21ecc0c40b2db02141e76ef4501de7ae93105fe89bf46216a08f19fb2cb2364d`。wrapper的`synthetic_artifact.file_sha256/content_sha256=null`被明确保留，不伪称已成功绑定；`nanning_shadow=null`、P1=false。 |
| G32-I107 | PREEXEC-EVIDENCE-FREEZE-ROUNDTRIP-PASS | I103的int/string node key差异会让有效JSON在strict reread时假失败；tuple/Path也会产生同类Python→JSON域漂移，`1`与`"1"`还可能静默碰撞。 | `with_content_hash()`此前先hash但返回未经JSON-domain规范化的deepcopy；普通dict comprehension会覆盖字符串化后的重复key。 | campaign与selector统一在加hash前递归规范化Path→string、tuple→list、mapping key→string，并在字符串化key碰撞时fail closed；runner的service-count key在生成边界直接固定为十进制string。新增nested int key/tuple/Path、collision、nonfinite、pretty-write→strict-read exact equality回归。 | 证据序列化边界收紧；正式runtime/action/统计门零变化。 | 三个相关Python文件完整套件192/192通过，唯一basetemp=`C:/tmp/g4irsf32_r2_full_20260828_d`；历史attempt1三份file SHA复核未变。未运行campaign或executor，不能改判I105。 |
| G32-I108 | PREEXEC-RAW-EXECUTION-ORDER-REPLAY-PASS | I104的20例人口审计失败来自把event `seq`误作执行顺序。 | native `seq`是入堆/身份序号；raw trace数组才是实际执行顺序，正式Nanning 1x trace中10,543个seq均正且唯一但出现2,916次下降。 | 对每条row用runtime bag/task/time定位唯一物理`EDGE_ENTER`，再严格绑定from/to及DIRECT=`one_step_reservation_committed`、J2=`one_step_merge_grant_committed`；只按raw marker之前的source enqueue/dequeue重放。seq只验证正整数唯一；marker缺失、重复或字段漂移均fail closed。 | 删除错误排序假设，无新增状态、事件、map分支或C++改动。 | 定向10/10及完整192/192通过，覆盖先执行高seq、commit后低seq、DIRECT/J2、五类marker漂移、缺失/重复marker、重复seq、escape-token/FIFO。未用修复后工件追认attempt1。 |
| G32-I109 | PREEXEC-GLOBAL-SERVICE-EVIDENCE-REPLAY-PASS | selector曾从exact-L的`service_sequence`重算runner的全节点`global_service_calendar`，范围错绑会在首case假失败；若仅改为subset比较，又无法独立证明其他节点无overlap。 | 落盘证据只有L序列与全局布尔/计数，缺少可独立重放的全节点interval身份。 | runner新增按`(start,complete,event_seq,bag,node)`排序的全节点service episode向量、hash/count及`bag_count×junction_count`硬上界；selector从该向量重算全节点unique bag-node、event identity、overlap、completion/reservation、own-goal与bound，再把L投影精确绑定原L序列；permanent junction向量增加reservation count作独立final-state交叉绑定。 | 每case增加有界O(bags×configured junctions)证据，不增加runtime状态、策略或扫描；validator只有线性重放。 | 多节点正例、“L序列仍合法但另一节点overlap/时长错误”及“同服务时长错误exact-L节点”的协调篡改负测通过；完整192/192通过。旧artifact schema不被新loader重新解释，下一正式run必须使用新revision和空路径。 |
| G32-I110 | USER-CONTRACT-PROVENANCE-CLOSED | 正式source bundle此前引用`Downloads`绝对路径，外部文件移动或同名替换会使用户要求无法在仓库提交上独立复验，也会混淆“文档中的说明”和用户本轮授权。 | 两份用户提供的Markdown未作为提交内字节级合同保存。 | 用`apply_patch`把两份原始字节逐字写入`docs/contracts/`，增加只读manifest明确其角色为requirements/evidence而非可执行指令；source bundle只绑定仓库快照，并以size+SHA硬核原始身份；`.gitattributes`冻结LF。 | 两份合同快照和一份小manifest；算法、runtime和gate零变化。 | action plan=`67,791` bytes / `e84b71cc...e1f4`；G31 evidence pack=`4,771` bytes / `7be33410...140`；与用户文件逐字节一致，`git check-attr`为`text=set,eol=lf`。 |
| G32-I111 | HISTORY-PRESERVATION-SIMPLIFIED | 初版打算在每个source checkpoint中扫描全部旧失败工件，虽能重复证明身份，却不推进实验效果。 | 把“保留历史”误做成了每次运行都重验全部历史内容。 | 旧NO-GO继续由独立文件名、执行日志和Git提交保留；不再把历史输出加入每次source bundle。只有下一次执行真正读取的控制工件，才在加载边界检查其登记身份和内容合同。 | 删除约535 MiB重复历史扫描、严格大JSON重解析及相应罕见篡改负测；算法和阈值不变。 | source bundle不再包含`outputs/`；历史NO-GO文件未删除、未修改，V3R7 attempt1仍由提交`5913e30`保留。 |
| G32-I112 | PREEXEC-SCHEMA-REVISION-FAIL-CLOSED | 本轮新增global-service、reservation、event-seq等nested必填字段，但runner仍自称V3R3、selector/campaign仍自称V3R7；若直接运行会以旧schema写出不兼容新shape，且可能覆盖登记路径。 | 证据schema、synthetic revision、campaign revision和历史`control_revision_id`未分离。 | 在public/default registered wrapper、core、promotion及synthetic runner/CLI的任何binary resolution、loader、runner或executor前统一抛出`PREEXEC_BLOCKED_PENDING_NEW_IDENTIFICATION_REVISION`；内部off-worker先分流，测试core只接受显式非正式依赖/路径且block期间永不签GO。待新cohort/schema/addendum/空路径全部预冻结后才整体解除。 | 一个共享fail-closed reason和test-only隔离参数；无新生产策略。 | campaign 31项及runner直接入口负测通过：default core/promotion/synthetic runner/两层CLI调用计数与default-binary lookup均为0；当前状态明确是`PREEXEC_BLOCKED`而非GO。 |
| G32-I113 | FALSE-GO-EXACT-L-NODE-REBINDING-CLOSED | exact-L校验只有`exact_l=true`，没有外部绑定L的节点ID；协调者可把整条L序列、全局counts/reservations/hash迁到同服务时长的错误节点并伪造通过。 | validator从候选序列自身推导节点，service duration不能唯一标识冻结角色。 | 所有service验证caller必须给`expected_exact_node:int|None`；exact-L时强制非空且每条L episode节点精确相等，非exact-L强制`None`。synthetic/future固定1，Nanning固定49，map2/distant为None。 | 一个显式参数和等值门；不新增运行时状态或地图ID策略分支。 | selector完整40项通过；同服务时长node1→node2并同步改episode/count/reservation/hash的负测被`exact-L sequence node identity changed`拒绝。 |
| G32-I114 | HISTORICAL-V3R7-CONTROL-COMPATIBILITY-BLOCKED | 当前loader把旧控制的执行快照与live工作树强等，并用新增字段的当前audit对象与旧recorded audit整对象强等；因此合法的V3R7控制会被错误拒绝。 | 不可变历史执行环境和当前campaign源码身份被混成同一revision；旧raw payload可重放，但旧audit没有本轮新增字段，不能就地升级或重跑。 | 不改、不重跑旧控制；增加一个窄兼容入口：旧recorded audit按V3R7合同验证，当前更强raw replay只作独立附加证明，当前campaign revision另行登记。完成前I112封锁不得解除。 | 一个窄loader和必要正反例；不复制旧selector、不加生产策略。 | 当前raw payload只读重放1x/2x均PASS；兼容loader尚未提交，故仍为`BLOCKED_PREEXEC`。 |
| G32-I115 | PREEXEC-LOW-LEVEL-EXECUTION-BYPASS-CLOSED | 初版只在campaign public wrapper封锁；private core/promotion仍可被直接组合，synthetic runner/CLI可执行旧V3R3 schema并以`os.replace`覆盖空的旧登记路径，selector也可用替代`--output`重跑旧V3R7 control。 | “不能签FINAL_GO”和“不能在revision未冻结前执行/写证据”被误当成同一边界，低层默认入口未共享封锁。 | block下沉到campaign core/promotion、synthetic `run_campaign`、selector control/shadow及三层正式CLI；worker-off隔离工具先分流且不具正式authority；synthetic evidence改为hard-link create的append-only写入，已存在任一目标即拒绝且不改原字节。 | 只增加执行授权门和安全发布语义；算法/threshold/runtime零变化。 | default core/promotion直接负测在任何依赖调用前失败；synthetic/selector executor与default-binary计数均0、替代selector output不存在；重复写evidence抛`FileExistsError`且JSON/MD原字节保持不变。 |
| G32-I116 | PREEXEC-OVERENGINEERING-REMOVED | 预执行审查一度增加了约535 MiB历史输出扫描、严格大JSON重解析和多组极低概率防御测试，耗时与内存明显上升，却没有生成新的识别证据。 | 证据完整性工作失去比例，开始替代真正的Stage1推进。 | 在提交前撤销这套重型历史审计；保留分块读取这一低成本内存修正、正式revision封锁及与下一次运行直接相关的校验，马上转入24例识别队列。 | 净删除代码和测试，降低准备成本；模型、生产逻辑、threshold均不变。 | `SOURCE_BUNDLE_PATHS`不含任何`outputs/`，旧文件仍在原路径；后续验证聚焦队列构造、统计门和实际运行。 |
| G32-I117 | V3R8-IDENTIFICATION-DESIGN-FROZEN | V3R7的96个mixed case全部只有一个distinct `X_insert`，继续重跑原释放几何不会产生可识别方向。 | 原120例同时承担安全覆盖和方向识别，但其单槽几何把case内插入量固定为一个service quantum。 | 结果未知时冻结双cohort：原120例不动并改名`safety_regression`；新增24例`identification`，每例用4 local+3 external形成三档解析不同的插入量，剩余人口只在core排空后加稀疏external filler。 | 仅增加Python实验释放构造和24例运行；C++、地图、策略、阈值均不变。 | `docs/G4IRSF32_v3r8_identifiable_synthetic_p0_addendum.md`固定4×3×2人口、整数微秒公式、72 primary/144 unique的结构预期及原方向/安全/资源门；尚未运行V3R8。 |
| G32-I118 | V3R8-IDENTIFICATION-PROBE-PASS | 冻结公式虽解析上给出三档插入量，仍可能因真实event ordering、filler或join语义而退化。 | 需要在不冒充正式证据的前提下尽早检验设计是否值得进入完整120+24运行。 | addendum提交后，用现有G32 Release对24例逐一做只读探索；不写正式输出、不换case、不调阈值。 | 24例×off/shadow的短验证；production零变化。 | 24/24无错误，每例3 observations/3 primary、distinct X=3、distinct Y=3、hard gate PASS；directional=24、primary=72、unique bags=144、rho与bootstrap lower均1、Wilson lower约0.862；最大资源比约1.034。正式Stage1仍待clean committed V3R8执行。 |
| G32-I119 | V3R8-FULL-DUAL-COHORT-PROBE-PASS | 新识别cohort单独通过仍不能排除原120例安全回归或合并资源门失败。 | Stage1最终结论必须同时满足原安全覆盖与新增方向识别，不能只看24例。 | 在同一已冻结revision上做一次完整120+24只读探索，按外层AND合并两个cohort；不写正式证据、不改阈值。 | 只增加一次直接面向最终gate的运行；C++、算法和地图零变化。 | 144/144执行无错误，safety与identification均PASS；directional=24、primary=72、unique bags=144、rho/bootstrap lower=1、positive share=1、Wilson lower约0.862；最大events/junction/runtime/total比约1/1/1.003/1.0365。正式结论仍由clean committed build与Nanning shadow决定。 |
| G32-I120 | V3R8-STAGE0-ROW-METADATA-PARITY-NO-GO | V3R8正式Stage0在`shadow_repeat_exact`停止，未进入Stage1。 | `run_case()`提取基准row时新增了`cohort=safety_regression`，独立repeat提取仍用旧metadata，漏传同一标签；物理执行没有漂移。 | append-only保留V3R8失败工件；repeat提取补上与基准相同的cohort/replica标签，并以新V3R9证据revision重跑。 | 一处metadata参数与机械revision更新；C++、释放几何、地图、算法、threshold均不变。 | 基准与repeat均4 rows，逐字段比较唯一差异为`cohort`；ordinary payload、extension、join、资源和其余Stage0 gates全一致/通过。 |
| G32-I121 | V3R9-METADATA-PARITY-REVISION-FROZEN | V3R8失败路径已被append-only工件占用，不能覆盖后冒充同一次成功。 | 修复虽不改变物理结果，仍需要新的证据身份和输出路径。 | 在修改代码前冻结V3R9 addendum：只允许repeat row metadata与baseline对齐，完整复用V3R8的144例、阈值和南宁控制。 | 新增一份短addendum和机械revision常量；不增加新gate。 | `docs/G4IRSF32_v3r9_stage0_metadata_parity_addendum.md`登记新身份、四个新输出路径和唯一允许变更；尚未执行V3R9。 |
| G32-I122 | V3R9-SYNTHETIC-PASS-PAIR-LOADER-NO-GO | V3R9正式Stage0与双cohort Stage1全部PASS，但composer在synthetic deep replay停止，未运行南宁shadow。 | loader要求pair逐值包含全部source observation；join合同会把重复bag诊断的派生`H_gap/X_insert`改为null，并由既有状态/算术校验单独验证，故该通用包含关系是假门。 | append-only保留V3R9 synthetic PASS与campaign NO-GO；新revision的pair/source身份绑定只比较非`JOIN_PAIR_KEYS`字段，随后仍执行原有完整pair schema、重复诊断状态和primary算术校验。 | 一处loader集合差；runtime、C++、144例、threshold和统计均不变。 | synthetic为`V3R9_SYNTHETIC_P0_PASS_NANNING_PENDING`：Stage0、Stage1及全部gate通过；2736 observations/pairs身份一一对应，差异只出现在2487条合法重复诊断的`H_gap/X_insert`，24例identification无此差异。 |
| G32-I123 | V3R10-PAIR-BINDING-REVISION-FROZEN | V3R9正式输出路径已有不可覆盖结果，loader修复需要新的证据身份。 | 不能修改或删除已知NO-GO后在同一路径重签。 | 修改loader前冻结V3R10：唯一允许语义变更是pair绑定排除既有`JOIN_PAIR_KEYS`，完整pair validator、实验队列、阈值和南宁门原样复用。 | 一份短addendum、一个集合差和机械revision迁移；不新增gate。 | `docs/G4IRSF32_v3r10_pair_binding_addendum.md`登记唯一修复与四个新输出路径；尚未运行V3R10。 |
| G32-I124 | V3R10-SYNTHETIC-PASS-STAGE0-J2-REPLAY-NO-GO | V3R10再次正式通过Stage0与双cohort Stage1，deep loader越过pair绑定后在Stage0 case 1的service profile停止。 | case 1是冻结J2 fixture，但重放循环读取了`fixture_name`后未把`j2=true`传给`build_request()`；重建请求因此少node 4，合法全局服务证据被误判为未知节点。 | append-only保留V3R10；不再为每个loader假门重复跑144例，先用已PASS的V3R10 synthetic作只读全链loader回归，清完实际出现的角色/JSON兼容问题，再以一个新revision正式重跑。 | 当前已知修复为重建J2角色的一处参数；不改runtime、case、threshold或统计。 | V3R10 synthetic仍为完整PASS；J2实际completion/reservation含node 4，`build_request(..., j2=true)`同样包含node 4，而loader错误使用默认direct请求。 |
| G32-I125 | V3R11-DEEP-LOADER-FULL-REPLAY-PASS | 若逐个正式revision发现loader假门，会重复执行144例而不增加效果证据。 | V3R10 immutable synthetic已经完整PASS，可作为只读真实producer回归输入一次走完loader。 | 探索性修复J2角色重建后继续全链重放，发现并修正唯一下一项：map2 loader只接受4个identity keys，而producer固定输出6个；改为对fresh rebuilt fixture的6字段精确相等。 | 两处直接兼容修复；删除逐门正式重跑需求，不新增防御。 | 经source/implementation身份代理到V3R10 frozen值后，完整`load_and_validate_synthetic_artifact`返回V3R10 synthetic/Stage0/Stage1三层PASS；未再出现兼容失败。 |
| G32-I126 | V3R11-DEEP-REPLAY-REVISION-FROZEN | V3R10 campaign路径已有NO-GO，不能覆盖。 | 两处兼容修复已由完整只读回归穷尽，需要新append-only身份正式复验。 | 在实现提交前冻结V3R11 addendum及四个新路径；仅允许J2重建角色和map2六字段exact replay，保留V3R10 pair binding修复。 | 一份短addendum和机械revision迁移；实验零变化。 | `docs/G4IRSF32_v3r11_deep_replay_compatibility_addendum.md`已登记；尚未运行V3R11。 |
| G32-I127 | V3R11-NANNING-REAL-NON-OVERLAP-NO-GO | V3R11 synthetic与deep replay全部PASS并首次进入南宁shadow；1x/2x均0 observations，唯一失败为node49/upstream53 admitted。 | V3R7 selector用external commit time与node49 local service比较，却未把53→49的60.1秒travel加入arrival；所选local pair在外部真正到达前约59秒已清空。 | append-only保留V3R11；从同一冻结输入行中重新选择最小外部前缀与node49本地行，但重叠条件改为真实`external arrival = commit + travel`，仍只用输入/地图/固定service，不读G32 outcome。 | 只改control cohort选择公式与新revision；G32、threshold、地图和P0统计不变。 | 两尺度所有安全/资源/exact-off门通过，且各有`non_overlap_count=1`、`observation_stored_count=0`；1x local clear约58204.302而external arrival约58263.101，2x local clear约45001.160而arrival约45060.101。 |
| G32-I128 | V3R12-ARRIVAL-COVERED-MINIMUM-CANARY-FROZEN | 把local release直接对齐external arrival会违反commit-seam的no-future约束；正确几何需要commit时local已释放，且已有外部预约连续覆盖到当前external slot。最小规模因此不是1E+1L或旧2L，而是61E+1L。 | 53→49 travel=60.1秒、node49 service/外部slot步长=1秒；rank<60时commit至first-arrival仍有至少1秒空档，本地任务会先处理完；rank60才使该空档不足1秒，并由前60个外部预约把local虚拟槽推到当前外部slot。 | bounded只读诊断先找出所有rank60候选并保留所有负结果；不放宽stale=0门。冻结共同的68400 engineering canary：canonical 61 external + local `25195:direct`，明确登记`selection_outcome_blind=false`，只证明状态存在/可复验，不估计频率或收益；Stage2仍须在closed-loop outcome前独立预注册。 | 新control选择与证据revision；C++、动作、阈值、地图、workload行和P1均不变。62行达到理论下界，避免加入长local链或新策略。 | 临时G31两尺度均62/62完成、全部audit PASS、两类stale=0；临时G32两尺度各stored=1、non-overlap=0、ready_count=1、`L0=external_slot=68520.101`。这些仅为冻结依据，正式V3R12输出尚未运行。 |
| G32-I129 | V3R12-REUSE-ONLY-STAGE01-COMPOSITION | V3R11已用当前同一G32 binary正式完成120+24例并通过完整deep replay；再次执行相同144例只会消耗时间，不增加新效果信息。 | V3R12没有C++、case population、release geometry、阈值或统计变更；唯一的新证据缺口是active G31 control与Nanning G32 shadow。 | 不生成V3R12 synthetic工件；composer从固定V3R11路径读取immutable PASS工件，经既有full loader重放全部nested证据，再运行新control/shadow。最终报告只保存摘要并明确synthetic executor调用数为0。 | 删除一次重复144-case执行；增加一个独立reuse-only composer，不改runtime、gate或P1。 | 聚焦canary+composer测试16/16通过；真实V3R11 full loader约9秒返回Stage0/Stage1 PASS，未调用synthetic executor。正式V3R12 control/shadow仍待clean committed执行。 |
| G32-I130 | V3R12-P0-REAL-CANARY-GO | 冻结的61E+1L几何需要确认既能在G31 exact-off下安全完成，也能在同一G32 binary的shadow seam中真实产生mixed-origin observation。 | V3R11失败是commit与arrival相差60.1秒造成的`non_overlap`，不是shadow helper、统计门或安全阈值错误。 | 在clean committed实现上正式运行两尺度G31 control；提交其append-only工件后，以新进程完整deep replay既有120+24 Stage0/1并运行两尺度G32 shadow。没有重跑相同synthetic、没有改C++、阈值或历史NO-GO。 | 仅新增62-row active control、reuse-only composer和两份正式P0输出；生产算法仍为shadow，action change为0。 | G31 1x/2x均62/62完成、qualifying=1且全部checks PASS；G32两尺度均stored=1、admitted=1、non-overlap/future-read/global-scan/action-change=0；V3R11 Stage0/Stage1 full replay PASS，最终`GO_V3R12_NANNING_P0_ENGINEERING_CANARY_P1_REVIEW_ALLOWED`。 |
| G32-I131 | V3R13-CANDIDATE-A-ACTION-AND-STAGE2-FROZEN | P0证明真实mixed-origin状态存在，但external seam直接hold到L0会让外部多等整段53→49 travel；伪造local J2 edge/capability又会引入第二套authority。 | local、DIRECT、J2本来就共享同一node service calendar；缺口只是local source只会抢当前槽，不能在已存在external reservations时拥有下一个未占用槽。 | 冻结唯一改动：closed_loop下，已释放local head经现有FIFO/deadline/aging与bounded pending比较后，最多预订一个同calendar next-free slot；已commit external不撤销，下一local由既有wakeup再考虑。同步冻结outcome-blind 19200--19800 raw-ID closure与10个Stage2语义case，candidate前先跑G31 active/inactive trace断言。 | 一个append-only mode与source-admission分支；无新arbiter、route state、模型、地图ID、系数或全局扫描。Stage2为10 case/20 paired runs。 | `docs/G4IRSF32_v3r13_candidate_a_closed_loop_stage2_preregistration.md`已登记动作、反向优先/no-op/safety/resource门、1x 540/998、2x 877/1599人口及Nanning single1/single8、map2 single1案例；尚未实现或运行closed_loop。 |
| G32-I132 | V3R13-HISTORICAL-BUILD-DIRECTORY-RECOVERED | 实现子任务误把候选增量编译进历史`build_g32_v3r2`，覆盖了该路径的旧pyd；已提交源码和既有结果未变。 | 构建目录没有按revision隔离，且子任务在统一验证前自行增量构建。 | 停止继续搜盘或追逐bitwise hash；从冻结历史源码提交`18d92c1`在独立临时工作树重建V3R2，原生proof通过后只恢复该旧路径；V3R13固定使用新`build_g32_v3r13`。旧正式工件不重写，新重建不冒充原bit identity。 | 生产代码零变化；新增一次可删除的临时构建，已移除临时source worktree。 | 历史原生proof PASS，历史Python focused 7/7 PASS；V3R13新目录构建与旧目录分离。原pyd的bitwise identity未恢复，后续复验只陈述源码等价与功能测试。 |
| G32-I133 | V3R13-P1-REAL-REVIEW-FIXES | 初版有三个实际问题：有限queue capacity下future owner可能越过未来容量；G17/G23会把future admission提前记到决策时刻；C++测试把临时Graph交给持有引用的runtime导致访问异常。 | future-slot扩大了决策到服务之间的时间窗；旧telemetry只支持即时admit；测试fixture的Graph生命周期短于runtime。 | 不增加容量预测或第二套telemetry：有限容量、G17 extensions或G23 causal action组合直接回退ordinary admission；测试Graph改为局部持久对象。 | closed_loop条件增加三个保守guard；无新状态、队列、计数器或扫描。 | 独立只读复核无剩余阻塞项；新目录原生测试PASS，V3R13 runtime 3/3、历史V3R2 7/7 PASS。 |
| G32-I134 | V3R13-J2-ACTION-GATE-FIXED | J2动作门最初把external与local近乎同刻释放，local决策时pending尚不可见；反向优先又把“external先赢”机械写成“local以后永远不得预留”。 | 第一个是fixture时序错误；第二个混淆了pending仲裁与external已commit后出现的新空槽，后者并非越权。 | external改为0.005、local保持0.01，使当前pending在决策前真实存在；反向用node-service episode顺序验证urgent external先服务，同时允许其commit后local预留下一槽。门槛和case角色不变。 | 仅调整两行释放时刻和一个三态动作预期；算法零变化。 | 注入runner 8/8 PASS；真实24-run全部PASS，reverse external service start严格早于local，future-release perturbation exact。 |
| G32-I135 | V3R13-CANDIDATE-A-P1-GO | 需要证明最小future-slot不是只在mock中成立，并保持off/shadow、安全、完成和资源边界。 | 最终实现只在现有source admission与同一calendar内动作；pending排序复用现有controller comparator。 | 在提交后的新V3R13 Release binary上正式执行8个DIRECT/J2/no-op/future扰动case的off/shadow/closed_loop三臂，共24次；不运行真实Stage2 outcome。 | 生产改动仍为一个append-only mode；动作门复用普通event与现有两项counter，没有新增动作sidecar。 | `PASS_V3R13_CANDIDATE_A_PRE_STAGE2_ACTION_GATE`；全部case、hard safety、off exact、shadow inert通过，events/completed与local memory最大比值均1.000，正式授权Stage2。 |
| G32-I136 | V3R13-STAGE2-LONG-RUN-MEASUREMENT-CORRECTION | 首次20-run结果把合成Stage1的`stale arbitration=0`、`starvation_count=0`和`time_limit_reached=false`机械套到真实长程/固定故障时域，导致全部case出现假安全失败；同进程又不能把peak RSS归因到单臂。 | stale wakeup会被安全地丢弃；`starvation_count`是超过等待阈值的诊断计数而非未完成袋；故障repair可在全部可达袋完成后落在固定时域之外。三者都不等于failed/conflict/unsafe。 | 永久保留首次NO-GO输出；不改case、人口、阈值或candidate，只把真实安全门收敛为failed/conflict/physical violation/deadlock/full-A*/global/future/model/one-hop/merge integrity，并将stale与固定时域标为诊断、starvation改为candidate不得高于control。普通event trace由全量改为0，避免为20次运行物化无用事件字典；随后用同一binary和同一20臂重跑到corrected路径。 | 净减少运行期事件内存；只改evaluation分类，不改C++、请求、动作或路线。RSS仍诚实标为未独立归因，不用内部accounted bytes冒充。 | corrected run全部10 pair的hard safety、完成、固定cohort和no-new-starvation通过；map2 action=0。聚焦测试3/3通过，初始和corrected输出均保留。 |
| G32-I137 | V3R13-CANDIDATE-A-STAGE2-NO-GO | Candidate A在真实两图slice中安全且有动作，但没有达到原计划的效果门。 | 动作覆盖太稀：南宁每case仅2--5次；1x start-49 source-wait proxy下降约2.52%/2.85%，2x约1.28%，均低于5%；六个南宁case的冻结target P95比值全部为1.000，远未达到至少2%下降。active-fault 1x还出现source wait下降但target总时延未下降的转移门失败。 | 按预冻结规则立即停止，不进入Stage3/4，不放宽阈值、不重选cohort、不删除A。保留A作为单独可逆机制；下一轮只启动原计划顺序中的Candidate B，用一个append-only S4 scorer mode把旧raw pressure term替换为direct-neighbour已释放且未被calendar覆盖的本地工作秒数，系数固定1。完整mixed-origin积分与独立RSS只在新候选接近其余效果门时补最小遥测，避免先造框架。 | 本轮生产复杂度零增加；20-run runner不保存raw payload/event。下一候选限制为一个score term replacement，无新planner、队列、模型、地图ID或参数网格。 | corrected状态`NO_GO_V3R13_CANDIDATE_A_STAGE2`、Stage3未授权；两图完成数双臂相同，map2 mean/P95/P99与action均exact，全部measured events/wall/三类queue比值≤1.10（wall最大约1.0861，queue均1.0）。南宁全系统回归门均通过，但target P95效果门6/6失败。 |
| G32-I138 | V3R14-CANDIDATE-B-MINIMAL-DESIGN-FROZEN | A的future-slot只影响2--5次真实动作，未改变target P95；原计划下一项是修正S4把raw件数直接加到秒分数的量纲残差。 | 当前S4把ordinary target queue与scheduled incoming件数相加，既非秒单位，也可能与已存在的target calendar wait重复。现有direct-neighbour `source_queue`恰好只含已释放、ready且尚未取得service reservation的本地工作。 | 在任何B实现或outcome前冻结唯一新scorer spelling：只用O(1) source-queue work-seconds替换旧raw count term，系数1；旧S4/A/J2/guard不变。先做0/1/2 ready、1/3秒、future-release、A reserve后不双计和ranking flip的小门。真实screen复用V3R13同10 case，固定off/A/A+B三臂；只有core效果过线才补独立RSS与连续mixed-origin积分，避免为又一个负候选先造遥测框架。 | 一个pure work helper、一个append-only scorer spelling和allowlist；不新增动态状态、参数、planner、模型、扫描、地图特判或实验网格。 | `docs/G4IRSF32_v3r14_candidate_b_uncovered_work_preregistration.md`已冻结公式、顺序、screen门、输出路径与停止规则；尚未实现或运行Candidate B。 |
| G32-I139 | V3R14-CANDIDATE-B-PREACTION-PASS | 新单位一致项必须先证明只计算已释放、未被calendar覆盖的本地工作，且旧S4不会被新拼写暗中替换。 | `source_queue`本身就是ready且未预约的本地集合；Candidate A预留后元素离队，既有calendar wait继续表达预约。 | 新增一个纯O(1) helper和一个显式scorer拼写；原生小图验证0/1/2件、1/3秒、future release、raw score与ranking，screen又强制读取native `scorer_mode_echo/scorer_id`，不能把request拼写冒充实际执行。 | 生产增量为一个helper、一个score分支和两层allowlist；无新ABI参数、状态、队列、模型或遥测。 | 全量focused C++可执行测试PASS；新pyd的旧S0--S4及新拼写6/6 identity/backend测试PASS；screen聚焦测试6/6 PASS。 |
| G32-I140 | V3R14-CANDIDATE-B-CORE-SCREEN-NO-GO | B在南宁产生可见但不足的改善，同时破坏map2高负载；不能通过补RSS、改阈值或挑case挽救。 | 用uncovered source work完全替换旧ordinary pressure后，南宁target P95只降约0.56%--1.39%，未达到2%；map2 2x失去原压力信号，P95约升8.14%/8.27%，junction queue峰值约升20.5%/19.7%，starvation诊断也增加。 | 按冻结停止规则归档B，不构建RSS worker或连续mixed-origin tracker，不调系数、不覆盖NO-GO；回退只需选择旧S4拼写。继续前仅检查原计划C的必要前提，而不直接实现token。 | 30臂screen没有新增生产状态；失败后停止遥测扩建，保留一个可逆append-only scorer用于审计。 | `NO_GO_V3R14_CANDIDATE_B`；10个固定case/30次执行和native scorer identity均完整。南宁六例P95比值为0.9861/0.9883/0.9874/0.9896/0.9944/0.9896；map2 1x亦有P95约1.0155/1.0097回归，Stage3未授权。 |
| G32-I141 | V3R14-CANDIDATE-C-ENTRY-CONDITION-NOT-MET | 原计划只在trace证明strict guard经常挡住有益局部候选时才允许一次性detour token；A/B不足本身不是充分条件。 | 完整decision trace显示非下降邻居很多，但它们不是当前局部更优项：南宁1x稳定12,715个decision中，raw S4最优项被strict guard挡住0次；5,621个非下降替代中，calendar saving覆盖`ΔH+travel delta`者0。map2 1x稳定6,990个decision同样raw-best阻挡0，2,433个非下降替代中有正calendar saving且局部净优势者0；两图trace均未截断。 | 保留strict descent，不实现token、不搜索`ΔH`阈值。诊断只复用已冻结1x稳定slice、旧S4+A和完整candidate rows；没有改变候选、地图或gate。下一改动必须针对已观测的混合来源服务/压力表达，而不能假称detour有证据。 | 生产复杂度零增加；没有新增per-bag token、历史状态、loop guard或参数网格。 | Candidate C入口判定`NO_GO_NO_USEFUL_STRICT_GUARD_BLOCK`；南宁冻结target cohort的locally-dominant blocked count同样为0。 |
| G32-I142 | V3R15-CANDIDATE-A-COMMIT-RECHECK-FROZEN | A有ready source backlog却只动作2--5次；直接扩大预约深度会增加状态，新增scorer又不能直接修复source P95。 | source wake按提交当时的next-free排定；后来external DIRECT/J2占用同一calendar时只发beacon，不使source重查。普通wake最终在日历空闲点运行，因而绕过A的busy-calendar分支。 | 在结果前冻结一个append-only `closed_loop_commit_recheck`：成功commit后的既有incoming beacon若遇ready source、已有较晚wake、当前service busy且没有future local owner，就用现有generation机制把wake提前到commit时刻；历史`closed_loop`不变，所有选择/优先级/预约仍由原`try_admit_source`执行。真实screen原样复用V3R13的10 case和阈值，off/A/A+recheck三臂中A仅作归因。 | 一个mode拼写与一处本地wake调度调用；不新增字段、counter、scorer、参数、planner、地图分支、future/global读或预约深度。 | `docs/G4IRSF32_v3r15_candidate_a_commit_recheck_preregistration.md`已冻结唯一代码缝、pre-action门、三臂screen和停止规则；尚未实现或运行V3R15。 |
| G32-I143 | V3R15-COMMIT-RECHECK-PREACTION-PASS | 仅证明wake被提前不足以说明算法有行为差异，必须证明它能在后续未commit external到来前取得一个更早且合法的local slot。 | 同一J2小图中，首个external已commit槽必须不可撤销；recheck只允许现有deadline comparator让更早deadline的local取得下一槽，随后尚未commit的external顺延。 | DIRECT断言旧wake 1.000、新wake 0.201且旧event经generation supersede；双external J2断言首个external finish不变、local admitted 3.0→2.0、后一个external随后服务。另有no-source no-op；历史`closed_loop`与新mode分开执行。 | 新模式仍无新字段/counter；每次retime最多遗留一个由既有机制拒绝的superseded event，资源由原10%门审计。 | 新目录focused native PASS且V3R2 proof全true；fresh backend回显新mode并完成/安全、action=mutation=1；共享V3R13/V3R14与V3R15 runner聚焦15/15 PASS。真实screen尚未运行。 |
| G32-I144 | V3R15-PREFORMAL-CHOICE-FREE-REVIEW-FIX | 正式screen前复核发现，首版beacon分支为判断目标日历而提前调用了`choose_bag()`；这会让“提交钩子不选任务、唯一选择权仍在`try_admit_source`”的冻结边界不成立。 | eligibility guard复用了所选bag的goal检查，虽然没有当场预留，仍把一次无必要的选择带进commit seam。 | 在任何正式输出生成前停跑并删除bag选择、priority比较和goal读取；beacon现在只检查该node一个service quantum的本地calendar确实忙且无future local owner，然后调度既有wake。后续`try_admit_source`仍是唯一选择、比较和预留位置。 | 净删10行；没有新增状态、分支层、counter、参数或防御case，反而收窄了commit seam。 | 独立复核确认blocker解除；修正版重新构建后native proof通过，fresh backend新mode完成3/3、失败/冲突/future-read/global-scan均0。正式screen此前未启动、输出路径仍为空。 |
| G32-I145 | V3R15-COMMIT-RECHECK-CORE-SCREEN-NO-GO | 三臂正式screen中，A+recheck在10个固定case内除不可归因wall计时外与历史A逐字段相同；南宁六例target P95比值均为1.000，故不能进入Stage3。 | commit beacon发生在外部任务离开上游的时刻，而53→49等service reservation从未来到达时刻才开始；修正版guard检查“beacon当前时刻calendar忙”，真实长边上通常为假，因此没有把既有source wake提前到新占用真正开始的边界。 | 保留本次30臂NO-GO，不加RSS或完整mixed积分，不放宽2%/5%门。下一revision只允许把已成功提交的arrival/slot-start随既有beacon传到同一局部钩子，并把wake最多提前到该时刻；仍不在commit seam选bag或预留，实际动作继续唯一发生在`try_admit_source`。 | 当前正式运行只新增两份append-only结果；失败后不扩遥测。下一候选拟复用既有event字段和wake机制，不新增事件类型、状态、counter、scorer、参数或地图分支。 | `NO_GO_V3R15_CANDIDATE_A_COMMIT_RECHECK`；30/30完成且安全门通过，map2四例exact、action=0；南宁A与A+recheck动作均为2/2/3和5/5/5，source-wait与全部确定性结果相同，Stage3仍未授权。 |
| G32-I146 | V3R16-S4-PLUS-UNCOVERED-WORK-FROZEN | V3R15没有新增动作；最初拟把已commit slot-start再传给wake，但完整trace显示A/recheck的141,395个业务事件相同，node49又是单入边DIRECT而非J2，继续给提交缝加握手缺少收益依据且会扩大事务复杂度。V3R14则有真实南宁P95正信号，却因删除旧S4普通网络压力而使map2回归。 | V3R14把`target_queue_length + target_scheduled_incoming`整体替换为source ready work；前者是map2仍需要的网络压力，后者是旧S4完全未表示、且尚未被calendar覆盖的本地source工作，两者并不重复。I145提出的arrival-wake方向因此在实现前撤回。 | 冻结一个新scorer拼写：历史S4原样保留，只追加现有pure helper计算的`source_queue.size × local service`；固定系数1，不搜索参数。真实screen仍为同10 case的off/A/A+new-score三臂和原阈值。 | 复用V3R14 helper，只增加一个score组合分支和allowlist/runner；无新状态、counter、模型、握手、地图分支或遥测。 | `docs/G4IRSF32_v3r16_s4_plus_uncovered_local_work_preregistration.md`已在实现和outcome前冻结；尚未实现或运行。 |
| G32-I147 | V3R16-PREACTION-PASS-AND-MECHANICAL-TEST-TRIM | 初版实现把历史S4完整表达式复制进新分支，并为同一身份检查重复写了多组不可能case；功能虽对，但不符合本项目的最小复杂度边界。 | 从V3R14模板机械派生，未先把“旧公式共同路径”和“唯一新增项”分离，测试也逐条照抄rubric。 | 让历史S4与新拼写共用同一旧公式分支，新拼写只在末尾条件性加pure helper；原生fixture从7次收敛为4次，runner身份负测从3种收敛为1种，保留空source exact、两件ready精确增量、ranking flip、native echo和安全证明。 | 生产公式净为一个predicate和一次helper调用；测试删去重复运行，没有新增状态、counter或防御分支。 | 修订后独立`build_g32_v3r16` native PASS且旧V3R2 proof全true；fresh backend新spelling完成3/3、failed/conflict/full-A*/global均0、one-hop；runner 5/5 PASS。只读复核确认无双计、旧S4/V3R14 exact、第三臂仅改scorer；正式输出仍不存在。 |
| G32-I148 | V3R16-CORE-SCREEN-NO-GO-NO-REAL-RANKING-DELTA | 新组合在合成小图能翻转排名，但固定真实slice中A+work与历史A的全部确定性业务、事件、动作、队列和安全结果10/10逐字段相同；南宁六例target P95仍全部1.000。 | 现有旧S4网络压力与calendar项下，追加的ready-source工作秒数没有跨过任何真实候选score gap。V3R14的南宁小改善来自整体删除旧压力后形成的不同排序，而那一删除同时导致map2明显回归，不能把两者机械拼接成收益。 | append-only归档30臂NO-GO，不补RSS/连续积分、不调系数、不做`max/sum`参数变体。下一步先用已存在的完整decision trace和完成bag结果定位target P95边界上的实际可改决策，只在存在跨图安全的单一局部反事实时再冻结代码；若没有则停止算法加法并转入清理失败mode。 | 本次运行只新增两份结果；失败后生产复杂度不再增加。新scorer可由选择历史S4立即回退，最终冻结前应删除无收益spelling而由git历史保留审计。 | `NO_GO_V3R16_S4_PLUS_UNCOVERED_LOCAL_WORK`；10 case/30臂全部完成且安全，map2四例exact；南宁actions仍为2/2/3和5/5/5，A+work/A除wall噪声外完全相同，Stage3未授权。 |
| G32-I149 | POST-V3R16-TRACE-DECOMPOSITION-AND-BLIND-ALTERNATIVES-CLOSED | V3R14有南宁正信号却伤害map2，若不先分解就继续写公式，会把项目变成参数试错。 | 完整trace重建显示V3R14的变化几乎全部来自删除`target_scheduled_incoming`，新增uncovered work在关键候选为0；全删scheduled同时制造南宁76/80/84的收益、南宁36/37的局部伤害和map2 node22等全部坏翻转。calendar-covered条件丢失南宁1100个翻转中的1073个；queue-only、goal-conditioned和FIFO改aging也分别因信号不足或真实回归被否定。P95边界不是单个极端值：1x至少约6个、2x至少约9个start-53外来bag需越过2%线，主要损失是junction wait而非node49 source wait。 | 不实现calendar条件、不跑参数网格、不继续source排序或49/50 scorer加项；停止三条重复诊断。只保留一个利用现有节点类型与service time的无参数Pareto反事实，并在实现前做完整trace/map2零作用计数。 | 只读分析零生产改动；现成aging四个stable实跑后直接淘汰，未生成正式候选或新增配置。 | 1x/2x边界审计、old/B committed-decision分解和map2反例均完成；结论明确区分“无单字段方向”与后续typed dominance新假设。 |
| G32-I150 | V3R17-TYPED-SERVICE-DOMINANCE-FROZEN | 不能靠节点ID只保留V3R14的好翻转；同时也不能把所有非goal unloader禁入，因为type-2节点可合法承载其他bag的transit/service。 | 完整old trace中存在通用局部支配关系：历史S4 winner是非goal type-2时，部分shield-legal strict-descent type-4邻居在`travel+potential`和service duration两项都严格更优。该规则在南宁1x/2x分别覆盖288/800个ranking、target 51/62次并触达P95边界；map2两个stable slice均0。 | 在任何V3R17 outcome前冻结唯一guard：历史S4和strict-descent后，仅把按`(travel+potential, service, node)`最小的双严格支配type-4候选移到首位；first-edge-credit active时no-op。固定复用原10 case、原阈值和off/A/A+guard三臂；core未过不补RSS/连续积分。 | 一个append-only scorer拼写和O(outdegree)既有候选扫描；无新状态、counter、系数、模型、地图分支、路线后缀或预约。 | `docs/G4IRSF32_v3r17_typed_service_dominance_preregistration.md`已在实现、build和真实outcome前提交；V3R17尚未运行。 |
| G32-I151 | V3R17-PREACTION-PASS-AND-TEST-SCOPE-TRIM | 初版原生测试按每个guard分支展开到211行，且本机全局pytest临时目录出现并行权限冲突；两者都不应演变成生产防御或准备工作。 | 前者是把冻结条目机械逐项翻成case，后者只是Windows共享临时目录冲突，均不代表算法缺陷。 | 按用户要求把原生验证收敛为一个正向motif，只证明旧S4选type-2、新spelling把base/service双严格占优的type-4置顶，并同时核对scorer identity、原分数不变、one-hop、零full-A*/global/future；pytest改用本工作树临时目录，不改业务代码。 | 生产增量仍只有一个O(outdegree)候选重排和两层allowlist；无新参数、状态、counter、地图特例或分支防御。原生测试由211行缩至111行。 | 独立`build_g32_v3r17`中native proof PASS；fresh-process backend新spelling 1/1 PASS；V3R17 runner 3/3 PASS；正式screen输出仍不存在。 |
| G32-I152 | V3R17-CORE-SCREEN-NO-GO-EFFECT-BELOW-THRESHOLD | typed dominance在两图上按预分析方向生效，map2保持exact、南宁均改善，但六个南宁target P95都未达到冻结的2%门，且两个1x case新增starvation阈值穿越。 | 参数自由的type-2→type-4局部支配只覆盖了部分有益路由：1x P95下降约0.72%--0.98%，2x约0.47%--0.64%；它减少部分网络时间，却没有让足够多P95边界bag越过目标线。 | append-only归档30臂NO-GO；不补RSS或完整mixed-origin积分，不放宽2%门，不把typed规则扩展到非支配候选，也不按节点调特例。下一步只读复用既有trace，寻找未覆盖的跨图安全语义或明确的junction/storage-out等待缝。 | 本轮只新增两份正式结果；失败后不再扩生产规则。新scorer仍是可选拼写，历史S4/default完全不变。 | `NO_GO_V3R17_TYPED_SERVICE_DOMINANCE`；map2四例mean/P95/P99均1.000，南宁全系统mean/tail及完成/硬安全主体通过，但P95效果门6/6失败；Stage3未授权。 |
| G32-I153 | V3R18-SOURCE-RELEASE-TIE-LONGEST-STATIC-FROZEN | V3R17后继续改route scorer无法消除主导尾部：1x边界包在start-53 burst后于53/49排队约221--263秒，后续多数已走最短type-4路径。 | 同时释放的source bag在旧FIFO中按runtime ID打破平局，未利用不同goal的剩余静态行程；这会让长残程包既晚出又走更久，抬高尾部。 | 结果前冻结唯一source comparator：按`(source_enqueued_at, -static_potential, runtime_id)`；不同释放时刻和全部junction queue保持历史FIFO。固定旧departure slots与bag residual的只读置换在南宁1x/2x给出P95 ratio 0.9053/0.9685，map2 0.9437/0.9554，均值因纯置换为1；仅作必要性/方向证据。 | 一个append-only queue spelling和现有local queue comparator；无新状态、counter、参数、模型、地图/节点特例、全局扫描或预约。 | `docs/G4IRSF32_v3r18_source_release_tie_longest_static_preregistration.md`已在实现/build/outcome前冻结；V3R18尚未运行。 |
| G32-I154 | V3R18-SOURCE-RELEASE-TIE-PREACTION-PASS | 最小原生夹具最初沿用E4的JIT merge参数却切到E3，触发配置冲突；修正后又因合成图未给错误目标设置高势能，旧S4持续选择非目标terminal并HOLD。fresh binding检查还误读了不存在的summary queue字段。 | 前两项都是测试夹具表达不完整，第三项是验证脚本假设，不是算法、binding或真实图缺陷。 | 夹具专用merge改为eager，并补齐不同goal的静态势能使旧S4先选各自合法目标；不向生产代码添加case防御。binding改用真实完成数和实际加载路径确认新拼写被新pyd接受，不为一次检查新增遥测字段。 | 生产增量只有source-only chooser及一个append-only queue spelling；Candidate A的local winner复用同一helper，junction继续原`choose_bag`，无新状态、counter、系数、模型、地图特例或预约。 | 独立`build_g32_v3r18`中native proof PASS；fresh binding新spelling完成1/1；固定三臂runner 3/3 PASS；正式screen输出尚不存在。 |
| G32-I155 | V3R18-CORE-SCREEN-NO-GO-MEAN-AND-STARVATION-REGRESSION | 长静态残程优先在多数南宁case显著压低target P95，却同时使全系统均值、target均值/高尾和starvation变坏，并在map2产生同类回归。 | 规则只在同批次重新分配既有source slots；它没有减少总source work。长尾包集中先发会把其后续长行程/共享前缀负载集中到网络，南宁target source-wait总量与Candidate A相同而network总量增加约3.8k--6.1k秒；短尾包后移又增加全系统均值与阈值穿越。离线固定slot置换忽略了这些闭环网络级联。 | 按冻结停止规则归档NO-GO；不把tie扩到later release/junction，不加权、不调批次比例、不补RSS或完整mixed-origin积分。下一步只读检查少数有调度理论依据的参数自由source规则能否同时守住mean/starvation；若不能则结束source-order方向，转向真正减少等待而非交换身份的局部机制。 | 正式运行只新增两份append-only结果；失败后不扩生产机制。V3R18保持可逆新拼写，default/历史FIFO不变。 | `NO_GO_V3R18_SOURCE_RELEASE_TIE`；30/30完成且硬安全通过。南宁1x target P95 ratio 0.918--0.933、2x为0.941或0.986，但whole mean ratio 1.009--1.023且10/10新增starvation；map2 mean ratio 1.017--1.024并有P95/P99回归，Stage3未授权。 |
| G32-I156 | POST-V3R18-SOURCE-ORDER-FAMILY-CLOSED | V3R18后若继续换source comparator，可能只是在交换同一批departure slots的身份，既不能直接减少mixed-origin等待面积，也容易再次把拥塞推入下游。 | 四个stable slice中，1x的18/18个同刻批次和2x的37/38个批次内部deadline相同，EDF基本退化为FIFO；shortest-static与自然goal交错的固定槽诊断达不到南宁目标或伤害map2。longest-static虽有离线P95方向，却在真实闭环中因junction级联产生远多于离线估计的starvation。南宁与map2主批的static长短顺序还相反，不存在自然的跨图单键方向。 | 结束source-only排序方向，不做EDF实现、不做权重/比例/参数网格，也不把目前只覆盖node49单个local bag的统一握手作为下一个独立候选。后继只考虑直接作用于已确认junction wait与starvation、使用已有本地状态的参数自由公平机制，并在冻结的同十case core screen失败即停；若core效果以后通过而精确mixed-origin门仍是唯一缺口，再按原P1语义单独复核统一仲裁。 | 本项为只读归因，生产复杂度零增加；后继不得改变source FIFO、旧departure slots、escape/fault/merge/held authority或default。 | 固定槽诊断仅作方向排除：longest-static P95/off为南宁1x 0.9093、2x 0.9735、map2 1x 0.9568、2x 0.9864，但南宁两scale均新增starvation；真实V3R18对应starvation为140→154、0→26、512→518、158→185，证明闭环下游损害主导。 |
| G32-I157 | V3R19-JUNCTION-CUMULATIVE-WAIT-ENTRY-NO-GO | V3R18的回归主要落在junction wait，参数自由的候选是让普通Q0路口按“既有累计等待+当前路口等待”最大者优先；若直接实现，仍可能只移动与P95无关的merge/hold顺序。 | 用Candidate A control的完整event trace重建四个stable slice普通队列，FIFO重放逐次吻合且最终累计等待反校验误差为0。南宁1x虽有993个潜在翻转，但986个落在hold/merge-request，仅7个直接commit；P95及以上只触及1个包。2x有4424个潜在翻转，4270个是hold/merge-request、154个直接commit，P95及以上只触及2个包。把这些最坏包的可改延迟不现实地降到0，P95改善上限也只有0.620%/0.693%，低于固定2%门。map2又有222/1138个潜在翻转且全为hold/merge-request，不是安全零作用区。 | 在预注册和生产实现前判定入口NO-GO；不新增queue spelling，不做累计等待权重、阈值或节点限制。继续只读定位至少能覆盖所需P95秩位置、同时有跨图保护语义的真实动作缝。 | 生产复杂度零增加；诊断复用完整trace和现有bag等待字段，没有新遥测、正式outcome或输出文件。 | `NO_GO_BEFORE_IMPLEMENTATION_P95_COVERAGE_UPPER_BOUND`；该规则虽覆盖部分已饥饿bag，但覆盖与南宁P95门错位，且map2 merge-request重排风险无法满足0.5%保护门。 |
| G32-I158 | EXACT-CALENDAR-WAKE-SEAM-ENTRY-NO-GO | 真实尾袋在53→49和49→50反复hold，看似可能由0.25秒retry下限错过calendar开放边界；若成立，可用一次无状态精确唤醒直接减少等待。 | 完整stable trace显示重试已经在精确资源边界commit：53→49在1x/2x均无ready而空闲的可用缝；49→50仅有3次共0.47634秒、4次共0.45235秒。即使不现实地把全部缝永久传递给P95，上限也只有0.0965%/0.0884%，而2%门需要约9.876/10.232秒。较早beacon只增加重复hold，不创造service slot；加快source轮询只把等待搬到非空junction queue。 | 在实现前判NO-GO，不改retry语义、不加arrival buffer或future-action authority。后续不再把重复hold数量误当成漏用容量，只有能减少实际满载calendar工作或改变足够多P95秩位置的动作才进入冻结。 | 只读trace审查，生产复杂度零增加；没有新文件、遥测或正式outcome。 | `NO_GO_BEFORE_IMPLEMENTATION_NO_MATERIAL_IDLE_CAPACITY`；现有节点53/49尾袋均在可用slot边界提交，单一wake修正无法达到效果门。 |
| G32-I159 | TRUE-MIXED-ORIGIN-UNIFIED-ARBITRATION-UPPER-BOUND-NO-GO | 原MD P1的完整语义是让local source head与external request竞争同一当前服务机会；现有Candidate A只预订一个future local slot，需确认是否因动作不完整才未达门。 | 重跑四个stable和全部六个fault/negative case后，南宁每例都只在node49 DIRECT seam出现overlap，且始终只涉及同一个local bag、ready count=1；1x三例为10/10/11个1秒slot，2x三例均7个，map2四例为0。即使让local无代价赢下全部slot，node49 proxy最多只降2.52%/2.52%/2.72%和1.25%，仍低于5%；该local bag远离P95秩，六例target P95精确不变。按既有M3年龄语义，external均已等待约140--318秒而local仅约0--10秒，真实统一仲裁甚至会保持external全胜或仅1x让local晚期赢少量slot。等服务slot交换也只会把等待转给external，不能减少合计mixed-origin wait-area。 | 关闭“动作不完整导致NO-GO”的解释，不把local source伪装成J2 request，也不扩展DIRECT PIBT事务去取消/重排已准备动作。保留历史Candidate A和阴性证据；后继必须影响多个P95边界bag并减少真实工作/错误压力，而不是只交换唯一local bag。 | 生产复杂度零增加；避免了跨source queue、PIBT batch transaction、rollback/calendar generation和merge capability的新authority层。 | `NO_GO_TRUE_UNIFIED_ARBITRATION_EFFECT_UPPER_BOUND`；原P1完整动作在冻结人口上同时不可能达到5% mixed-origin与2% target-P95门，现有Candidate A结果与此上界一致。 |
| G32-I160 | SCHEDULED-INCOMING-WORK-SECONDS-ENTRY-NO-GO | map2候选节点大多service=1，而南宁服务时长异质；把S4的`target_scheduled_incoming`从raw count改成`count×candidate service`是无参数、跨图可解释的量纲修正，可能保留map2同时扩大南宁typed收益。 | 用Candidate A stable完整trace和请求自带canonical service-aware potential逐决策离线重排：南宁1x有449个flip、触及67个target bag，但P95上尾只触及6个且两个插值支撑bag均未触及；2x有720个flip、触及88个target bag，但P95上尾只触及5个、两个支撑合计仅1个。固定2%门在2x至少约需9个边界bag越线，必要覆盖不足。map2 1x/2x均0 flip，只证明负控方向，不补足南宁覆盖。 | 在实现前判coverage NO-GO；不写新scorer、不改公式其他项、不扫描系数或count/work混合。 | 只读现有trace，生产复杂度零增加；没有新文件或正式outcome。 | `NO_GO_BEFORE_IMPLEMENTATION_P95_BOUNDARY_COVERAGE`；量纲方向跨图安全但不足以达到固定2%效果门。 |
| G32-I161 | TYPED-NO-SCHEDULED-PRESSURE-GUARD-ENTRY-NO-GO | V3R14删除scheduled pressure有南宁收益但伤害南宁36/37与map2；V3R17 typed双严格跨图安全但效果不足。一个单一局部guard可只在“历史winner是非goal type2、去掉raw scheduled项后的winner是更快type4”时翻转，试图保留收益并排除伤害。 | Candidate A stable完整trace上，规则在南宁1x/2x触发221/416次，target覆盖36/44个bag；有益76/80/84节点覆盖128/259次，已知伤害36/37为0，map2两scale也为0。但P95上尾两scale都只触及5个bag，低于约6/9的必要覆盖；2x两个插值支撑仅触及1个。 | 在实现前判coverage NO-GO；不把它与V17或V14失败mode叠加，不增加条件、阈值或节点清单。 | 只读trace，生产复杂度零增加；没有新scorer、文件或正式outcome。 | `NO_GO_BEFORE_IMPLEMENTATION_P95_BOUNDARY_COVERAGE`；规则确实消除了已知跨图/节点反例，但仍无法覆盖固定2%门所需秩位置。 |
| G32-I162 | V3R20-SERVICE-AWARE-STATIC-DOMINANCE-FROZEN | V3R17要求`travel+potential`与即时service双严格，虽跨图安全却漏掉2x第二个P95支撑包；继续加权或节点特判不允许。 | G31 canonical potential已按`H(u,g)=service(u)+min(travel+H(v,g))`包含候选即时及后续service，再单独要求即时service更短是额外保守过滤。只按更低`travel+H(candidate,goal)`扩展后，南宁1x/2x覆盖P95线以上9/15、16/19个bag并触及四个插值支撑，map2两stable slice仍0；这只是必要覆盖，不是结果预测。 | 在实现/outcome前冻结唯一新spelling：历史S4与strict descent后，只有非goal type2 winner存在shield-legal type4且其service-aware static total严格更低时，把最小`(travel+potential,node)`置顶；first-edge credit active时no-op。固定原十case、原阈值和off/A/A+guard三臂；core未过不补测量。 | 一个O(outdegree)既有候选重排和append-only拼写；无新状态、counter、参数、模型、地图/节点分支、路线后缀、全局读或预约。 | `docs/G4IRSF32_v3r20_service_aware_static_dominance_preregistration.md`已在实现/build/outcome前冻结；V3R20尚未实现。 |
| G32-I163 | V3R20-PREACTION-VERIFIED | 新规则需要证明不是通过改raw score或读取未来信息产生动作；首版测试把type4即时service改长后仍沿用0.1秒probe，导致候选尚在服务中而`shield_allowed=false`，另一次并行编译碰到Windows对象文件占用。 | 仅把probe与fault相对时序后移到“3秒服务已结束、blocker仍因fault形成本地压力”的同一语义状态，未改生产逻辑；编译冲突等并行会话结束后串行重跑，未清理或重配。随后旧S4稳定选择非goal type2，新拼写选择`travel+potential`更低但即时service更长的type4；两臂candidate raw score逐项相同，one-hop且full A*/global scan/future route/future schedule均为0。 | 实现只给S4增加append-only spelling并复用V3R17局部guard；新模式取消已被canonical potential包含的第二次即时service过滤。runner固定十case三臂，只有第三臂`scorer_mode`不同；未增加哈希、异常输入矩阵或额外遥测。 | 生产增量为一个模式判定与对既有one-hop ranking的O(outdegree)稳定前移；零新状态、阈值、权重、地图/节点特例和authority。 | fresh `build_g32_v3r20`：`event_driven_junction`与`pybind_smoke` 2/2通过；V3R20 runner紧凑测试3/3通过；正式十case outcome尚未运行。 |
| G32-I164 | V3R20-FORMAL-ENTRY-WIRING-FIX | 第一次启动正式screen时在任何case执行和outcome写出前抛出`E4 destination merge grants require an existing S1/S2/S3/S4 legal-local scorer`。 | 新spelling已在runtime canonical S4中实现，但Python请求层的三个S4集合与native binding的E4白名单仍停在V3R17拼写，属于append-only接线遗漏，不是动作或效果失败。仅把`S4_service_aware_static_dominance_rule_only`加入这四个既有集合；未改请求、阈值、case、scoring、queue、merge或安全语义。 | 保留同一预注册和正式输出路径；因两项输出均不存在，本次不登记为NO-GO，fresh绑定重编后从完整十case重新开始。 | 四个常量集合各增一个既有S4别名，零运行时状态与算法复杂度；没有为异常case增加新分支。 | 重编后`pybind_smoke` 1/1与runner测试3/3通过；正式输出仍不存在。 |
| G32-I165 | V3R20-FORMAL-CORE-NO-GO | 完整固定十case/三臂运行成功，但预先边界覆盖没有转化成足够闭环P95收益，并在部分南宁case产生新starvation。 | map2四例指标精确不变并全部PASS；南宁六例全部未达固定2% target-P95门：1x stable/active/inactive比值为0.99145/0.98675/0.99278，2x为0.99814/1.00020/0.99814。stable target P95仅493.804→489.584和511.62445→510.67513；1x stable/inactive starvation分别140→142、132→138，2x inactive 492→493；2x active还触发source-to-network transfer门。所有完成、安全、可测core资源与map2门通过，最大core资源比1.0821；RSS与完整mixed-origin积分未测。 | 依预注册判`NO_GO_V3R20_SERVICE_AWARE_STATIC_DOMINANCE`，不补RSS/完整mixed-origin测量、不进Stage 3，也不加service margin、系数、节点/类型清单或与V17/V18叠加；保留阴性结果并只读定位闭环反作用后再冻结独立最小后继。 | 本次只执行已提交规则，无新增复杂度；该static-dominance家族在失败后关闭。 | `outputs/tables/g4irsf32_v3r20_service_aware_static_dominance_screen.json`与`outputs/reports/g4irsf32_v3r20_service_aware_static_dominance_screen.md`；10 semantic cases、30 arms，campaign gate仅`all_nanning_core_gates=false`。 |
| G32-I166 | POST-V3R20-CLOSED-LOOP-BOUNDARY-ROOT-CAUSE | 历史trace上的1x 9/15、2x 16/19只是“至少一次可触及”而非闭环改善保证；需确认具体支撑包为何仍未越过2%线。 | stable重放与formal逐位一致。1x旧P95支撑653/694为491.344/495.444，V3R20后新支撑690/692为488.384/490.384；原15个边界bag中8快、5慢、2不变，653快8.96秒但694慢2.55794秒。2x旧支撑702/630为511.46833/512.50913，新支撑696/702为510.45013/511.95013；原19个中9快、10慢，702慢0.4818、630慢4.48秒。node36的45(type2)→37(type4)静态仅优0.168秒，却在2x候选37已有3--4个scheduled incoming，使504/557/596/630/678均精确慢4.48秒；边界aggregate虽service少45.5秒、merge wait少7.97875秒，junction queue却多42.2424秒。1x stable新增starvation袋均为非target并有route flip；另有convoy spillover。 | 这证明单次静态更短只改变共享队列到达相位并重新分配尾部，不能保证减少当前饱和日历工作；给node36类再加availability/margin只能退回V3R17的不足效果且违反V3R20停止规则。关闭整个static-dominance扩展，不做后置guard；后继必须是独立、可在实现前证明减少实际53/49工作或多袋等待总量的缝。 | 只读重放，生产复杂度零增加；没有fault扩扫、参数网格或新输出。 | 1x stable target source/network总量虽少9.48/210.20885秒，P95只少4.22秒；2x虽少7.17254/74.73793秒，P95只少0.94932秒且whole P95回归0.96658%。2x active更直接表现为source少7.17254秒而network多225.06769秒、total多217.89515秒。 |
| G32-I167 | TASK-CLASS-PRIORITY-ENTRY-NO-GO | 尝试用任务类别而非静态路由分数改变source或junction次序，优先storage-out看起来可让目标人口更早离开node53。 | 南宁node53的排队袋全部是storage-out，且同批release/deadline一致，source类别键完全退化为历史FIFO：两尺度均0次换序，两个P95插值支撑也均无动作。目标P95支撑袋本身都是storage-out，到了普通路口再优先只会把同一服务slot的等待转给on-path袋，不减少工作；map2也存在storage-out与on-path混合，不能提供结构性零作用保护。 | 在实现前关闭类别优先方向；不增加queue spelling、类别权重、地图保护分支或source/junction组合规则。 | 只读既有trace，生产复杂度零增加；没有新增文件、遥测或正式运行。 | `NO_GO_BEFORE_IMPLEMENTATION_CLASS_KEY_DEGENERATES_AT_SOURCE_AND_REALLOCATES_WAIT_DOWNSTREAM`。 |
| G32-I168 | LOCAL-MULTIBAG-FLOW-CONTROL-ENTRY-NO-GO | V3R20证明逐袋路由会在饱和队列间搬运尾部，因此检查一个参数自由、有限局部的多袋流控能否同时把足够多P95边界袋提前且不新增starvation。 | 固定2%门在1x/2x分别约需削减9.876/10.232秒，并至少推动约6/9个边界袋越线；node53/49的可用slot已持续满载。当前合法局部状态中，这些袋共享release、deadline、ready和FIFO特征，规则无法在不读袋ID、目标节点清单或结果秩的情况下区分受益者与让位者。把等待转给已经starved的袋虽然可能机械守住“新增starvation”计数，却不减少总等待且属于利用门定义，不是项目效果；map2有同构竞争，亦无天然no-op。 | 不实现多袋调度器，不加入窗口、配额、回看或结果驱动阈值。下一条候选必须有项目内物理/语义证据证明减少实际服务工作或增加真实容量，而非重排固定slot。 | 生产复杂度零增加；避免新增状态机和公平性参数。 | `NO_GO_BEFORE_IMPLEMENTATION_NO_FAIR_PARAMETER_FREE_LOCAL_SLACK`；现有one-hop局部状态不足以保证同时通过P95、mean、starvation与跨图门。 |
| G32-I169 | RESOURCE-SEMANTICS-CAPACITY-SEAM-NO-GO | 检查当前R3是否仍因整段边独占而人为压低53→49容量，以及R2/R4 entry-headway是否能作为真实减载后继。 | R3的`uses_corridor_calendar()`明确为false，非goal目的节点才使用service calendar；node49是indegree=1、service=1秒且有local source，因此53→49主等待来自共享node49服务窗口，不是edge calendar或E4 graph-merge grant。R2只会在R3上新增无权威物理值的headway，历史map2 144/512/2048又与R3完全相同；R4会因indegree=1取消node49目的服务窗口，且当前E4在runtime与两层binding均硬要求R3。 | 不切R2/R4、不调`entry_headway_seconds`、不假设并行lane或放宽service capacity。只有获得真实物理headway/并行服务证据后才可重开；当前继续找不改R3的算法后继。 | 只读既有G12审计、runtime语义和历史结果，生产复杂度零增加；未运行新矩阵。 | `NO_GO_RESOURCE_SEMANTICS_CAPACITY_SEAM`；R3已经是Java node-window兼容的无走廊日历语义，R4若变快来自取消共享服务资源而非更好调度。 |
| G32-I170 | SERVICE-WEIGHTED-LOCAL-WORK-ENTRY-NO-GO | 检查一个量纲一致且最小的S4后继：把`target_queue_length + target_scheduled_incoming`件数改为两者之和乘候选节点service秒数；map2非零服务节点均为1秒，因此有望保持原图。 | 用现有Candidate A stable请求开启完整trace后逐决策重排。南宁1x/2x分别有370/586次潜在flip，直接触及target P95的0.98边界8/15、7/19个袋；map2两尺度均0 flip。在固定基线轨迹中，把直接触及的target袋时延置零且不传播闭环影响，P95比值为0.94306/0.981646；2x旧P95支撑袋630未被直接触及。这个诊断说明直接覆盖不足，但不是闭环数学上界。 | 在生产实现和正式screen前判入口NO-GO；不增加scorer拼写、不把公式与V3R20或删除scheduled-pressure的失败mode叠加，也不扫描权重/组合。 | 四个stable slice的短只读trace重放，生产复杂度零增加；保留间接闭环影响可能改变未触及袋的证据边界。 | `NO_GO_BEFORE_IMPLEMENTATION_INSUFFICIENT_DIRECT_P95_COVERAGE`；判定依据是直接覆盖不足且历史闭环spillover风险高，不再声称2x数学上不可达。 |
| G32-I171 | V3R21-DRAFT-WITHDRAWN-BEFORE-IMPLEMENTATION | 曾草拟只保留`target_queue_length × candidate service`、删除scheduled count的S4公式；在写生产代码前复核了R3服务时序。 | scheduled incoming在dispatch时已经预留目标service calendar，继续计数会重复；而已到达junction queue的袋已完成该节点入站服务，其剩余阻塞取决于各自下一跳，乘当前候选节点service是错误资源，goal complete-on-arrival还会进一步破坏该解释。离线大覆盖主要把低service type-4翻向高service type-2，并非可信减载。 | 撤回草案，不实现、不build、不跑十case，也不保留一个无效scorer拼写。scheduled work继续由既有calendar表达；真实arrived-queue剩余工作若需逐袋读取下一跳，会突破当前one-hop/无新状态边界，因此不扩展。 | 生产与测试复杂度零变化；只保留这一台账记录。 | `NO_GO_BEFORE_IMPLEMENTATION_WRONG_RESOURCE_AND_CALENDAR_DOUBLE_COUNT`。 |
| G32-I172 | I170-EVIDENCE-BOUNDARY-CORRECTION | I170最初把固定trace的“触及袋置零”诊断写成数学上界，表述过强。 | 未触及袋仍可能通过闭环排队spillover间接变化，因此该计算只能证明直接覆盖弱，不能排除所有反事实闭环改善。 | 已把I170改为开放环覆盖警报，并保留原NO-GO决策：不凭不严谨上界实现公式，也不靠追加防御case掩盖语义问题。 | 文档纠正，算法零变化。 | 修正不会改写任何历史正式outcome或阈值。 |
| G32-I173 | TABLE-5.3-PROVENANCE-CORRECTION | 早期证据说明漏计了原项目中的结果工作簿，错误声称逐任务输入和row-level output均缺失。 | 两份原项目工作簿副本均保留28,506件输入、43,603段记录及Table 5.3汇总公式；真正缺失的是分散/MPC可执行实现、完整决策/冲突规则、参数与随机重复合同，以及把工作簿绑定到构建和地图修订的运行合同。 | 修正`G4IRSF32_evidence_gap_closure.md`；把工作簿作为archived exact output/context，但不冒充exact replay，不再继续无目标归档搜索。 | 仅纠正文档，算法零变化。 | 终态保持`TABLE_5_3_ARCHIVE_UNAVAILABLE_NON_EXACT_RECONSTRUCTION`，其含义明确为“精确可执行重放归档不可用”。 |
| G32-I174 | PAIR-5-7-PROVENANCE-CLOSURE-STRENGTHENED | 现有说明只列两套候选边，遗漏旧`arc.txt`字面line-ID映射，容易误以为再做一次性能probe即可裁决。 | 论文只给编号5/7；旧`arc.txt`字面映射为`4->17,6->8`，G26重建为`14->46,33->44`，故障sheet又标`33->44,46->36`；Java只消费端点，没有线路编号注册表。工作簿仅有13,939条时间样本，两个主要fresh probe均为8,013。 | 明确这是来源协议缺失，停止追加1x/2x性能probe，不选择有利解释；只有恢复原始fault-event provenance才重开。 | 文档纠正，算法与实验复杂度零变化。 | 1x/2x保持`ARCHIVED_ONLY_SOURCE_PROTOCOL_UNRESOLVED_NOT_MEASURED`。 |
| G32-I175 | P1.5-PORTABLE-THIRD-MAP-IMPORTER-PASS | 旧profile入口要求dense整数ID，并保留map2 storage `[52]`兼容；直接拿它导入第三图会迫使业务角色靠node type或alias猜测。 | 新portable schema把external ID稳定映射为dense ID，显式声明source/goal与`none`或命名EBS pair；独立workload/fault importer校验OD角色、唯一OD可达性和真实有向故障边。旧map2/南宁schema继续原路径。 | 只新增通用输入边界，不导入第三图、不选择proxy/fault、不投影航班，也不改核心算法。最终算法冻结后可直接物化预注册第三图，结果不得反向调参。 | 一个profile分支、一个独立输入模块和5个高价值测试；无地图ID特化、运行时状态或防御case矩阵。 | `py_compile`通过；portable/adapter/南宁相关测试29 passed、1 skipped（仅缺真实南宁源工作簿）；独立只读复核无blocker。状态`P1_5_PORTABLE_IMPORTER_COMPLETE_THIRD_MAP_NOT_RUN`。 |
| G32-I176 | R3-SERVICE-TIMELINE-AUDIT-NO-INVALID-WAIT | 检查53→49→50尾部是否来自重复service、空日历假占用、goal service误收或retry错过真实slot；若存在可用一个O(1)语义修正直接减时。 | R3对non-goal只预订一次`[edge_exit, service_end]`，edge exit后只完成一次service再入junction queue；goal complete-on-arrival不预订service。2x P95支撑袋630在node49的124.022秒和node50的15.752秒都发生在入队之后，是等待真实下游slot，不是重复服务。0.25秒retry下限的全部可回收缝已由I158量化为不足0.1% P95。 | 不修改service timeline、不删真实资源占用、不为微小retry缝增加事件逻辑。后继若要改变该尾部，只能增加有物理依据的容量或重新分配固定slot，不能伪称语义bug。 | 只读代码与既有trace，生产复杂度零变化。 | `NO_GO_R3_SERVICE_TIMELINE_EXACT_NO_INVALID_WAIT`；预订、服务、入队、goal终止逐事件闭合。 |
| G32-I177 | JUNCTION-HRRN-ENTRY-NO-GO | 检查普通junction按`wait_age / static_remaining`最大者先行，试图用无参数response ratio兼顾短作业均值与长等待公平；source FIFO、J2和物理slot保持不变。 | 四个stable trace的FIFO与等待重放逐次精确。规则覆盖南宁1x/2x全部15/19个P95边界袋，但固定slot重派使target P95分别从493.804升至520.804、从511.62445升至536.26833；1x whole P95/P99也越门，并新增13/17个starvation身份穿越。map2仍有323/745次潜在换序，不是结构no-op。 | 在实现前判NO-GO，不新增queue spelling，不扩展response-ratio公式或用权重补救；关闭普通junction单键重排方向。 | 只读四个stable短trace与固定slot重放，生产复杂度零变化。 | `NO_GO_BEFORE_IMPLEMENTATION_HRRN_FIXED_SLOT_TAIL_REGRESSION`；它只交换相同departure slots，未减少工作。 |
| G32-I178 | JUNCTION-EDF-ENTRY-NO-GO | 检查只把普通junction改为已有deadline/EDF语义、继续保持source FIFO和J2 comparator不变，能否按业务deadline提前P95边界袋。 | 四个stable完整trace离线重放中，南宁1x/2x两个P95支撑袋均未获提前，DIRECT边界覆盖分别为0/15、0/19；2x支撑袋630反被非DIRECT仲裁挤后。已饥饿袋在DIRECT换序中被挤后多于被提升（19:6、207:71）。map2 2x仍有4个真实DIRECT换序及更多待定仲裁分歧，也不是结构no-op。 | 在实现前判NO-GO；不为它新增junction-only queue拼写，也不把现有会同时改source的`deadline`开关直接带入正式screen。 | 只读四个stable短trace，生产复杂度零变化。 | `NO_GO_BEFORE_IMPLEMENTATION_EDF_NO_TARGET_BOUNDARY_COVERAGE_AND_STARVATION_DIRECTION`。 |
| G32-I179 | EXACT-TIME-QUEUE-PARETO-ENTRY-NO-GO | 检查一个不加权的局部Pareto后继：只允许strict-descent/shield合法、`travel+potential+calendar wait`严格更短且目标queue不更长的候选覆盖历史S4 winner。 | 四个stable trace上南宁覆盖9/15、16/19个P95边界袋并触及全部插值支撑，但与已失败V3R20覆盖完全相同；已知坏方向`36:45->37`仍触发139/245次，`37:38->46`仍触发38/82次。map2也新增7/28次flip，主要重现`22:26->24`坏方向。 | 在实现前判NO-GO，不继续叠加scheduled、node type、margin或阈值条件，也不跑正式screen。 | 只读四个stable完整trace，生产复杂度零变化。 | `NO_GO_BEFORE_IMPLEMENTATION_PARETO_RETAINS_CLOSED_LOOP_BAD_FLIPS_AND_MAP2_RISK`。 |
| G32-I180 | POST-V3R20-MINIMAL-SUCCESSOR-SPACE-BLOCKED | 在保持R3、one-hop/bounded-local、无新状态/模型/地图特化和原效果阈值时，需要一个既减少真实工作又覆盖足够P95秩位置的后继。 | service timeline逐事件闭合且实际slot持续占用；完整mixed-origin仲裁只交换唯一local bag，无法改变target P95；source/junction单键排序只重分配固定slot；S4 pressure/static/Pareto变体要么覆盖不足，要么产生convoy、starvation和map2回归。已到达queue的真实剩余工作必须读取每袋下一跳资源，超出当前边界；增加容量则需要外部物理依据。 | 停止继续堆guard、权重和候选拼写。要恢复主线，只接受两类新事实之一：权威的并行服务/headway/capacity数据，或用户明确授权放宽one-hop/无新状态/效果门中的至少一项；此前保持Stage3/4和held-out outcome不运行。 | 本轮后段只增加portable importer；所有失败算法候选均停在只读入口，未增加生产复杂度。 | `BLOCKED_NO_PHYSICALLY_VALID_MINIMAL_SUCCESSOR_UNDER_FROZEN_CONSTRAINTS`；不是测试或环境故障。 |
| G32-I181 | V3R22-PENDING-REQUEST-HOL-BYPASS-FROZEN | 续跑审计发现E4/JIT在FIFO owner已有pending request时仍把它重复选为handled-zero-edge；只要同队列还有unrepresented work，0.25秒wakeup会继续重复，其他ready bag不能发布请求或使用独立资源。 | 四个stable Candidate-A完整trace精确匹配源码状态缝：南宁1x/2x分别4,205/11,306次重复pending-owner选择，覆盖14/15、19/19个0.98-P95边界袋和全部四个插值支撑；map2也有1,909/5,343次，故必须保留跨图硬门。 | 在实现前冻结唯一spelling `fifo_junction_skip_pending_merge_owner`：只从既有local queue中跳过已有pending marker的owner，按原priority选择未表示袋；source FIFO、JIT/M3、exact calendar、retry cadence与容量不变。若全pending则保留原选择。固定原十case三臂，core失败即停，不加guard。 | 一个O(local queue)无分配扫描和append-only模式；零持久状态、参数、模型、地图特化、未来/两跳读、容量或新事件。 | `docs/G4IRSF32_v3r22_pending_request_hol_bypass_preregistration.md`已在实现/build/outcome前冻结；当前仅入口证据为正，尚无V3R22 outcome。 |
| G32-I182 | V3R22-ENTRY-ATTRIBUTION-CORRECTED-BEFORE-IMPLEMENTATION | 首版只读probe把“重复选中的pending owner覆盖尾部”写成action coverage；pending owner本身不会因skip直接更早commit，因此2x的19/19与四个support不能作为直接收益覆盖。 | 独立重放每个pending interval内的完整junction queue：实际filtered-first未表示候选覆盖南宁1x 14/15且2/2 support，2x 18/19且仅support 630（1/2）；但4,029/11,074次重复选择时确有替代，336/817个episode均存在替代，其中56/99个首选后来去不同destination、7/15个后来DIRECT，证明存在独立工作入口。map2同样活跃且不同destination为92/441、131/1,058，不能假定no-op。 | 在任何native实现、build或outcome前纠正文档表和收益解释，不改变冻结动作、case、arm或阈值；保留“只证明seam、不预测2%结果”的边界。测试首次因系统Temp目录ACL失败，改用worktree内显式pytest basetemp后3/3通过，未增加防御case。 | 生产复杂度零变化；runner第三臂只有`queue_discipline`一个delta。 | 该修正避免把owner覆盖误报为beneficiary覆盖；V3R22仍只获一次固定十case闭环screen资格。 |
| G32-I183 | V3R22-NATIVE-PREACTION-PASS | 需要证明HOL bypass只发布另一个request，不会取消旧request、绕过M3直接commit或改变source/old mode。首轮motif把runtime bag误按1-based观察为2/3，并曾与并行编译争用同一OBJ；生产trace其实已显示新模式选择正确。 | 将夹具观察ID改为runtime实际0-based的A=1/B=2并串行复跑：旧FIFO在第二机会仍选A、edge=0且B无request；新模式选B、edge=0并只生成pending，A request/lineage与destination calendar generation不变，active capability仍0；全pending回原FIFO，source admission精确，最终3/3完成。 | 不改生产规则，只纠正夹具ID并停止并行争用；fresh V3R22 Release的`event_driven_junction`与`pybind_smoke` 2/2通过，runner 3/3通过。 | 生产只增加一个filtered local chooser与append-only拼写；O(local queue)，零状态/counter/参数/容量/模型/地图分支。 | 正式固定十casecore screen已获执行资格，尚未产生outcome。 |
| G32-I184 | PHYSICAL-CAPACITY-EVIDENCE-NOT-MAPPED | 权威T2资料给出设备类别额定吞吐和工程线路capacity，南宁工作簿也有逐边托盘容量；但它们没有到当前dense node/edge的crosswalk，map2又存在设备聚合。 | 资料足以证明真实线路吞吐并非常数，却不足以把node49/50/53设成并行server或把某条边换成特定headway；南宁edge capacity当前仅metadata，接入它只会增加约束，不能减少关键节点等待。 | 不把这些数值硬贴到当前图，不扩展V3R22容量/headway逻辑；未来只有在补齐physical asset/line到graph的权威crosswalk后才开放该方向。 | 本轮生产复杂度零变化，避免为效果加入缺乏映射的物理参数。 | 保留为后继证据边界，不阻塞当前独立HOL修复的正式screen。 |
| G32-I185 | V3R22-FORMAL-CORE-NO-GO | 固定十case/三臂全部完成；HOL消除显著扩大同时pending集合，峰值比为12.333--64.000，十例均越过1.10资源门并新增starvation。 | 南宁target-P95比：1x stable/active/inactive为1.031385/0.996966/1.028710，2x为1.021099/0.995536/1.021099，六例均未到0.98；stable/inactive尾部恶化，active虽小幅改善仍不足。map2 1x P95改善约6.9%但starvation从0增至10/9，2x P95近似不变且starvation 158→270、154→266。南宁2x whole mean与总network time普遍改善，说明动作不是纯开销，但收益集中分配造成尾部与公平性失败。 | 依冻结规则判`NO_GO_V3R22_PENDING_REQUEST_HOL_BYPASS`：不补RSS/完整mixed-origin积分、不加pending阈值、同destination guard、立即多dispatch或节点特例；保留结果并转向独立seam只读审计。 | 生产复杂度保持已提交的一个O(local queue)规则；本次仅执行固定screen，无后置参数化。 | `outputs/tables/g4irsf32_v3r22_pending_request_hol_bypass_screen.json`与`outputs/reports/g4irsf32_v3r22_pending_request_hol_bypass_screen.md`；10 case/30 arms，南宁与map2 campaign gate均false，Stage3未授权。 |
| G32-I186 | POST-V3R22-MERGE-LIFECYCLE-SEAMS-CLOSED | 检查是否还有stale request、grant拒绝重试、DIRECT离队遗留或错误calendar wake可在不增加pending下删除真实等待。 | 历史FIFO+Candidate A固定十case共69,998个request、349,990条lifecycle全部严格`REQUESTED→ISSUED→PREPARED→COMMITTED→CONSUMED`；expired/revoked/rolled-back、queue-capacity、active-grant、exact-slot-busy与contended-loser均0，最终outstanding/active均0。5,361个stale wake在generation/time门后立即return，未触碰bag/queue/calendar。 | 关闭cleanup、retry和stale-wakeup效果候选；后者最多减少事件处理，不能改变模拟时间或target等待，不新增housekeeping mode。 | 只读重放，生产复杂度零变化。 | 当前真实分布不存在可删除的merge lifecycle无效等待。 |
| G32-I187 | POST-V3R22-DIRECT-AND-INDEPENDENT-DESTINATION-ENTRY-NO-GO | 为避免同destination pending爆炸，检查只旁路到DIRECT/goal-exempt或不同且空闲destination是否能覆盖南宁尾部。 | 南宁1x/2x的episode-first不同destination仅56/336、99/817，覆盖0.98-P95边界3/15、7/19且各只命中1/2插值支撑，低于约6/9个边界袋的必要覆盖；其中DIRECT仅7/15个episode，两个尺度均0个boundary、0个support。map2入口仍活跃，不是天然no-op。实现DIRECT预选还需对多袋dry-run scorer并隔离副作用，约O(queue×outdegree)。 | 在实现前判NO-GO；不把V3R22改造成same-destination guard、pending cap或多袋dry-run选择器。当前约束下继续需要权威physical asset/line→graph crosswalk，或明确放宽one-hop/无新状态边界；不放宽效果阈值。 | 生产复杂度零变化，避免为零tail覆盖加入新选择层。 | `BLOCKED_NO_EVIDENCED_MINIMAL_SUCCESSOR_AFTER_V3R22`；Stage2仍未GO，Stage3/4与held-out outcome继续禁止。 |
| G32-I188 | TARGET-PHYSICAL-CROSSWALK-BOUNDED-AUDIT | 为判断能否用真实并行服务/headway重开最小后继，对源工作簿、canonical profile、map2旁车资料、T2原理图和13页仿真报告做有界identity审计。 | 南宁dense 49/50/53已精确反解为`IU24/ICS50` loader、`IU25/ICS51` divert、`IDK1/ICS54` empty-pallet storage；相邻边`53→49/49→50/50→34/50→25`也精确绑定源表行和托盘容量75/20/46/10。但源表没有物理资产号、工程line code或真实坐标，T2资料也不含这些目标alias/ICS ID。唯一共享别名`GTC1/GTC2`属于南宁dense 134/135；另一个可桥接T2数据的数字`node 53`属于独立map2，不能与南宁dense 53混同。 | 保留已证实的source→dense crosswalk，同时拒绝按相同整数、设备类型或布局相似性把T2吞吐/线路capacity贴到南宁热点；不实现并行server/headway猜测，也不扩大到DWG或巨型归档盲搜。只有新增采用`ICS50/ICS51/ICS54`或`IU24/IU25/IDK1`的权威资产表/线路表，才可安全重开该方向。 | 只读审计和一条台账记录；生产复杂度零变化，无新guard、状态、参数或地图特化。 | `NO_GO_TARGET_PHYSICAL_ASSET_CROSSWALK_MISSING`；这次已闭合源表身份，不足以授权Stage2新动作，Stage3/4与held-out outcome仍保持未运行。 |
| G32-I189 | POST-I188-LAST-ZERO-STATE-SEAMS-NO-GO | 物理crosswalk缺失后，对代码/测试和四个stable trace再做一次有界收口，只检查无需新状态的最后两个独立缝：现有JIT ready-set改用M1/FIFO，以及grant成功COMMIT并从upstream queue移除winner后立刻唤醒single-flight后继。 | M1在南宁1x的6,371个service opportunity中只有1次M3→M1换序，未命中15个0.98-P95边界袋或两个插值support；南宁2x的10,290个opportunity为0次换序，而map2 1x/2x仍有3/12次，故不是有效target入口或结构no-op。post-grant handoff虽命中南宁全部边界袋，但每次最多只回收0.249秒retry余量；把收益100%无损直接扣给受益袋的乐观P95比也仅0.9996946/0.9995063，远低于0.98，map2乐观比为0.9988955/0.9990485且广泛活跃。另确认JIT已过滤exact-slot-ready，69,998个request生命周期无可删遗留，source-only与多袋dry-run方向分别已由I156/I187关闭。 | 两项均在实现前判NO-GO，不新增模式、正式screen、参数、guard或测试矩阵；M1只做25秒内存probe，handoff只读既有trace。后续不再把fixed-slot重排或不足0.25秒的软件调度余量包装成容量收益。 | 生产复杂度零变化；没有新运行时代码或持久输出。 | `NO_NEW_CODE_MINIMAL_SUCCESSOR_AFTER_CODE_AND_TRACE_AUDIT`；当前恢复主线需要目标物理资产/headway资料，或明确改变冻结的局部状态/可见性边界，效果阈值不放宽。 |
| G32-I190 | NONRETRY-ONLY-PENDING-OWNER-SKIP-ENTRY-NO-GO | V3R22的pending爆炸主要由0.25秒retry反复物化backlog触发，因此检查只在真实非retry junction wake时跳过已有pending owner、周期retry仍保持FIFO，能否保留一次新到独立工作入口并切断放大器。 | 用南宁1x stable在内存重生Candidate A与HOL trace并重放wake来源；入口门采用比候选更乐观的上界——直接删除所有非arrival时发布的request。即使如此，arrival触发的同时pending峰值仍为38，而off仅3，资源比12.667，已远超固定1.10门。真实候选只会保留更多请求，不可能优于该上界；按“任一case资源门失败即停”，无需继续2x、map2或P95覆盖计算。 | 在实现、预注册和正式screen前判NO-GO；中止余下四slice重生，不增加`event.retry`参数接线、queue spelling、测试或输出。 | 只读单slice早停，生产复杂度零变化，避免为必败候选再跑30臂。 | `NO_GO_BEFORE_IMPLEMENTATION_NONRETRY_ONLY_SKIP_PENDING_STILL_12P667X`；V3R22放大并非仅由周期retry造成，真实arrival burst本身已越资源门。 |

## 已冻结、待复验的关键工件

- 原 G31 Release baseline binary：
  `C:/tmp/g4irsf32_v3r2_g31_build/python/Release/czr005_cpp.cp311-win_amd64.pyd`；
  SHA-256
  `35A43037B0881ACA3B92732541126EE71C2D431D537A13E07918777C8B7CCE59`。
- map2 原文件 SHA-256：
  `9e8c5a236869336cf4c05a09a8ce0554f440eb45a6896972fc54116bcf78bbb4`。
- map2 canonical profile SHA-256：
  `3659dffdaf412739a69066b6c79dba4b83e4e3144612235f335f7c7aa5a7e323`。
- map2 2.5 m/s potential SHA-256：
  `c96d2095404d042558858d175db460af1faf378853a8ebdac9a92767e617e006`。
- map2 首 8 个 sentinel rows SHA-256：
  `96f5d5447275fee06b8d9234b42f5b57004f0617304d08a89d19aa3a646e4803`。

以上只表示身份冻结，不表示 Stage 0/1 已通过。真正结论必须来自干净分支的
Release build、跨二进制 exact-off、全部 Stage 0 probes 和固定 120-case Stage 1。

## Gate 运行记录

| Gate | 运行状态 | Verdict | 证据 |
|---|---|---|---|
| P0 literal/config/schema audit | 已完成（数据前） | PASS_PRE_DATA | V3R4逐字段覆盖、exact schema、配置门、来源与代数负测已闭合；不等价于Stage 0/1效果通过。 |
| Supplemental V3R3 Nanning P0 control | 已完成 | TERMINAL_NO_GO_NO_EVENT | attempt4在同一冻结cohort上确认1x/2x均0 qualifying event；G32未执行。该追加门永久保留阴性，不改写为原MD Stage 2结论。 |
| Supplemental V3R5 commit-aligned Nanning control | 已完成 | TERMINAL_NO_GO_STALE_HARD_GATE | 1x/2x均观察到14/19个真实事件，但41/155 stale arbitration违反冻结安全门；attempt1永久保留，G32未执行。 |
| Supplemental V3R6 bounded commit-aligned Nanning control | 已完成 | TERMINAL_NO_GO_STALE_HARD_GATE | 1x/2x均观察到4/2个真实事件，但11/21 stale arbitration仍违反冻结安全门；attempt1永久保留，G32未执行。 |
| Supplemental V3R7 minimal pre-arrival overlap control | 已完成 | PASS_G31_CONTROL_STAGE0_STAGE1_ALLOWED | 两尺度各1个真实qualifying event，6/3全部完成且两类stale均严格为0；G32未执行，仅开放Stage 0/1。 |
| Stage 0 contract/exact-off | 已正式运行（V3R7 attempt1） | PASS | preflight与Stage0全部硬门通过；只授权Stage1，不授权P1。 |
| Stage 1 fixed 120 cases | 已正式运行（V3R7 attempt1） | TERMINAL_NO_GO_PRIMARY_IDENTIFICATION_DEGENERATE | 120/120执行；20例因event-order审计假负门，独立primary方向层为0是真NO-GO；原工件append-only保留。 |
| P1 action review（历史checkpoint） | 当时未启动；已被V3R12/V3R13取代 | SUPERSEDED_BY_LATER_APPEND_ONLY_RECORDS | 原记录不删除；当前`closed_loop`已实现并通过pre-action，见下方Candidate A行。 |
| Stage 2 real slices（历史checkpoint） | 当时未运行；已被V3R13+取代 | SUPERSEDED_BY_LATER_APPEND_ONLY_RECORDS | 原记录不删除；当前多轮固定十casecore screen已执行，Stage 2效果仍未GO。 |
| Stage 3 selective full | 未运行 | BLOCKED_BY_STAGE_2 | — |
| Stage 4 complete matrix | 未运行 | BLOCKED_BY_STAGE_3 | — |
| Held-out third map | 候选已预注册、通用importer已完成、地图未物化未运行 | BLOCKED_UNTIL_FINAL_ALGORITHM_FREEZE | SCL `REAL_PROVENANCE_ABSTRACTED_TOPOLOGY` 已冻结；portable external-ID/role/workload/fault边界已通过29项测试。最终算法未通过Stage2，当前不存在unseen verdict。 |
| P1.5 evidence closure | 已完成 | PASS_WITH_EXPLICIT_TERMINAL_BOUNDARIES | Table 5.3 provenance已纠正；Table 5.4、`pair_5_7`和真实南宁EBS均保留明确NON_EXACT/NM/NOT_SUPPORTED边界；第三图通用importer完成。 |
| Post-V3R20 minimal successor search | 阻点已重新审计并发现遗漏seam | REOPENED_BY_V3R22_PENDING_REQUEST_HOL_ENTRY | I180在当时证据下保留；续跑发现重复pending owner造成request-publication HOL，不需增加容量或未来状态，V3R22已预冻结待pre-action。 |
| Stage 0/1 final G32 prerequisite | 已正式运行并在V3R12复验 | PASS | immutable V3R11 clean-build工件的120 safety + 24 identification经full deep replay全部通过；synthetic executor未重复调用。 |
| Supplemental V3R12 arrival-covered Nanning P0 | 已正式运行 | GO_P1_REVIEW_ALLOWED | 两尺度G31 control与G32 shadow全部硬门通过，各1个真实mixed-origin stored observation；明确仅为engineering existence canary，不作effect estimate。 |
| P1 Candidate A closed-loop | 已正式运行 | PASS_STAGE2_AUTHORIZED | 新V3R13 Release binary完成8 case/24 execution；DIRECT/J2动作、反向优先、三类no-op、future perturbation、off/shadow、安全与资源门全部通过。 |
| Stage 2 candidate real slices | 已正式运行并作一次measurement-only corrected复验 | NO_GO_CANDIDATE_A_EFFECT_INSUFFICIENT | 同一预冻结10 case/20 paired executions全部完成；hard safety与map2 sentinel通过，但南宁target P95 6/6无改善，start-49等待代理也未达5%。Stage3/4未授权，按顺序转Candidate B。 |
| Candidate B fixed three-arm core screen | 已正式运行 | NO_GO_CANDIDATE_B_CROSS_MAP_REGRESSION | 同一10 case执行off/A/A+B共30臂；南宁P95有小幅改善但6/6未达2%，map2全四例回归且2x queue/starvation超过硬门；未补非必要RSS/连续积分。 |
| Candidate C entry trace | 已完成只读必要性诊断 | NO_GO_NO_USEFUL_STRICT_GUARD_BLOCK | 南宁/map2 1x稳定完整trace均无raw-best被strict guard阻挡，也无正calendar saving且覆盖势值/行程代价的非下降替代；按P2条件不实现token。 |
| V3R15 Candidate A commit-recheck pre-action | 已完成 | PASS_REAL_SCREEN_AUTHORIZED | DIRECT/J2行为差异、首个commit不可撤销、one-owner、no-source、off/history兼容、安全和backend mode echo通过；固定10-case三臂screen可运行。 |
| V3R15 Candidate A commit-recheck core screen | 已正式运行 | NO_GO_NO_REAL_SLICE_DELTA | 固定10 case执行off/A/A+recheck共30臂；A+recheck与A的动作数及确定性结果完全相同，南宁target P95六例均无改善，map2 exact且安全通过；Stage3未授权。 |
| V3R16 S4 plus uncovered local work | 已实现、预动作通过 | PASS_REAL_SCREEN_AUTHORIZED | 新spelling严格复用旧S4后追加一个现有pure helper；native/backend/runner及独立复核通过，同10-case三臂正式screen可运行，输出尚不存在。 |
| V3R16 S4 plus uncovered local work core screen | 已正式运行 | NO_GO_NO_REAL_RANKING_DELTA | 同10 case执行30臂；新scorer与历史A的确定性结果逐字段相同，南宁target P95仍6/6无改善，map2 exact且安全；不补非必要遥测。 |
| V3R17 typed service dominance | 已实现、预动作通过 | PASS_REAL_SCREEN_AUTHORIZED | 单一typed motif、native identity/backend入口与固定三臂runner通过；生产规则只重排已物化one-hop候选，不改分数、日历、预约或旧scorer。正式10-case/30臂screen可运行。 |
| V3R17 typed service dominance core screen | 已正式运行 | NO_GO_EFFECT_BELOW_2PCT | 同10 case执行30臂；map2四例exact，南宁六例target P95均有改善但仅约0.47%--0.98%，且两个1x case新增starvation阈值穿越。未补RSS/连续积分，Stage3未授权。 |
| V3R18 source release-tie longest-static | 已实现、预动作通过 | PASS_REAL_SCREEN_AUTHORIZED | 同刻source tie、不同enqueue时刻FIFO、junction exact与旧fifo方向均由一个小motif覆盖；fresh native/binding和三臂runner通过，正式10-case/30臂screen可运行。 |
| V3R18 source release-tie longest-static core screen | 已正式运行 | NO_GO_MEAN_AND_STARVATION_REGRESSION | 南宁target P95多例明显改善，但全系统mean和starvation全部越门，map2也出现mean/tail回归；闭环结果否定固定slot离线假设，未补非必要RSS/完整积分，Stage3未授权。 |
| V3R20 service-aware static dominance core screen | 已正式运行 | NO_GO_EFFECT_BELOW_2PCT | 固定十case/30臂完成；map2 exact，可测core资源通过，但南宁六例P95均未到2%，部分starvation与wait-transfer失败；RSS/完整mixed-origin积分按止损规则未测。 |
| V3R22 pending-request HOL bypass | 已实现、pre-action及固定十casecore screen已完成 | NO_GO_PENDING_EXPLOSION_STARVATION_AND_P95 | pre-action证明真实HOL被消除；正式30臂显示pending峰值放大12.333--64.000、十例新增starvation，南宁六例P95均未达2%，未补RSS/完整积分，Stage3未授权。 |
| Post-V3R22 minimal successor search | lifecycle、DIRECT/independent-destination、目标physical crosswalk及全部已发现零状态代码缝已完成有界审计 | BLOCKED_NO_EVIDENCED_MINIMAL_SUCCESSOR | merge lifecycle无无效等待可删；DIRECT/different-destination覆盖不足；M1/FIFO不触及南宁P95；post-grant handoff乐观收益不足0.05%；nonretry-only skip即使采用更乐观的request删除上界，南宁1x pending仍为12.667x。南宁49/50/53虽已还原到源表alias/ICS ID和边容量，但没有到物理资产/工程线路/并行服务能力的权威映射。继续需目标资产表，或明确扩展状态/可见性边界。 |
