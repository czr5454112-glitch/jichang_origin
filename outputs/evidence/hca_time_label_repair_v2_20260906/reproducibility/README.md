# HCA 时间标签修复 V2：对照与新构建留存

此目录只整理已完成两格实验的复现支持材料，没有运行模拟、修改冻结文件、复制原材料或改变旧源码。

- `hca_time_label_repair_v2_controls_20260906.json` 是本机 `tmp` 中同名对照记录的完全相同字节副本。
- `hca_time_label_repair_v2_build.zip` 只包含新目录 `build/hca_time_label_repair_v2_20260906` 的 28 个 class 和 `build_identity.json`，共 29 项；没有混入旧构建。ZIP 按成员相对路径排序，时间固定为 1980-01-01 00:00:00，权限固定为 0644，采用无压缩 ZIP_STORED，避免压缩库版本影响输出字节。独立生成两次所得字节相同。
- `reproducibility_index.json` 绑定原文件/副本/ZIP及每个成员的 SHA-256 与字节数。所有 class 还与构建身份清单中的逐文件 SHA 比较；逐成员解压核对后，再复查了源构建和对照原件没有发生字节变化。

这些材料用于保留本轮对照与确切构建，**不构成完全可移机复现包**。冻结记录中的绝对路径，以及对原始材料、legacy 和旧工具共 64 个原件的保留校验，仍依赖当前主机及相应文件；本目录未复制这些原件。将 ZIP 解压到一个新目录可以恢复该构建的成员字节，但不会自动重写冻结命令、补齐原始输入或消除外部路径依赖。
