# DH 原生截尾结果的解析恢复（2026-09-07）

30 分钟监视发现首阶段在 15 格有效、1 格后处理失败时停止。失败格为南宁 2×、2.5 m/s、种子 104729 的 CIE-DH V6。Java 已正常结束，返回码 0，状态 HORIZON_REACHED；未完成段的时间使用合法的 `N/A`，旧 Python 读取器没有接受该值。

新 `run_feng_dh_paper_suite_v2.py` 只修正缺失时间的解析并拒绝无穷/畸形数字。方法仍为 FENG_DH_PAPER_SUITE_V6，Java 源码、类文件、JDK、物理机制、工作负荷、释放 D 和 98259 秒时限不变。恢复专用 spec 禁止从普通运行入口重新执行 Java。

`recover_feng_dh_paper_suite_v2.py` 校验原始 spec、真实 Java 命令、构建及九个原始文件 SHA，将原生文件逐字节复制到新 `_postprocess_v2` 目录，再计算完整人口账目并验证压缩归档。原始失败目录、状态与调度日志均保留；新目录显式记录 `native_reexecuted: false`、原始 native runner SHA 和新的 normalizer SHA。

该格 57,012 件中完成 49,638 件，87,205 段中完成 79,460 段。完成率 87.0659%，未完成原始袋 7,374 件；六项总体 THT 均为空。固定时限未完成是需要报告的算法结果，不等于执行器错误；不能计算幸存者均值来填总体 THT。

新权威计划为 `../frozen_plan_v5/plan.json`，SHA256 `00a95b47298fb1aac808bec9275f6d9b3c28d340859017c0667e39ff736b9719`。V4 保留为历史。V5 保持全部 1,760 个实验坐标及 15 格已验收记录不变；116 个尚未运行 DH 格只换解析器绑定，另 1 个恢复格使用新 ID、spec 来源记录和输出目录。基础 480 格、断线 1,280 格；断线仍只有 G31/HCA、2.5 m/s，无速度偏差实验。

验证证据包括 25 项解析测试（含三个已完成真实 DH 格在两版解析器下逐袋与汇总完全一致）、原始数据独立诊断、恢复来源与参数篡改拒绝检查，以及混合解析器调度/报告兼容性检查。详细范围以各验证文件为准；终态检查不是额外的完整逐时间步物理证明。

此恢复未启动新的 Java 仿真，未提交或推送仓库。后续 master 在逐格重新验证后复用已验收结果，再继续剩余实验。全部 1,760 格验收及最终汇总完成前，不能宣布整套实验完成或给出最终排名。
