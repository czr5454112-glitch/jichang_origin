# DH 固定时限 N/A 解析：独立只读诊断

此目录仅保存已完成诊断的字节副本和原生文件 SHA256 清单。冻结时再次核对全部原文件 SHA，未编辑原输出、runner、Java、status 或恢复脚本，也未运行 Java。

Java 正常返回 0，并在 98259 秒固定时限以 HORIZON_REACHED 结束；已完整输出 57012 袋、87205 段。完成 49638 袋、79460 段，其余人口继续保留。原外层退出 1 的直接原因是 Python 未识别未完成值 N/A；固定时限截尾本身不是执行错误或已证明的算法故障，但未完成量仍是必须披露的实验观测。

全部 canonical 身份、OD、释放时刻、已完成目标和时序、逐袋 CSV 与 summary/event 人口计数一致。终态 93 个边上袋的坐标范围和 footprint 未发现异常；无完整逐 tick trace，因此不据此声称全部连续物理行为已证明。

只在独立诊断命名空间为原 normalize 提供 N/A 解析后，审计通过；六个正式 THT 字段全部为 null，不能以幸存者均值代替。完成率及两种 deadline 成功率仍采用全部 57012 袋分母。

复查命令（工作树原位置，纯读取）：

    python tmp/diagnose_dh_na_horizon_20260907.py

所保存脚本依赖当前工作树模块和记录的绝对输入路径。副本是源代码证据，不能从本归档位置直接当作完整便携运行环境执行。
