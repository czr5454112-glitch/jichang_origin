# Final zero-through correctness repair: independent P2 evidence

All P2 workload regressions passed with final Java source aggregate SHA
`3b47ffcefa558365e55e27508fc8904608026fd3235102eee6c305539999a208`.
The five production sources use CRLF bytes to reproduce the frozen Windows
checkout identity. Production class aggregate SHA is
`21fc22d8cd27628e2933eb73256cafa6f2bd695628fd46988ea445aafd7a5d47`.
The audit class identity additionally includes the independent OD harness.

| Check | Original source `99bf...` | Final repair `3b47...` |
|---|---:|---:|
| Actual map2 formal ODs, independent single bags | 25/25 complete | 25/25 complete |
| Actual Nanning formal ODs, independent single bags | 149/496 complete | 496/496 complete |
| Nanning ODs traversing a zero-time intermediate node | 0/347 complete | 347/347 complete |
| Nanning topology witness 130→57→58 | Repeated zero service | Complete at tick 251 |
| Original map2 population | 28,506 bags / 43,603 segments | Byte-identical bag and segment outputs |

`formal_od_coverage.json` verifies all 60 frozen formal workload files (both
maps, three loads, ten seeds) against their recorded raw SHA-256, raw population
and expanded segment population. Directed ODs were deduplicated after complete
expansion; jitter-induced early/direct classification changes remain covered.
The original Table 5.3 schedule's 25 ODs are also covered. Map2 covers 25
zero-time goals; Nanning covers 11 ODs with a zero-time goal. Every one of the
174 OD tests with no zero-time intermediate node has an identical complete
audit record, including service events and completion tick, before and after
the final repair.

The formal business witness is raw bag 7007, segment 0, from Nanning 1× seed
104729, original raw row `7007 25734.13853 30000 0 20 ID21 ID1`. Its 0→20 route
passes through node 56. At tick 690 the original program instantaneously ends
the zero service but retains edge 2, position 40 and MOVING_ON_EDGE; tick 691
repeats the service. The final repaired isolated bag completes at tick 755.
The two `*_formal_business_witness.json` files retain exact source provenance
and service events. The original program's failures stop immediately after
the second invalid restart. Every isolated test resets release to tick 0.
These OD results are correctness checks, not population performance estimates.

`map2_full_population_regression.json` verifies every field of all 28,506 bag
records and 43,603 segment records byte for byte against the original primary
run. Event summaries and trace outputs are identical too. The only changed
summary field is wall_seconds (31.2096943 versus 32.1528051). Complete native
files remain local with their SHA-256 values recorded; repaired native
summary, event summary and runner identity are under
`map2_full_population_repaired/`.

`single_bag_od_comparison.csv` is the compact per-OD review surface. Six
`.jsonl.gz` archives retain all detailed service-event audit records;
`single_bag_equivalence_and_archives.json` binds compressed and original bytes.
The original-version OD evidence was copied byte for byte from the completed
audit rather than rerun. `../regression/` retains the superseded intermediate
`3f3d...` implementation evidence; the final repair additionally fixes Z12
same-tick zero-service release visibility before edge-entry arbitration.

Reproduction from the repository root, with the formal workloads already
generated by the external robustness runner:

```powershell
$evidence = 'outputs/runtime/feng_cie_dh_zero_through_repair_20260905/regression_final'
python scripts/eval/audit_feng_zero_through_regression.py prepare --output-dir $evidence
python scripts/eval/audit_feng_zero_through_regression.py prepare-old --output-dir $evidence
python scripts/eval/audit_feng_zero_through_regression.py audit --version old --source-dir tmp/feng_zero_through_old_sources/App --output-dir $evidence
python scripts/eval/audit_feng_zero_through_regression.py audit --version repaired --output-dir $evidence
python scripts/eval/audit_feng_zero_through_regression.py regression --output-dir $evidence
python scripts/eval/audit_feng_zero_through_regression.py summarize --output-dir $evidence
```

The original source is recovered with read-only `git show` from audited commit
`f101c2f6c21bd4a147e060ba09bf95b26b48b50c`, and aggregate SHA
`99bf695a787accce5780996d06bbc8eb816992169ef8b731e8116a49c10f14d8` is asserted.
The harness uses independent build directories and changes neither maps nor
simulator semantics. Existing run directories are protected against overwrite;
use a fresh evidence directory when reproducing the population run.
